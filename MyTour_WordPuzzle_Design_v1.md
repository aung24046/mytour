# MyTour — ปริศนาใบ้คำ (Word Puzzle) Design v1

**วันที่:** 26 ส.ค. 2026 · **แก้ครั้งที่ 4** · **สถานะ:** เฟส 1 เสร็จ + รันบน Supabase จริงแล้ว

**แก้ครั้งที่ 4:** รัน migration บน Supabase จริงแล้ว — ผลอยู่ที่ **ข้อ 15.8**

**แก้ครั้งที่ 3:** เขียนเฟส 1 เสร็จ — สิ่งที่ต่างจากแบบและผลทดสอบอยู่ที่ **ข้อ 15**

**แก้ครั้งที่ 2:** รูปใบ้ **1-6 รูป** และ**ระบบจัดเรียงให้เองอัตโนมัติ** (ข้อ 4.7) ·
คำใบ้ตั้งได้ **สูงสุด 3 ขั้น** · **ตัดการขอคำใบ้เองของผู้เล่นออกทั้งหมด** — คนคุมเกมเป็นคนเปิดอย่างเดียว (ข้อ 4.3)

**แก้ครั้งที่ 1 (หลังได้ตัวอย่างจริงจาก `เกมใบ้คำ_ตัวอย่าง.pdf`):**
คะแนนเป็น **1 แต้มต่อข้อ** ไม่ใช่ 1000 แต้มถ่วงความเร็ว (ข้อ 6) · **ตอบได้ไม่จำกัดครั้ง** (ข้อ 4.2) ·
รูปใบ้ **2-6 รูป** · **ผู้เล่นขอคำใบ้เองได้ตามโควตา** ไม่ใช่ทีมงานเปิดอย่างเดียว (ข้อ 4.3) ·
เพิ่ม **ภาพเฉลย** (ข้อ 2) · เพิ่มสกินจอเวที **`arcade`** (ข้อ 7.1) · ตัดช่อง `▢▢` รายตัวอักษรทิ้ง

**สิ่งที่จะได้:** เกมทายคำจากภาพ — จอใหญ่ขึ้นรูปใบ้ 1-6 รูป (ระบบจัดเรียงให้เอง) กับป้าย "3 พยางค์"
ลูกทัวร์เห็นรูปเดียวกันบนมือถือตัวเอง แล้วพิมพ์คำตอบส่งเข้ามาได้ไม่จำกัดครั้ง
คนคุมเกมกดเปิดคำใบ้ให้ทั้งห้องได้สูงสุด 3 ขั้น ตอบถูกได้ 1 คะแนน คะแนนรวมมากที่สุดชนะ

**หลักการ:** ADDITIVE เป็นหลัก — ไม่สร้างเกมใหม่ทั้งใบ แต่**ต่อยอดเครื่องยนต์ควิซที่รันจริงแล้ว**
ของเดิมที่ต้องแก้มีจำกัดและระบุไว้ครบในข้อ 10 ห้ามแตะ `bingo_*` / `draw_*`

---

## 0. สรุปการตัดสินใจ

| หัวข้อ | ตัดสินใจ | เหตุผลสั้นๆ |
|---|---|---|
| สถาปัตยกรรม | **ต่อยอดเครื่องยนต์ควิซ** (`quiz_sessions` / `quiz_players` / `quiz_answers` / `host_token` / นาฬิกา server) ไม่สร้างตารางห้องชุดใหม่ | ของยากทั้งหมด — นาฬิกาตรงกัน, กันโกง, ตัวตนผู้เล่น 3 แบบ, จอเวที, polling บนรถ — ทำเสร็จและผ่านสนามจริงแล้ว เขียนใหม่ = รับบั๊กเดิมทั้งชุดกลับมาฟรีๆ |
| แยกจากควิซตรงไหน | `quiz_sets.game_kind = 'puzzle'` + `quiz_questions.kind = 'puzzle'` | คลังชุดแยกกันในสายตาผู้ใช้ (การ์ดคนละใบ หน้าคนละหน้า) แต่ใช้ตารางและ RPC ชุดเดียวกัน |
| ตอบยังไง | **พิมพ์คำตอบ** ไม่ใช่ตัวเลือก | เกมนี้ทั้งเกมอยู่ที่การเดาคำ ถ้าให้เลือก 4 ข้อจะกลายเป็นควิซที่มีรูปสวย |
| **คะแนน** | **ตอบถูก = 1 คะแนน** เท่ากันทุกคน ไม่ถ่วงเวลา · คะแนนรวมมากสุดชนะ · เท่ากันตัดสินด้วยเวลารวม | ตามกติกาในเอกสารตัวอย่าง — และเป็นเกมนั่งเล่นด้วยกัน ไม่ใช่เกมแข่งกดปุ่ม ถ้าถ่วงเวลาแรงๆ คนพิมพ์ไทยช้าจะรู้ตัวว่าแพ้ตั้งแต่ข้อสองแล้วเลิกเล่น |
| **ตอบได้กี่ครั้ง** | **ไม่จำกัด** ผิดไม่หักคะแนน · ถูกครั้งแรกจบข้อของคนนั้น | เดาสุ่มไม่มีทางเจอเพราะพื้นที่คำตอบไม่จำกัด · การจำกัดครั้งลงโทษคนพิมพ์ไทยผิดบนรถที่โยก มากกว่าลงโทษคนโกง · กันสแปมด้วย rate limit ที่ผู้เล่นปกติไม่มีวันชน (ข้อ 4.2) |
| **คำใบ้** | **คนคุมเกมเปิดอย่างเดียว** — ตั้งได้สูงสุด 3 ขั้นต่อข้อ เปิดทีละขั้น เห็นพร้อมกันทั้งห้อง · **ผู้เล่นไม่มีปุ่มขอคำใบ้** | คนถือไมค์อ่านห้องออกว่าตันจริงหรือแค่ยังไม่ถึงเวลา — ดีกว่าให้แต่ละคนตัดสินใจเอง · และตัดทั้ง ตาราง/โควตา/RPC/ปุ่ม ออกได้หมด เกมง่ายขึ้นทั้งสำหรับคนเล่นและคนเขียน |
| ใช้คำใบ้แล้วหักคะแนนไหม | **ไม่หัก** | คำใบ้เปิดพร้อมกันทั้งห้อง ทุกคนได้เท่ากัน จึงไม่มีอะไรให้หัก · และ 1 คะแนนหักไม่ได้อยู่แล้ว |
| รูปใบ้ | **1-6 ชิ้นต่อข้อ** · เป็นรูปอัปโหลด / อีโมจิ / คำสั้นๆ ก็ได้ · **ระบบจัดเรียงให้เองตามจำนวนและสัดส่วนรูป** | ตัวอย่างจริงใช้ 3 รูป แต่ข้อที่ใบ้ด้วยรูปเดียว (เช่นรูปเดียวสื่อทั้งคำ) ก็ควรทำได้ · คนสร้างข้อไม่ควรต้องมานั่งเลือกเลย์เอาต์ (ข้อ 4.7) |
| **ภาพเฉลย** | มี 1 ภาพต่อข้อ (ไม่บังคับ) | ตัวอย่างจริงเฉลย "ภูกระดึง" ด้วยรูปผาหล่มสักเต็มจอ — เป็นทั้งจังหวะพีคของเกมและของแถมความรู้สำหรับทริป |
| มือถือเห็นโจทย์ไหม | **เห็น** — ต่างจากควิซที่ตั้งใจให้เงยหน้าดูจอ | รูปใบ้เป็นรายละเอียดเล็ก (รูป "หน้าคนมีกระ" บนทีวี 19" แถวหลังดูไม่ออกแน่นอน) และคนต้องก้มพิมพ์อยู่แล้ว |
| เวลาต่อข้อ | ค่าเริ่มต้น **90 วินาที** (ควิซใช้ 20) แต่ทีมงานกด "ปิดรับ + เฉลย" ได้ทุกเมื่อ | คิดคำ + พิมพ์ไทยบนรถที่โยก ใช้เวลาคนละเรื่องกับกดปุ่มสี |
| หน้าตาจอเวที | สกินใหม่ **`arcade`** เป็นค่าเริ่มต้นของเกมนี้ (ยังสลับไป `day` / `neon` ได้) | เอกสารตัวอย่างมีภาษาทางสายตาชัดมาก — ตาหมากรุกเขียว-กรมท่า ป้ายพิลสีเหลือง เส้นขอบดำหนา ของประดับพิกเซล — และมันคือส่วนหนึ่งของความสนุก ไม่ใช่การตกแต่ง |
| โหมดทีม | ใช้ของเฟส 3 ของควิซได้ทันที ไม่ต้องทำอะไรเพิ่ม | `quiz_players.team_id` มีอยู่แล้วและทำงานอยู่ |
| ภาษา | ชุดหนึ่ง = ภาษาเดียว (`quiz_sets.lang`) | กติกาเดียวกับควิซ ข้อ 8 — และเกมนี้เล่นคำ แปลไม่ได้อยู่แล้ว |

---

## 1. ทำไมถึงไม่สร้างเป็นเกมใหม่ทั้งใบ

เกมนี้ "หน้าตา" ต่างจากควิซมาก แต่ **โครงข้างในเหมือนกันเกือบทั้งหมด**

| สิ่งที่เกมนี้ต้องมี | ควิซทำไว้แล้วหรือยัง |
|---|---|
| ห้อง (สร้าง → แขกเข้า → เริ่ม → จบ) | ✅ `quiz_sessions` + `quiz_join` |
| ผู้เล่น 3 แบบ (ลูกทัวร์ / ทีมงาน / ไกด์ท้องถิ่น) | ✅ `quiz_players` |
| นาฬิกาตรงกันทุกเครื่อง (`question_started_at` เป็นเวลาสัมบูรณ์) | ✅ + `quiz_now()` |
| กันแขกสั่งงานเกมเอง | ✅ `quiz_session_tokens` |
| ปิดเฉลยไม่ให้เห็นใน DevTools | ✅ `quiz_keys` (REVOKE หมด) |
| จอเวทีแท็บแยก + ระบบสกิน | ✅ `QuizStage.jsx` (เพิ่มสกินที่สาม) |
| realtime + polling ชั้นสองบนรถ | ✅ `useQuizSession.js` |
| กระดานอันดับ + โหมดทีม | ✅ |
| อัปโหลดรูป + ย่อรูปก่อนอัปโหลด | ✅ `quizMedia.js` |
| รายงานหลังเกม | ✅ `quiz_report` |
| **ตรวจคำตอบที่พิมพ์เป็นภาษาไทย** | ❌ ของใหม่ |
| **ตอบได้ไม่จำกัดครั้ง** | ❌ ของใหม่ |
| **รูปใบ้หลายรูปต่อข้อ + คำใบ้ที่ผู้เล่นขอเองได้** | ❌ ของใหม่ |

ของใหม่มี 3 อย่าง ของเดิมมี 10 อย่าง — เขียนเกมใหม่ทั้งใบคือการก๊อป 1,085 บรรทัด SQL
กับอีกราว 2,600 บรรทัด JSX มาแก้ชื่อตัวแปร แล้วรอเจอบั๊กเดิมซ้ำ (เช่น `gen_random_bytes`
ที่ไม่มีตัวตนบน Supabase — ข้อ 15 ของเอกสารควิซ)

**ราคาที่ต้องจ่ายของการใช้ร่วม** อยู่ที่ข้อ 10 — มี 6 ไฟล์ที่ query ของเดิมต้องเติมตัวกรอง
`game_kind` ไม่งั้นชุดปริศนาจะไปโผล่ในคลังควิซ ถ้าลืมไฟล์ใดไฟล์หนึ่งจะเห็นผลทันที
(ห้องปริศนาโผล่ในการ์ดควิซ) ซึ่งดีกว่าพังเงียบ

---

## 2. หน้าตาของ "หนึ่งข้อ" — อ่านจากตัวอย่างจริง

### ตัวอย่างที่ 1 — ภูกระดึง

```
ป้ายหัวเรื่อง:  3 พยางค์
รูปใบ้:  [หมีพูห์ส่องกระจก]  [ใบหน้ามีกระ]  [เด็กชักเย่อ]
            พูห์      →  ภู       กระ  →  กระ      ดึง  →  ดึง
คำใบ้:   "อุทยานแห่งชาติ จังหวัดเลย"
เฉลย:    ภูกระดึง  + รูปพระอาทิตย์ตกที่ผาหล่มสักเต็มจอ
```

### ตัวอย่างที่ 2 — กาละแม

```
ป้ายหัวเรื่อง:  3 พยางค์
รูปใบ้:  [อีกา]      [กะละมัง]        [แพะกำลังร้อง]
           กา   →  กา   กะ-ละ-มัง → ละ    เสียงแพะ → แม
คำใบ้:   "ขนม หนียวหนึบ"
เฉลย:    กาละแม  + รูปกาละแมห่อ
```

**สามอย่างที่ตัวอย่างจริงบอก และแบบร่างเดิมเดาผิด:**

| เรื่อง | ที่เดาไว้ | ของจริง |
|---|---|---|
| ช่องตัวอักษร | โชว์ `▢▢ ▢▢ ▢▢▢▢` ตามความยาวแต่ละพยางค์ | **ไม่มี** — มีแค่ป้าย "3 พยางค์" เท่านั้น ตัดทิ้ง (ดูกล่องข้างล่าง) |
| รูปใบ้ ↔ พยางค์ | รูปหนึ่งให้เสียงหนึ่งชุดตรงๆ | **ไม่ตรงเสมอ** — "กะละมัง" ให้แค่พยางค์กลาง "ละ" · ความสัมพันธ์เป็นเรื่องที่คนสร้างข้อกำหนดเอง ระบบไม่ต้องรู้ |
| คำใบ้ | บันได 3 ขั้น | **ขั้นเดียวก็พอ** และเป็น "ของประเภทไหน" — ตัวอย่างทั้งสองข้อใช้แบบเดียวกัน · รองรับได้ถึง 3 ขั้น แต่ Builder โชว์ช่องเดียวเป็นค่าเริ่มต้น กด "+ เพิ่มคำใบ้" ถึงจะได้ขั้นที่ 2-3 |

> **ทำไมตัดช่อง `▢▢` ทิ้ง:** มันบอกจำนวนตัวอักษรของแต่ละพยางค์ ซึ่งแคบกว่าที่เกมนี้ตั้งใจมาก
> — "ภู-กระ-ดึง" กับ "ผา-กระ-ดิ่ง" ยาวเท่ากัน แต่ช่องจะทำให้คนไล่เดาแบบครอสเวิร์ดแทนที่จะ
> อ่านรูป และเอกสารตัวอย่างก็ไม่มี ป้าย "3 พยางค์" ป้ายเดียวคือโจทย์ที่ถูกต้องแล้ว
> (`answer_split` ยังเก็บไว้ แต่ใช้เพื่ออนิเมชันตอนเฉลยอย่างเดียว)

### โครงสร้างข้อมูลหนึ่งข้อ

```
ส่วนที่ทุกคนอ่านได้ (quiz_questions + quiz_puzzle + quiz_puzzle_clues)
   syllable_count  3                             ← ป้ายหัวเรื่อง
   clues           [ {image}, {image}, {image} ] ← 1-6 ชิ้น ไม่มีคำกำกับ ไม่มีเลย์เอาต์
   time_limit_sec  90
   hint_count      1                             ← มีคำใบ้กี่ขั้น (0-3) แต่ยังไม่เห็นเนื้อ

ส่วนที่ไม่มีใครอ่านได้เลยจนกว่าจะถึงจังหวะ (quiz_keys)
   correct_text     "ภูกระดึง"
   answer_split     "ภู-กระ-ดึง"                 ← ใช้ตอนเฉลยเท่านั้น
   answer_aliases   ["ภูกะดึง"]
   clue_labels      ["หมีพูห์", "กระ", "ดึง"]     ← โชว์ใต้รูปตอนเฉลย
   answer_image_url  ".../xxxx.jpg"              ← รูปผาหล่มสัก
   hints            [{step:1, body:"อุทยานแห่งชาติ จังหวัดเลย"}]
   explain          "พูห์ + กระ + ดึง = ภูกระดึง"
```

> **ทำไม `clue_labels` ต้องอยู่ฝั่งเฉลย:** ถ้าเก็บคำว่า "หมีพูห์" ไว้ในตารางที่ `anon` อ่านได้
> (เช่นเป็น `alt` ของรูป) คนเปิด DevTools จะได้เฉลยไปหนึ่งในสามทันที · ด้วยเหตุผลเดียวกัน
> **ชื่อไฟล์รูปต้องสุ่มใหม่ทั้งหมด** — ไฟล์ชื่อ `กะละมัง.jpg` ในถังที่เปิด public คือการแจกเฉลย
> (กติกาเดียวกับข้อ 14.1 ของเอกสารควิซ) และ `alt` ต้องเป็น "รูปใบ้ที่ 1" เท่านั้น
> **รวมถึง `answer_image_url` ด้วย** — ถ้าส่งไปพร้อมข้อ คนเปิด Network tab จะเห็นรูปเฉลย
> ก่อนใครทั้งห้อง ต้องส่งตอน `reveal` เท่านั้น (ข้อ 4.3)

---

## 3. รูปแบบการเล่น (state ใช้ของเดิมทั้งหมด)

```
ทีมงาน                                   จอใหญ่                    ลูกทัวร์
──────                                   ──────                    ────────
สร้างห้องจากชุดปริศนา
  state = lobby                          หน้ากติกา + "รอเริ่ม"      กดเข้าร่วม → โหลดรูปใบ้ทั้งชุดไว้ล่วงหน้า
กด "เริ่ม"
  state = answering                      นับ 3-2-1 แล้วขึ้นรูป      ช่องพิมพ์เปิดพร้อมกันทุกเครื่อง
  (question_started_at = now()+3s)
                                                                   พิมพ์ → ส่ง → ผิด → พิมพ์ใหม่ (ไม่จำกัด)
เห็น "ตอบถูกแล้ว 12/38" → อ่านห้องออกว่าตัน
กด "เปิดคำใบ้ 1/3"
  hint_level = 1                         คำใบ้ขึ้นแถบล่าง           คำใบ้ขึ้นบนมือถือทุกเครื่องพร้อมกัน
กด "ปิดรับ + เฉลย"
  state = locked → reveal                รูปได้คำกำกับ + ภาพเฉลย    เห็นว่าตัวเองถูก/ผิด
กด "อันดับ"  → scoreboard                Top 10 / Top 3
กด "ข้อถัดไป"                            ...
กด "จบเกม"  → finished                   สรุป + ผู้ชนะ              สรุปส่วนตัว "ตอบถูก 7/12"
```

**ไม่มี state ใหม่แม้แต่ตัวเดียว** — การเปิดคำใบ้เกิดระหว่าง `answering` เป็นแค่การขยับ
`hint_level` แล้วยิง realtime ออกไปตามปกติ (แถว `quiz_sessions` ถูก subscribe อยู่แล้ว)

---

## 4. ปัญหาที่ต้องแก้ให้ได้ (แก่นของเอกสารนี้)

### 4.1 ตรวจคำตอบภาษาไทย — ปัญหาที่ยากที่สุดของเกมนี้

**ตัวอย่างจริงพิสูจน์ปัญหานี้ให้เห็นเองแล้ว:** เอกสารเขียนเฉลยว่า **"กาละแม"**
แต่พจนานุกรมและป้ายตามร้านเขียน **"กะละแม"** — ทั้งสองแบบมีคนใช้จริง
ถ้าเก็บไว้แบบเดียวแล้วตรวจตรงตัว ครึ่งห้องที่ตอบถูกจะโดนปฏิเสธตั้งแต่ข้อแรก

รูปแบบที่คนตอบถูกแต่พิมพ์ไม่ตรง มีอย่างน้อย 6 แบบ:

| ที่พิมพ์มา | เฉลย | สาเหตุ |
|---|---|---|
| `กะละแม` | `กาละแม` | สะกดได้สองแบบทั้งคู่ |
| `ภู กระดึง` | `ภูกระดึง` | เว้นวรรค |
| `อุทยานภูกระดึง` | `ภูกระดึง` | เติมคำนำหน้า |
| `ภูกะดึง` | `ภูกระดึง` | ตกตัวอักษร |
| `ภูกระดึง ` / `ภูกระดึง.` | `ภูกระดึง` | ช่องว่าง/จุดท้ายจากคีย์บอร์ดมือถือ |
| `ภูกรัะดึง` | `ภูกระดึง` | ลำดับสระ-วรรณยุกต์สลับ (พิมพ์บนมือถือเกิดบ่อย) |

**ทางแก้ 3 ชั้น — ทุกชั้นทำงานฝั่ง server เท่านั้น**

```
ชั้นที่ 1  normalize แบบเข้ม  (quiz_norm_answer)
          ตัดช่องว่างทั้งหมด · ตัดอักขระที่มองไม่เห็น (ZWSP U+200B) ·
          ตัดเครื่องหมายวรรคตอน · ตัด ๆ ฯ · ตัดคำนำหน้า
          (จังหวัด, จ., อำเภอ, อ., อุทยาน, ขนม, คำว่า, คือ) · lower() ตัวโรมัน
          → เทียบกับ correct_text และ answer_aliases ที่ normalize แบบเดียวกัน

ชั้นที่ 2  normalize แบบผ่อนผัน  (quiz_loose_answer)
          ชั้นที่ 1 + ตัดวรรณยุกต์ (่ ้ ๊ ๋ ็) + **ตัดทัณฑฆาต ์ พร้อมพยัญชนะตัวหน้ามัน**
          → ผ่านชั้นนี้ = ถูก ได้คะแนนเหมือนกัน

ชั้นที่ 3  เกือบถูก  (edit distance ≤ 2 บนสตริงชั้นที่ 2)
          → ยังไม่นับว่าถูก แต่ไปโผล่บนมือถือทีมงานเป็นรายการ "คำตอบที่เกือบถูก"
            ทีมงานกด "รับ" ทีเดียว → คนที่ตอบแบบนั้นทั้งหมดกลายเป็นถูกย้อนหลัง
            + เก็บเข้า answer_aliases ถาวร ชุดนี้ครั้งหน้าไม่ต้องกดอีก
```

> **ทัณฑฆาตกินพยัญชนะตัวหน้ามันด้วย ไม่ใช่ตัวมันตัวเดียว** — `ย์` = `ย` + `์` และ `์`
> ทำหน้าที่บอกว่า `ย` ไม่ออกเสียง ต้องลบทั้งคู่ถึงจะตรงกับเสียงที่คนพิมพ์

**ชั้นที่ 3 คือหัวใจของการใช้งานจริง** — ไม่มีวันเขียนกฎครอบคลุมภาษาไทยได้หมด
แต่ทีมงานที่ถือไมค์อยู่ตัดสินได้ในครึ่งวินาทีว่า "กะละแม" คือคนที่รู้คำตอบ
ให้เครื่องเสนอ ให้คนตัดสิน แล้วให้ระบบจำ — คลังชุดจะฉลาดขึ้นเองทุกครั้งที่เล่น

**จุดที่พลาดง่ายตอนเขียน:**

| เรื่อง | ต้องทำ |
|---|---|
| กด "รับ" ได้ถึงเมื่อไหร่ | ถึงก่อน `state = reveal` เท่านั้น — หลังเฉลยแล้วคะแนนขึ้นจอไปแล้ว แก้ย้อนหลังคือทำลายความน่าเชื่อถือของกระดาน |
| `edit distance` เรียกจากไหน | **เขียน plpgsql เอง อย่าเรียก `levenshtein` ของ fuzzystrmatch** — บน Supabase ส่วนขยายอยู่ schema `extensions` และฟังก์ชันเราตั้ง `SET search_path = public` จะมองไม่เห็น (บทเรียนเดียวกับ `gen_random_bytes` ข้อ 15 ของเอกสารควิซ) · เรียกเฉพาะตอนทีมงานขอดูรายการ ≤ 40 แถว ประสิทธิภาพไม่ใช่ประเด็น |
| คำตอบว่าง / ยาวเกิน | ตัดที่ RPC — ว่าง = ไม่นับ, ยาวเกิน 80 ตัวอักษร = ปฏิเสธ |
| Builder ต้องช่วยตั้งแต่ต้นทาง | ช่อง "คำที่ยอมรับเพิ่ม" ต้องอยู่ติดกับช่องคำตอบ **ไม่ใช่ซ่อนใน 'ตั้งค่าขั้นสูง'** — คนสร้างข้อ "กาละแม" ต้องนึกถึง "กะละแม" ตอนนั้นเลย ไม่ใช่ตอนยืนอยู่หน้าไมค์ |

### 4.2 ตอบได้ไม่จำกัดครั้ง — และห้ามทำให้กระดานอันดับเดิมพัง

เอกสารตัวอย่างเขียนว่า "ตอบได้ 2 ครั้ง" แต่**ตัดสินใจให้ไม่จำกัด** เหตุผล:

- เกมกระดาษจำกัดครั้งเพราะคนคุมเกมต้องฟังทีละคน — ในแอปทุกคนพิมพ์พร้อมกันได้ ข้อจำกัดนั้นหมดหน้าที่
- เดาสุ่มไม่มีทางเจอ พื้นที่คำตอบคือทุกคำในภาษาไทย
- คนพิมพ์ไทยผิดบนรถที่โยกคือคนส่วนใหญ่ ไม่ใช่คนโกง
- ถ้าอยากจำกัดจริง ตั้งที่ระดับชุดได้ (`attempt_limit` — ค่าเริ่มต้น NULL = ไม่จำกัด)

`quiz_answers` มี `UNIQUE (session_id, question_id, player_id)` — "ตอบได้ครั้งเดียว"
เป็นกติกาที่ตั้งใจ และ `quiz_report` / กระดานอันดับ / โหมดทีม อ่านตารางนี้อยู่

**ห้ามถอด UNIQUE ตัวนั้น** วิธีที่เลือกคือ:

```
quiz_puzzle_guesses          ← ทุกครั้งที่กดส่ง INSERT ที่นี่ (anon อ่านไม่ได้)
   raw_text, norm_text, is_correct, elapsed_ms, created_at

quiz_answers                 ← เขียน "แถวสรุป" แถวเดียวเท่านั้น เมื่อ:
   ตอบถูก           → is_correct = true,  points = 1
   หมดเวลา/ปิดรับ    → is_correct = false, points = 0   (RPC quiz_lock เขียนให้)
```

ผลคือทุกอย่างที่อ่าน `quiz_answers` ทำงานต่อได้โดยไม่รู้เลยว่าเกมนี้ตอบได้ไม่จำกัด
และเราได้ log การเดาไว้ครบสำหรับหน้ารายงาน ("ข้อนี้เดาเฉลี่ย 6.2 ครั้ง" = ข้อยากเกินไป)

**กันสแปมโดยผู้เล่นปกติไม่มีวันรู้สึก:**

| กติกา | ค่า | ทำไม |
|---|---|---|
| เว้นระยะระหว่างครั้ง | ≥ 400 ms | คนพิมพ์เร็วที่สุดยังห่างกว่านี้ |
| ส่งข้อความเดิมซ้ำ | ไม่นับ ไม่บันทึก | กดปุ่มรัวเพราะคิดว่าเน็ตค้าง |
| เพดานนิรภัยต่อข้อ | 40 ครั้ง/คน | ไม่ใช่กติกาของเกม เป็นกันสคริปต์ — ถ้ามีใครชนเพดานนี้จริง แปลว่าไม่ได้กำลังเล่น |

### 4.3 คำใบ้ — คนคุมเกมเปิดอย่างเดียว และห้ามรั่วก่อนเปิด

```
quiz_puzzle_hint(session, token, step)          ← มีทางเดียว ไม่มีปุ่มฝั่งผู้เล่น
   ✱ ตรวจ token + state = 'answering'
   ✱ ตรวจ step = hint_level + 1        ← กันกดรัวแล้วข้ามขั้น (optimistic lock
                                          แบบเดียวกับ quiz_next ที่ตรวจ current_index)
   ✱ ตรวจ step ≤ hint_count ของข้อนั้น (สูงสุด 3)
   ✱ hint_level = step
   ✱ คัดลอก "เฉพาะเนื้อคำใบ้ขั้นนั้น" ลง quiz_sessions.hint_payload (jsonb ที่อ่านได้)
     — ห้ามคัดทั้งก้อน hints ลงไป ไม่งั้นขั้น 2-3 รั่วตั้งแต่เปิดขั้น 1
   ✱ realtime พาไปทุกจอพร้อมกัน
```

**ทำไมตัดปุ่ม "ขอคำใบ้" ของผู้เล่นทิ้ง** — ร่างก่อนหน้าให้ผู้เล่นขอเองได้ตามโควตา
ซึ่งฟังดูใจดีแต่แลกมาด้วยของสามอย่าง: ตารางนับโควตา, RPC อีกตัว, และปุ่มที่ต้องมีหน้าจอ
ยืนยันเพราะกดพลาดแล้วคืนไม่ได้ · ที่สำคัญกว่านั้นคือมันย้ายการตัดสินใจไปอยู่ผิดที่ —
**คนถือไมค์เป็นคนเดียวที่เห็นทั้งห้อง** รู้ว่าเงียบเพราะตันหรือเงียบเพราะกำลังคิดอยู่
ปล่อยให้ต่างคนต่างเปิดจะได้ห้องที่บางคนเห็นคำใบ้บางคนไม่เห็น ซึ่งอธิบายบนจอใหญ่ไม่ได้เลย

**ผลพลอยได้:** ตัด `quiz_puzzle_hint_uses`, `quiz_sets.hint_quota`,
`quiz_puzzle_take_hint()` และปุ่มบนมือถือลูกทัวร์ออกได้ทั้งหมด — มือถือลูกทัวร์เหลือ
สิ่งเดียวที่ต้องทำคือพิมพ์คำตอบ ซึ่งเป็นสิ่งที่ถูกต้องสำหรับจอเล็กบนรถที่โยก

**`hint_payload` ต้องถูกล้างเป็น `[]` ทุกครั้งที่ `quiz_next` เปิดข้อใหม่** — ถ้าลืมข้อนี้
คำใบ้ของข้อที่แล้วจะค้างอยู่บนจอข้อถัดไป ซึ่งอาจเป็นเฉลยของข้อเก่าที่ยังไม่เฉลย

**ภาพเฉลยก็เป็นความลับ** — `answer_image_url` อยู่ใน `quiz_keys` และไปกับ `reveal_payload`
ตอน `quiz_reveal` เท่านั้น ผลข้างเคียงคือ**จอใหญ่จะโหลดรูปนั้นตอนเฉลยพอดี** ซึ่งบนรถอาจช้า
1-2 วินาที รับได้ (จังหวะนั้นทีมงานกำลังพูดอยู่) แต่ **ห้ามแก้ด้วยการส่งล่วงหน้า**

### 4.4 รูปใบ้ 6 รูป × มือถือ 40 เครื่อง — เลขที่ต้องดูก่อนเขียนโค้ด

ควิซมีรูป 1 รูปต่อข้อ และ**ตั้งใจไม่ส่งให้มือถือ**เมื่อมีจอใหญ่ (ให้เงยหน้าดูจอ)
เกมนี้กลับกันทั้งสองข้อ — ได้ถึง 6 รูปต่อข้อ และมือถือทุกเครื่องต้องเห็น

| เกม 12 ข้อ · 40 เครื่อง | รูปไม่ย่อ (4 MB) | ย่อแบบควิซ (1920px ≈ 250 KB) | **ย่อแบบเกมนี้ (900px ≈ 80 KB)** |
|---|---|---|---|
| Egress · เฉลี่ย 3 รูป/ข้อ | 5.8 GB ← **หมดโควตาเดือนในเกมเดียว** | 360 MB | **115 MB** |
| Egress · เต็มที่ 6 รูป/ข้อ | 11.5 GB | 720 MB | **230 MB** |
| เล่นได้กี่เกม/เดือน (free 5 GB) | 0.4 | 7-13 | **20-40** |
| Storage ต่อชุด 12 ข้อ (3 รูป) | 144 MB | 9 MB | **2.9 MB** |

**รูปใบ้ต่างจากรูปประกอบคำถามตรงที่มันเป็นรูปของ "ของหนึ่งชิ้น"** — อีกา กะละมัง แพะ
ไม่ใช่ภาพวิวที่ต้องการรายละเอียด จึงย่อเหลือด้านยาว **900px คุณภาพ 0.8** ได้โดยไม่เสียอะไร
(ใช้ `quizMedia.js` เดิม เพิ่ม preset ไม่ต้องเขียนใหม่)

**ยกเว้นภาพเฉลย** — รูปผาหล่มสักเต็มจอโปรเจกเตอร์คือจังหวะพีคของข้อนั้น
ย่อที่ **1600px (~200 KB)** และส่งให้เฉพาะตอน `reveal` (มือถือโหลดแค่ตอนนั้น ไม่ใช่ทั้งชุด)

**ห้ามครอบรูปเป็นสี่เหลี่ยมจัตุรัส** — ตัวอย่างจริงมีทั้งรูปแนวนอน (อีกา ชักเย่อ) และจัตุรัส
ครอปกลางภาพจะตัดเชือกออกจากรูปชักเย่อ แล้วข้อนั้นก็ตอบไม่ได้อีกเลย
ใช้ `object-contain` ในแถวที่ความสูงเท่ากัน

**และทางที่ดีกว่าคือไม่อัปโหลดเลย:**

| ชนิดใบ้ | ตัวอย่าง | ขนาด | เวลาที่ทีมงานใช้สร้าง |
|---|---|---|---|
| `emoji` | 🐦 🥣 🐐 | 0 ไบต์ | 5 วินาที |
| `text` | คำสั้นๆ ตัวใหญ่เต็มช่อง | 0 ไบต์ | 5 วินาที |
| `image` | รูปที่หาหรือวาดเอง | ~80 KB | 2-10 นาที |

**นี่คือความเสี่ยงที่แท้จริงของเกมนี้ ไม่ใช่เรื่องเทคนิค** — เอกสารตัวอย่าง 2 ข้อใช้เวลาทำ
นานกว่าที่คิด ถ้าชุดหนึ่งต้องมี 12 ข้อ × 3 รูป = ไปหารูป 36 รูป จะไม่มีใครสร้างชุดที่สอง
ปุ่มอีโมจิคือทางออกที่ถูกที่สุด

> **เรื่องลิขสิทธิ์รูป (สั้นๆ แต่ควรรู้):** ตัวอย่างใช้ภาพจากอินเทอร์เน็ตรวมถึงภาพนิ่งจาก
> การ์ตูนดิสนีย์ ใช้ในงานเลี้ยงปิดของบริษัทความเสี่ยงต่ำ แต่**อย่าเอาสกรีนช็อตของเกมนี้
> ไปลงเพจหรือใช้ในสื่อโฆษณา** และถ้าจะทำคลังชุดกลางที่ใช้ข้ามบริษัท ควรเปลี่ยนไปใช้
> อีโมจิหรือภาพที่วาดเอง

**preload ตอน lobby** — ระหว่างรอทีมงานกดเริ่ม เน็ตว่างอยู่ ให้มือถือโหลดรูปใบ้
**ทั้งชุด** ไว้เลย (~3-6 MB) จบเรื่องภาพขึ้นช้ากลางเกมทั้งเกม

### 4.5 พิมพ์ไทยบนรถที่โยก + คีย์บอร์ดบังจอ

| เรื่อง | ต้องทำ |
|---|---|
| คีย์บอร์ดเด้งขึ้นมาบังรูปใบ้ | ใช้ `visualViewport` — พอคีย์บอร์ดเปิด ย่อรูปใบ้เป็นแถวเล็กบนสุดและตรึงช่องพิมพ์ไว้เหนือคีย์บอร์ด **ห้ามให้ผู้เล่นต้องเลื่อนจอเพื่อดูรูปที่ตัวเองกำลังทาย** |
| ระบบเดาคำของคีย์บอร์ด | `autocomplete="off" autocorrect="off" autocapitalize="off" spellcheck="false"` — แถบเดาคำภาษาไทยชอบแก้คำที่พิมพ์ถูกแล้วให้ผิด |
| อ่านค่าจากช่องยังไง | ผูกกับ `onInput` ไม่ใช่ `onKeyDown` — แถบเดาคำเติมข้อความโดยไม่ยิง keydown |
| กด Enter | = กดส่ง และ**ไม่ปิดคีย์บอร์ด** (ตอบได้ไม่จำกัดครั้ง คนจะพิมพ์ต่อทันที) |
| ตอบผิด | ช่องสั่น 1 ครั้ง + "ยังไม่ใช่" + **ล้างช่องให้เลย** — ให้แก้คำเดิมทีละตัวคือการทรมานคนบนรถที่โยก |
| ตอบถูกแล้ว | ปิดช่องพิมพ์ เปลี่ยนเป็นการ์ด "ถูกต้อง! +1 คะแนน" — อย่าเปิดช่องทิ้งไว้ให้กดส่งซ้ำ |
| ปุ่มส่ง | สูงอย่างน้อย 56px และอยู่ติดช่องพิมพ์ ไม่ใช่ท้ายหน้า |

### 4.6 คนตะโกนเฉลย

เกมทายคำมีปัญหาที่ควิซไม่มี: คนแรกที่คิดออกมักตะโกนออกมาเลย แล้วข้อนั้นจบทันที

**และกติกา "1 คะแนนเท่ากันทุกคน" ทำให้ปัญหานี้หนักกว่าเดิม** — ถ้าให้คะแนนตามความเร็ว
คนที่รู้ก่อนจะรีบพิมพ์เพื่อกินแต้ม แต่พอทุกคนได้ 1 เท่ากัน การตะโกนไม่มีต้นทุนอะไรเลย

ระบบบังคับอะไรไม่ได้ สิ่งที่ทำได้:

- **โหมดทีม** (ของเฟส 3 ควิซ ใช้ได้ฟรี) เปลี่ยนการตะโกนจากปัญหาเป็นกลไก — ตะโกนในทีมตัวเองคือวิธีเล่นที่ถูกต้อง **แนะนำให้เปิดเป็นค่าเริ่มต้นของเกมนี้**
- ตัวเลือกที่ระดับชุด: "ให้คะแนนคนตอบถูก 3 คนแรกเพิ่มคนละ 1" — คืนแรงจูงใจให้พิมพ์แทนพูด โดยไม่ทิ้งกติกา 1 คะแนน (ยังไม่ตัดสิน — ข้อ 13)
- ข้อความในหน้าทีมงานตรงปุ่มเริ่ม: *"บอกลูกทัวร์ก่อนเริ่มว่าห้ามตะโกน — คิดออกให้พิมพ์"*
  บรรทัดเดียว แต่ตัดสินว่าเกมสนุกหรือพัง มากกว่าโค้ดทั้งไฟล์


### 4.7 จัดเรียงรูปใบ้ให้เอง — คนสร้างข้อไม่ต้องเลือกเลย์เอาต์

คนสร้างข้ออัปโหลดรูปมา 1-6 ชิ้น เรียงลำดับได้ **แต่ไม่ต้องเลือกว่าจะวางแบบไหน**
เพราะสิ่งที่ตัดสินความสวยไม่ได้อยู่ที่ความชอบ แต่อยู่ที่ **จำนวนรูป × สัดส่วนของรูป × ขนาดจอ**
ซึ่งเป็นเลขที่ระบบรู้ดีกว่าคน

```
เลือกกริดจากจำนวนรูป (ค่าเริ่มต้น — จอเวที 16:9)
   1 รูป → 1 ช่องใหญ่กลางจอ       สูงได้ถึง 62% ของความสูงจอ
   2 รูป → 2 ช่องแนวนอน
   3 รูป → 3 ช่องแนวนอน           (แบบเดียวกับเอกสารตัวอย่าง)
   4 รูป → 2 × 2
   5 รูป → 3 บน / 2 ล่าง (ล่างจัดกึ่งกลาง)   ← ห้ามได้ 3+2 ที่ล่างชิดซ้าย
   6 รูป → 3 × 2

ปรับตามสัดส่วนรูปจริง
   ถ้ารูป "ส่วนใหญ่" เป็นแนวตั้ง (h/w > 1.2) → ลดจำนวนคอลัมน์ลง 1 (ขั้นต่ำ 2)
     เพราะรูปแนวตั้ง 3 ใบเรียงกันจะสูงจนล้นจอ
   ถ้ารูป "ส่วนใหญ่" เป็นแนวนอนจัด (w/h > 2)  → บังคับเรียงเป็นแถวเดียวถ้ามี ≤ 3 รูป

กติกาที่ห้ามแหก
   • ทุกช่องสูงเท่ากันเสมอ — ความสูงเท่ากันคือสิ่งที่ทำให้แถวดูเป็นระเบียบ ไม่ใช่ความกว้าง
   • รูปใช้ object-contain ในช่อง ไม่ใช่ object-cover  ← ครอปแล้วอาจตัดคำใบ้ทิ้ง (ข้อ 4.4)
   • พื้นหลังช่องเป็นสีขาว/มุมมน + เส้นขอบดำ — รูปพื้นขาวจะได้ไม่ลอยหายไปกับพื้นจอ
   • อีโมจิ/คำ ใช้ช่องขนาดเดียวกับรูป แล้วขยาย font-size ให้เต็มช่องอัตโนมัติ
     (ไม่ใช่ตั้ง px ตายตัว — 1 รูปกับ 6 รูปขนาดช่องต่างกัน 3 เท่า)
```

มือถือใช้ตรรกะเดียวกันแต่เพดานคอลัมน์ต่างกัน (สูงสุด 2 คอลัมน์ในแนวตั้ง, 3 ในแนวนอน)
โค้ดอยู่ที่ `src/lib/puzzleLayout.js` **ฟังก์ชันบริสุทธิ์ ไม่แตะ DOM** จะได้เขียนเทสต์ได้
และ Builder เรียกตัวเดียวกันตอนพรีวิว — พรีวิวที่ไม่ตรงกับของจริงแย่กว่าไม่มีพรีวิว
---

## 5. Schema — ส่วนที่เพิ่ม

```
quiz_sets (game_kind='puzzle') ──┬── quiz_questions (kind='puzzle') ──┬── quiz_puzzle        [อ่านได้]
                                 │                                    ├── quiz_puzzle_clues  [อ่านได้ ไม่มีคำกำกับ]
                                 │                                    └── quiz_keys (+คอลัมน์ใหม่)  [อ่านไม่ได้]
                                 └── quiz_sessions (+hint_level, hint_payload)
                                        ├── quiz_players             (ของเดิม ไม่แตะ)
                                        ├── quiz_answers             (ของเดิม — แถวสรุปแถวเดียว)
                                        └── quiz_puzzle_guesses      [อ่านไม่ได้] ของใหม่
```

### 5.1 ตารางใหม่ `quiz_puzzle` (1:1 กับข้อ)

| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| `question_id` | uuid PK → quiz_questions ON DELETE CASCADE | |
| `syllable_count` | int NOT NULL CHECK BETWEEN 1 AND 12 | ป้ายหัวเรื่อง |
| `hint_count` | int NOT NULL DEFAULT 0 CHECK BETWEEN 0 AND 3 | บอกว่ามีคำใบ้กี่ขั้น (เนื้อไม่อยู่ที่นี่) |

**ไม่มีคอลัมน์ `layout`** — การจัดเรียงคำนวณตอนเรนเดอร์จากจำนวนรูป + สัดส่วนรูป + ขนาดจอ
(ข้อ 4.7) เก็บลง DB ไม่ได้เพราะจอเวทีกับมือถือได้คนละคำตอบจากข้อมูลชุดเดียวกัน

### 5.2 ตารางใหม่ `quiz_puzzle_clues`

| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| `id` | uuid PK | |
| `question_id` | uuid → quiz_questions ON DELETE CASCADE | |
| `sort_order` | int | |
| `clue_kind` | text CHECK `('image','emoji','text')` | |
| `body` | text | URL / อีโมจิ / คำ |

บังคับ **1-6 แถวต่อข้อที่ RPC** ไม่ใช่ที่ตาราง (constraint ข้ามแถวใน Postgres ต้องใช้ trigger ซึ่งไม่คุ้ม)

### 5.3 `quiz_keys` — เพิ่ม 6 คอลัมน์ (ตารางนี้ `anon` อ่านไม่ได้อยู่แล้ว)

| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| `correct_text` | text | คำตอบหลัก |
| `answer_split` | text | `ภู-กระ-ดึง` — ใช้ตอนเฉลยเท่านั้น |
| `answer_aliases` | text[] DEFAULT `'{}'` | **โตขึ้นเองเมื่อทีมงานกด "รับ"** |
| `clue_labels` | text[] DEFAULT `'{}'` | เรียงตาม `sort_order` ของรูป |
| `answer_image_url` | text | ภาพเฉลย ส่งตอน `reveal` เท่านั้น |
| `hints` | jsonb DEFAULT `'[]'` | สูงสุด 3 ขั้น `[{step:1, body:"อุทยานแห่งชาติ จังหวัดเลย"}]` |

### 5.4 `quiz_sessions` — เพิ่ม 3 คอลัมน์

| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| `hint_level` | int NOT NULL DEFAULT 0 | คำใบ้ที่ทีมงานเปิดให้ทั้งห้อง (0 = ยังไม่เปิด) |
| `hint_payload` | jsonb NOT NULL DEFAULT `'[]'` | เนื้อคำใบ้ที่เปิดแล้วของข้อปัจจุบัน — **ล้างทุกครั้งที่ขึ้นข้อใหม่** |
| `game_kind` | text NOT NULL DEFAULT `'quiz'` CHECK `('quiz','puzzle')` | denormalize จาก set เพื่อให้ query รายการห้องไม่ต้อง join |

`stage_theme` ที่มีอยู่แล้วต้องรับค่า `'arcade'` เพิ่ม (ข้อ 7.1)

### 5.5 `quiz_sets` — เพิ่ม 3 คอลัมน์

| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| `game_kind` | text NOT NULL DEFAULT `'quiz'` CHECK `('quiz','puzzle')` | **default `'quiz'` ทำให้ชุดเดิมทั้งหมดถูกต้องอัตโนมัติ ไม่ต้อง backfill** |
| `attempt_limit` | int NULL | NULL = ตอบไม่จำกัด (ค่าเริ่มต้น) |
| `points_per_correct` | int NOT NULL DEFAULT 1 | เผื่อชุดที่อยากให้ข้อละหลายแต้ม — ปกติไม่แตะ |

### 5.6 ตารางใหม่ `quiz_puzzle_guesses` — ไม่มี policy = ไม่มีใครอ่านผ่าน REST

| คอลัมน์ | ชนิด |
|---|---|
| `id`, `session_id`, `question_id`, `player_id` | |
| `raw_text` | text — เก็บของดิบไว้ดูตอนทำรายงาน |
| `norm_text` | text — ผลของ normalize ชั้นที่ 2 (index ไว้) |
| `is_correct` | boolean |
| `elapsed_ms` | int — **คิดฝั่ง server เท่านั้น** |
| `created_at` | timestamptz |

```sql
ALTER TABLE public.quiz_puzzle_guesses ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON public.quiz_puzzle_guesses FROM anon, authenticated;
CREATE INDEX quiz_puzzle_guesses_q_idx ON public.quiz_puzzle_guesses (session_id, question_id);
```

> Security Advisor จะเตือน `rls_enabled_no_policy` เพิ่มอีก 1 รายการ — **ตั้งใจ**
> เหมือน `quiz_keys` / `quiz_answers` (ข้อ 15 ของเอกสารควิซ)

### 5.8 CHECK ที่ต้องแก้ของเดิม (จุดเดียวที่แตะตารางที่ใช้งานอยู่)

```sql
ALTER TABLE public.quiz_questions DROP CONSTRAINT quiz_questions_kind_check;
ALTER TABLE public.quiz_questions ADD  CONSTRAINT quiz_questions_kind_check
  CHECK (kind IN ('mcq', 'tf', 'numeric', 'puzzle'));
```

เป็นการ**ขยาย** ไม่ใช่การบีบ — แถวเดิมทุกแถวยังผ่าน และ `time_limit_sec BETWEEN 5 AND 300`
ที่มีอยู่แล้วครอบ 90 วินาทีพอดี ไม่ต้องแก้ · `stage_theme` ก็ขยายเช่นกัน (`day`/`neon`/`arcade`)

---

## 6. คะแนน

```
ตอบถูก      → +1 คะแนน   (points_per_correct)
ตอบผิด      → 0 · ไม่หัก · ตอบใหม่ได้ทันที
หมดเวลา     → 0

ผู้ชนะ = คะแนนรวมมากที่สุด
คะแนนเท่ากัน → เวลารวมที่ใช้ตอบถูกน้อยกว่าอยู่บนกว่า (ไม่โชว์บนจอ ใช้ตัดสินเงียบๆ)
```

**ทำไมทิ้งสูตร 1000 แต้มถ่วงความเร็วของควิซ:** เอกสารตัวอย่างเขียนกติกาไว้ชัดว่า
"ตอบถูกได้ 1 คะแนน" และมันถูกสำหรับเกมนี้ — ควิซเป็นเกมกดปุ่มที่ทุกคนอ่านจบพร้อมกัน
ความเร็วจึงวัดความรู้ แต่เกมนี้ความเร็ววัด **ความเร็วในการพิมพ์ภาษาไทย** ซึ่งลูกทัวร์อายุ 60
แพ้ตั้งแต่ยังไม่เริ่มคิด · ให้คนคิดออกได้แต้มเท่ากันหมด แล้วเกมจะเป็นเรื่องของการอ่านรูป
ซึ่งคือสิ่งที่เกมนี้อยากวัดจริงๆ

**สิ่งที่เสียไปคือแรงจูงใจให้รีบพิมพ์** — เป็นต้นทางของปัญหาคนตะโกน (ข้อ 4.6)
ทางแก้ที่เสนอคือโหมดทีม ไม่ใช่การเอาคะแนนความเร็วกลับมา

**`streak_bonus` ของควิซต้องปิดเสมอสำหรับ `game_kind='puzzle'`** — โบนัส +100/+200
บนสเกล 1 คะแนน จะทำให้คนตอบถูกติดกัน 3 ข้อชนะขาดตั้งแต่ต้นเกม

---

## 7. หน้าจอ

### 7.1 จอใหญ่ `/staff/puzzle/stage/:sessionId` (`PuzzleStage.jsx`)

**สกินใหม่ `arcade`** — ถอดมาจากภาษาทางสายตาของเอกสารตัวอย่างโดยตรง:

| องค์ประกอบ | สเปก |
|---|---|
| พื้นหลัง | ตาหมากรุก 2 สี (เขียว/ขาว หรือ กรมท่า/ขาว) — CSS gradient ไม่ใช่รูป |
| กรอบ | เส้นขอบดำหนา 6-8px มุมมน 24px ซ้อนสองชั้น (ขาวนอก เขียวใน) |
| ป้ายหัวเรื่อง | พิลสีเหลือง ขอบดำ ลอยคร่อมขอบบน มี ✳ สองข้าง |
| ตัวอักษร | หนามาก ขอบดำรอบตัวอักษร (text-stroke) — ใช้ฟอนต์ที่ self-host อยู่แล้ว **ไม่เพิ่มฟอนต์ใหม่** |
| ของประดับ | ดาว/หัวใจ/ลูกเต๋าพิกเซล วางมุมจอ — **SVG inline ไม่ใช่รูปอัปโหลด** (ไม่กิน egress และคมทุกความละเอียด) |
| นาฬิกา | ไอคอนนาฬิกาจับเวลามุมขวาบน |

```
ระหว่างเปิดรับคำตอบ
┌───────────────────────────────────────────────────────┐
│              ╭──────────────╮                     ⏱   │
│              │  3 พยางค์     │                        │
│              ╰──────────────╯                         │
│                                                       │
│   ┌────────┐   ┌────────┐   ┌────────┐                │
│   │ [รูป]  │   │ [รูป]  │   │ [รูป]  │  ← 1-6 ชิ้น    │
│   └────────┘   └────────┘   └────────┘                │
│                                                       │
│   💡 อุทยานแห่งชาติ จังหวัดเลย   ← เมื่อทีมงานเปิด      │
│                                    ตอบถูกแล้ว 12 คน   │
└───────────────────────────────────────────────────────┘

ตอนเฉลย
┌───────────────────────────────────────────────────────┐
│              ╭──────────────╮                         │
│              │  ภูกระดึง     │                        │
│              ╰──────────────╯                         │
│   [รูป]        [รูป]        [รูป]                     │
│   หมีพูห์       กระ          ดึง      ← คำกำกับโผล่    │
│      ภู    +   กระ    +     ดึง                       │
│                                                       │
│         [ ภาพผาหล่มสักเต็มความกว้าง ]                  │
│   ตอบถูก 23/38 คน · เร็วที่สุด: พี่แต๋ว 8.2 วิ         │
└───────────────────────────────────────────────────────┘
```

- `screen_mode = 'bus_tv'` → รูปใบ้ใหญ่ขึ้น (สูงสุด 4 รูปต่อแถว) · ตัดอนิเมชันประกอบคำเหลือ fade
  (เหตุผลเดิม: CPU ทีวี Android บนรถ) · ลดของประดับมุมจอลงเพื่อคืนพื้นที่ให้รูป
- **หน้ากติกา** — ก่อนกดเริ่ม จอเวทีขึ้นกติกา 4 บรรทัดตามเอกสารตัวอย่าง
  (ทายคำจากภาพใบ้ · ตอบถูกได้ 1 คะแนน · ตอบได้ไม่จำกัดครั้ง · คะแนนมากสุดชนะ)
  ข้อความ generate จากค่าจริงของชุด ไม่ใช่ hardcode — ถ้าตั้ง `attempt_limit` ไว้ต้องขึ้นตามนั้น

### 7.2 มือถือทีมงาน `/staff/puzzle/host/:sessionId` (`PuzzleHost.jsx`)

```
┌────────────────────────────────────┐
│ ข้อ 4/12 · เหลือ 1:12              │
│ ตอบถูก 12/38 · เดามาแล้ว 74 ครั้ง   │  ← poll 1.5 วิ (ของเดิม)
│                                    │
│ ┌ คำตอบที่เกือบถูก ──────────────┐ │
│ │ "ภูกะดึง"      3 คน    [ รับ ] │ │  ← ข้อ 4.1 ชั้นที่ 3
│ │ "ภู กระดึง"    1 คน    [ รับ ] │ │
│ └────────────────────────────────┘ │
│                                    │
│ [ 💡 เปิดคำใบ้ 1/3 ]                │
│    "อุทยานแห่งชาติ จังหวัดเลย"      │  ← เห็นเนื้อก่อนกด
│                                    │
│ ┏ ปิดรับ + เฉลย ━━━━━━━━━━━━━━━┓  │
│ ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛  │
│ [ ข้ามข้อนี้ ] [ อันดับ ] [ จบเกม ]│
└────────────────────────────────────┘
```

**ต้องเห็นเนื้อคำใบ้ก่อนกดเปิด** — ทีมงานต้องรู้ว่ากำลังจะแจกอะไรให้ห้อง

**"เดามาแล้ว 74 ครั้ง แต่ถูก 12" คือสัญญาณว่าห้องกำลังตัน** — คนเดารัวแปลว่ายังไม่มีใคร
จับทางได้ ต่างจาก "เดามา 15 ครั้ง ถูก 12" ที่แปลว่าข้อง่ายไป · ตัวเลขคู่นี้คือสิ่งที่คนคุมเกม
ใช้ตัดสินว่าจะเปิดคำใบ้ขั้นถัดไปหรือปิดรับเลย

### 7.3 มือถือลูกทัวร์ `/t/:code/puzzle` (`Puzzle.jsx`)

```
กำลังเล่น                        ตอบถูกแล้ว
┌──────────────────────┐        ┌──────────────────────┐
│      3 พยางค์         │        │ ✓ ถูกต้อง! +1 คะแนน  │
│  [รูป] [รูป] [รูป]    │        │                      │
│                      │        │ ตอนนี้ได้ 4 คะแนน     │
│ 💡 อุทยานแห่งชาติ     │        │                      │
│    จังหวัดเลย         │        │ [รูป][รูป][รูป] ← ย่อ │
│                      │        │                      │
│ ┌──────────────┐     │        │  รอทีมงานเฉลย...     │
│ │ พิมพ์คำตอบ.. │[ส่ง]│        └──────────────────────┘
│ └──────────────┘     │
└──────────────────────┘
```

**มือถือลูกทัวร์มีปุ่มเดียวคือ "ส่ง"** — ไม่มีปุ่มขอคำใบ้ ไม่มีตัวเลือกอะไรให้กดผิด
คำใบ้โผล่มาเองเมื่อคนคุมเกมเปิด (ข้อ 4.3) ยิ่งจอเล็กบนรถที่โยกมีของให้กดน้อยยิ่งดี

### 7.4 หน้ารวมและคลัง

- `/staff/puzzle` (`PuzzleManager.jsx`) — โครงเดียวกับ `QuizManager.jsx` (ห้องที่เปิดอยู่ + คลังชุด)
- `/staff/puzzle/builder/:setId` (`PuzzleBuilder.jsx`) — ต่อข้อกรอก:
  รูปใบ้ 1-6 ชิ้น (ลาก-เรียง · เลือกรูป/อีโมจิ/คำ · **ไม่ต้องเลือกเลย์เอาต์**) + คำกำกับของแต่ละชิ้น +
  คำตอบ + **คำที่ยอมรับเพิ่ม (อยู่ติดกัน)** + `ภู-กระ-ดึง` + คำใบ้ 1-3 ขั้น + ภาพเฉลย +
  **พรีวิวจอเวทีที่ใช้ `puzzleLayout.js` ตัวเดียวกับของจริง**
- เพิ่ม `GameCard` ใบใหม่ใน `pages/staff/Games.jsx` และ `pages/guest/Games.jsx`

> **Builder ต้องมีปุ่ม "ทดลองตรวจคำตอบ"** — พิมพ์คำที่คิดว่าลูกทัวร์จะตอบ แล้วดูว่าระบบรับไหม
> เป็นวิธีเดียวที่คนสร้างข้อ "กาละแม" จะรู้ตัวก่อนขึ้นเวทีว่า "กะละแม" ยังไม่ผ่าน

---

## 8. RPC ใหม่ (ที่เหลือใช้ของเดิมทั้งหมด)

| ฟังก์ชัน | ใคร | ทำอะไร |
|---|---|---|
| `quiz_norm_answer(text) → text` | ภายใน | normalize ชั้นที่ 1 · `IMMUTABLE` |
| `quiz_loose_answer(text) → text` | ภายใน | normalize ชั้นที่ 2 (ตัดวรรณยุกต์ + การันต์พร้อมพยัญชนะ) · `IMMUTABLE` |
| `quiz_edit_distance(text,text) → int` | ภายใน | plpgsql เขียนเอง **ห้ามพึ่ง fuzzystrmatch** (ข้อ 4.1) |
| `quiz_puzzle_guess(session_id, player_id, question_id, text)` | แขก | ตรวจ state/เวลา/rate limit → INSERT `quiz_puzzle_guesses` → ถ้าถูก เขียนแถวสรุปลง `quiz_answers` + `score += points_per_correct` → คืน `{correct}` **ไม่บอกเฉลย ไม่บอกว่าใกล้แค่ไหน** |
| `quiz_puzzle_hint(session_id, token, step)` | host | เปิดให้ทั้งห้อง — `hint_level = step` + คัด**เฉพาะ**เนื้อขั้นนั้นลง `hint_payload` |
| `quiz_puzzle_near_misses(session_id, question_id, token)` | host | จัดกลุ่มคำที่ระยะ ≤ 2 พร้อมจำนวนคน |
| `quiz_puzzle_accept(session_id, question_id, token, text)` | host | รับคำนั้นเป็นถูกย้อนหลัง + เพิ่มเข้า `answer_aliases` ถาวร |
| `quiz_puzzle_stats(session_id, question_id, token)` | host | จำนวนคนตอบถูก / จำนวนครั้งที่เดา / จำนวนคนที่ยังไม่ตอบถูกและยัง online |
| `quiz_puzzle_upsert(question, clues, key, staff_id, pin)` | สตาฟ | เขียน question + `quiz_puzzle` + clues + keys ใน transaction เดียว (ตรวจ PIN แบบ `verify_tour_staff_pin`) |

**ที่ต้องแก้ของเดิม 3 ตัว:**

- `quiz_next` — ล้าง `hint_level = 0` และ `hint_payload = '[]'` (ข้อ 4.3)
- `quiz_lock` — เขียนแถวสรุป 0 คะแนนให้คนที่ยังไม่ตอบถูก **เฉพาะข้อ `kind='puzzle'`**
- `quiz_reveal` — ใส่ `answer_image_url` + `clue_labels` + `answer_split` ลง `reveal_payload`

ทุกตัวเป็น `SECURITY DEFINER SET search_path = public` + `GRANT EXECUTE TO anon, authenticated`

---

## 9. Routes + สิทธิ์

```jsx
// src/lib/permissions.js — เพิ่ม 3 บรรทัด (ระดับเดียวกับ quiz.*)
'puzzle.host': 'staff',
'puzzle.edit': 'lead',
'puzzle.define': 'admin',
```

```jsx
// src/App.jsx
<Route path="/staff/puzzle"                  element={staffRoute('puzzle.host', <PuzzleManager />)} />
<Route path="/staff/puzzle/builder/:setId"   element={staffRoute('puzzle.edit', <PuzzleBuilder />)} />
<Route path="/staff/puzzle/host/:sessionId"  element={staffRoute('puzzle.host', <PuzzleHost />)} />
<Route path="/staff/puzzle/stage/:sessionId" element={/* ไม่มี RequireRole — เหมือน QuizStage */} />
// guest
<Route path="puzzle" element={<GuestPuzzle />} />
```

ไม่ต้องมี legacy redirect (ของใหม่ ไม่มี QR เก่าชี้มา)

---

## 10. ของเดิมที่ต้องแก้ — รายการครบ ไม่มีมากกว่านี้

**นี่คือราคาของการใช้เครื่องยนต์ร่วมกัน ต้องแก้ครบทุกบรรทัด ไม่งั้นชุดปริศนาจะโผล่ในควิซ**

| ไฟล์ | บรรทัด | ต้องเติม |
|---|---|---|
| `src/pages/staff/QuizManager.jsx` | 60, 66, 76, 197 | `.eq('game_kind', 'quiz')` ทั้ง 4 query |
| `src/pages/guest/Games.jsx` | 96 | นับห้องเฉพาะ `game_kind = 'quiz'` + เพิ่ม query ห้อง puzzle |
| `src/pages/staff/Games.jsx` | 96 | เหมือนกัน |
| `src/pages/guest/Quiz.jsx` | 281 | รายการห้องต้องกรอง `game_kind` |
| `src/pages/staff/QuizReport.jsx` | 96 | รายงาน puzzle ต้องอ่านคนละหน้า (เฟส 3) — ระหว่างนี้กรองออกก่อน |
| `src/lib/quizHost.js` | 258 | ตอนสร้างห้อง ต้อง copy `game_kind` จาก set ลง session |
| `supabase/migrations/20260818_quiz.sql` | CHECK `kind`, `stage_theme` | ขยายค่าที่รับได้ (ข้อ 5.8) |
| `quiz_next` / `quiz_lock` / `quiz_reveal` | RPC | ข้อ 8 |

~~`useQuizSession.js` ไม่ต้องแก้~~ — **ผิด ต้องแก้** ดูข้อ 15.1

---

## 11. แผนแบ่งเฟส

| เฟส | ได้อะไร | ของใหม่ |
|---|---|---|
| **1. เล่นได้จริง** | รูปใบ้ 1-6 (อีโมจิ + รูป) จัดเรียงอัตโนมัติ · พิมพ์ตอบไม่จำกัดครั้ง · normalize 2 ชั้น · คำใบ้ 1-3 ขั้นที่คนคุมเกมเปิด · ภาพเฉลย · จอเวทีสกิน `arcade` · 1 คะแนน/ข้อ | migration 1 ไฟล์ · `lib/puzzleLayout.js` · `PuzzleManager/Builder/Host/Stage.jsx` · `guest/Puzzle.jsx` |
| **2. ฉลาดขึ้นหน้างาน** | รายการ "เกือบถูก" + ปุ่มรับย้อนหลัง + alias สะสม | 3 RPC ในข้อ 8 |
| **3. รายงาน + คลัง** | สรุปหลังเกม (เดาเฉลี่ยกี่ครั้ง / เปิดคำใบ้ถึงขั้นไหน / ข้อไหนยากเกิน) · clone ชุด · ผูก `destination_id` | ต่อยอด `quiz_report` / `quiz_clone_set` |
| **4. ของเล่นเพิ่ม** | รูปใบ้ที่เริ่มปิดไว้แล้วค่อยเปิดทีละรูป · โบนัส 3 คนแรก | ยังไม่ตัดสิน |

**แนะนำให้หยุดดูหลังเฟส 1** — เกมทายคำวัดกันที่บรรยากาศจริงหน้างานซึ่งเดาจากโต๊ะทำงานไม่ได้
ถ้าสนามจริงบอกว่า 90 วิยาวไป หรือควรมีคำใบ้ 3 ขั้นทุกข้อ ตัวเลขพวกนี้แก้ที่ค่าตั้งต้นของชุด
ไม่ต้องแก้สถาปัตยกรรม

**เฟส 2 จำเป็นกว่าที่เห็น** — ถ้าไม่มีปุ่ม "รับ" ทีมงานจะเจอลูกทัวร์ยืนยันว่าตอบ "กะละแม"
ถูกแล้วแต่ระบบไม่ให้ ตั้งแต่ทริปแรก และนั่นคือเหตุผลที่ฟีเจอร์ถูกเลิกใช้

---

## 12. ความเสี่ยง

| เรื่อง | ผลถ้าเกิด | กัน |
|---|---|---|
| **ไม่มีใครสร้างชุดเพราะหารูปยาก** | ฟีเจอร์ตายเงียบ | ปุ่มอีโมจิ/คำ (ข้อ 4.4) — **ความเสี่ยงอันดับหนึ่ง ไม่ใช่เรื่องเทคนิค** |
| ตรวจคำตอบไม่ผ่านทั้งที่ตอบถูก | ทะเลาะกันหน้างาน | normalize 2 ชั้น + ช่อง alias ใน Builder + ปุ่ม "รับ" (เฟส 2 — อย่าเลื่อน) |
| คนตะโกนเฉลย | ข้อนั้นจบใน 3 วินาที · **หนักกว่าเดิมเพราะไม่มีคะแนนความเร็ว** | โหมดทีมเป็นค่าเริ่มต้น (ข้อ 4.6) |
| อีโมจิไม่ขึ้นบนทีวี Android ในรถ | จอโล่ง เล่นไม่ได้ | ทดสอบกับทีวีจริงก่อนทริป · แผนสำรอง subset `NotoColorEmoji` ลง `public/fonts/` |
| รูปไม่ย่อ / ครอปเป็นจัตุรัส | หมดโควตา egress ในเกมเดียว / รูปใบ้ถูกตัดจนตอบไม่ได้ | preset 900px + `object-contain` (ข้อ 4.4) |
| คีย์บอร์ดบังรูปใบ้ | เล่นไม่ได้บนมือถือเลย | `visualViewport` (ข้อ 4.5) — **ต้องทดสอบบน iOS Safari จริง ไม่ใช่ DevTools** |
| ลืมล้าง `hint_payload` | คำใบ้ข้อเก่าค้างบนจอข้อใหม่ = แจกเฉลย | เขียนไว้ใน `quiz_next` ตั้งแต่ต้น + unit test |
| ส่ง `answer_image_url` ไปพร้อมข้อ | เปิด Network tab เห็นรูปเฉลยก่อนทั้งห้อง | อยู่ใน `quiz_keys` ออกทาง `reveal_payload` เท่านั้น |
| ลืมปิด `streak_bonus` | ตอบถูก 3 ข้อติดแล้วชนะขาดตั้งแต่ต้นเกม | บังคับปิดเมื่อ `game_kind='puzzle'` |
| ลืมเติม `game_kind` ในไฟล์ใดไฟล์หนึ่ง | ชุดปริศนาโผล่ในคลังควิซ | รายการครบในข้อ 10 |

---

## 13. เรื่องที่ตัดสินแล้วระหว่างทาง / ที่ยังเปิดอยู่

| # | เรื่อง | สถานะ |
|---|---|---|
| 1 | ชื่อเกมในแอป | **"ปริศนาใบ้คำ"** ตามชื่อในเอกสารตัวอย่าง |
| 2 | คำใบ้ | **คนคุมเกมเปิดอย่างเดียว สูงสุด 3 ขั้น** — ตัดปุ่มขอคำใบ้ของผู้เล่นทิ้ง (ข้อ 4.3) |
| 3 | จำนวนรูปใบ้ | **1-6 รูป จัดเรียงอัตโนมัติ** (ข้อ 4.7) |
| 4 | จำนวนครั้งที่ตอบ | **ไม่จำกัด** (ข้อ 4.2) |
| 5 | เปิดโหมดทีมเป็นค่าเริ่มต้นไหม | **ยังไม่ตัดสิน** — ข้อเสนอคือเปิด เพราะเป็นทางแก้เดียวที่ได้ผลจริงกับปัญหาคนตะโกน (ข้อ 4.6) และไม่ต้องเขียนโค้ดเพิ่มเลย |
| 6 | โบนัส "3 คนแรกที่ตอบถูก +1" | **ยังไม่ทำในเฟส 1** — ขัดกับกติกา 1 คะแนน ถ้าสนามจริงบอกว่าเงียบเกินค่อยเพิ่ม |
| 7 | เวลา 90 วินาทีต่อข้อ | 90 คือเพดาน ไม่ใช่เวลาที่ต้องรอ — ทีมงานกดปิดได้ตลอด |
| 8 | เสียง | มีบนจอเวทีเท่านั้น ปิดโดยปริยายเมื่อ `screen_mode = 'bus_tv'` (ลำโพงรถใช้ประกาศ) |

---

## 14. ถ้าอนุมัติแบบนี้ ลำดับลงมือ

1. `supabase/migrations/20260826_word_puzzle.sql` — ตาราง + คอลัมน์ + CHECK + RPC ของเฟส 1
2. รันบน Supabase จริงทันทีหลังเขียนเสร็จ **อย่ารอจนหน้าเสร็จ** — บทเรียนจากควิซคือ
   ปัญหาของจริง (`gen_random_bytes`, Security Advisor) โผล่ตอนรันบนของจริงเท่านั้น
3. เติม `game_kind` ครบ 6 ไฟล์ในข้อ 10 **ก่อน** เขียนหน้าใหม่ — เป็นการแก้ที่กระทบของที่ใช้อยู่
   ต้องเห็นว่าควิซยังทำงานปกติก่อนไปต่อ
4. `PuzzleBuilder` ก่อน `PuzzleStage` — ต้องมีข้อจริงถึงจะทดสอบจอได้
   (ใส่ "ภูกระดึง" กับ "กาละแม" จากเอกสารตัวอย่างเป็นสองข้อแรกไปเลย)
5. เล่นทดสอบกับคน 3-4 คนบนมือถือจริง **ก่อน** ทำสกิน `arcade` ให้สวย

---

**เอกสารอ้างอิง:** `เกมใบ้คำ_ตัวอย่าง.pdf` (ในโฟลเดอร์เดียวกัน) — กติกาต้นฉบับ
และตัวอย่างข้อจริง 2 ข้อพร้อมเฉลย

---

## 15. ภาคผนวก — สิ่งที่เปลี่ยนตอนลงมือเขียนจริง (เฟส 1 เสร็จแล้ว)

### 15.1 `useQuizSession.js` ต้องแก้ ไม่ใช่ไม่ต้องแก้

แบบเขียนไว้ว่า hook นี้ subscribe ทั้งแถวอยู่แล้วจึงได้คอลัมน์ใหม่มาเอง — **ผิด**

`SESSION_COLS` เป็นรายชื่อคอลัมน์แบบระบุเอง ไม่ใช่ `select('*')` ผลคือสองทางเข้าให้ค่าไม่ตรงกัน:

| ทางเข้า | ได้คอลัมน์ไหน |
|---|---|
| realtime (`payload.new`) | ครบทุกคอลัมน์ รวม `hint_payload` |
| polling ทุก 4 วิ / โหลดครั้งแรก | เฉพาะที่ระบุใน `SESSION_COLS` |

อาการที่จะเจอถ้าไม่แก้: **คำใบ้ขึ้นแป๊บนึงแล้วหายไปทุก 4 วินาที** วนแบบนั้นทั้งข้อ
เพิ่ม `game_kind, hint_level, hint_payload` เข้าไปแล้ว พร้อมคอมเมนต์เตือนไว้ที่ตัวคงที่

hook ยังถูกขยายอีกอย่าง: เมื่อข้อที่โหลดมาเป็น `kind = 'puzzle'` จะดึง `quiz_puzzle`
กับ `quiz_puzzle_clues` ต่อให้เลย ทุกจอจึงได้ก้อนเดียวกันจาก hook เดียวกัน

### 15.2 `[[:punct:]]` ของ Postgres กินทัณฑฆาต (์)

**บั๊กที่อ่านโค้ดยังไงก็ไม่เห็น เจอตอนรันบน Postgres 16 จริง**

`quiz_norm_answer` เดิมตัดเครื่องหมายวรรคตอนด้วย `regexp_replace(x, '[[:punct:]]', '', 'g')`
ซึ่ง Postgres จัด `์` (ทัณฑฆาต) ไว้ในคลาสนั้นด้วย ผลคือ:

```
บุรีรัมย์ → (ชั้นที่ 1) → บุรีรัมย     ← การันต์หายไปแล้ว
          → (ชั้นที่ 2) → บุรีรัมย     ← ตัดการันต์ไม่ได้ เพราะไม่มีให้ตัด
คนพิมพ์  บุรีรัม                        ← ไม่มีวันตรงกัน
```

กลไกทั้งชั้นที่ 2 ของข้อ 4.1 ตายสนิทโดยไม่มี error ให้เห็น

**แก้:** เลิกใช้ `[[:punct:]]` เปลี่ยนไประบุอักขระตรงๆ ด้วย `translate()`
ซึ่งได้ผลพลอยได้คือไม่ขึ้นกับ collation ของเครื่องที่รันด้วย (เครื่องพัฒนา C.UTF-8
กับ Supabase ที่ตั้ง en_US.UTF-8 ให้ผลต่างกันได้ในคลาสอักขระและช่วง `ก-ฮ`)

**บทเรียนที่กว้างกว่านั้น:** คลาสอักขระ POSIX ถูกออกแบบมาก่อนยูนิโคด
อย่าใช้กับภาษาไทยถ้ายังไม่ได้ทดสอบด้วยคำจริง

### 15.3 คะแนนขยับตอนตอบถูก ไม่ใช่ตอนเฉลย (ต่างจากควิซ)

`quiz_submit` ของควิซตั้งใจ **ไม่** บวกคะแนนตอนส่ง เพราะ `quiz_players` เปิดให้อ่าน
(ใช้ทำกระดานอันดับ) ถ้าคะแนนขยับทันที ลูกทัวร์ poll แถวตัวเองก็รู้ว่าตอบถูกก่อนเวลาเฉลย

เกมนี้ **ตั้งใจบอกทันทีอยู่แล้ว** — ไม่งั้นคนจะพิมพ์คำเดิมซ้ำไปเรื่อยๆ ทั้งที่ตอบถูกแล้ว
จึงไม่มีอะไรให้รั่วเพิ่ม และ `quiz_puzzle_guess` บวกคะแนนตรงนั้นเลย

**ผลที่ตามมาซึ่งห้ามลืม:** `quiz_reveal` ต้องมีสาย `puzzle` ที่ **ไม่คิดคะแนน**
ถ้าเผลอปล่อยให้ตกไปรวมกับสาย `mcq/tf` คะแนนจะถูกบวกซ้ำทั้งห้อง

### 15.4 `quiz_start_session` คัด `game_kind` ให้เอง

แบบเขียนไว้ว่าให้ `quizHost.js` คัดค่าลงห้องตอนสร้าง — เปลี่ยนไปทำฝั่ง DB แทน

`game_kind` เป็นตัวตัดสินว่าห้องจะไปโผล่ในหน้าควิซหรือหน้าปริศนา ถ้าให้ client ส่งมา
ก็เท่ากับให้ client เลือกได้ว่าจะไปโผล่ที่ไหน และมีโอกาสไม่ตรงกับชุดที่ห้องใช้จริง
ซึ่งผู้ใช้จะเห็นเป็น "กดเข้าห้องแล้วจอว่าง" · ฟังก์ชันคัดจาก `quiz_sets` ให้เลย
พร้อมตั้ง `stage_theme = 'arcade'` และชื่อห้องเริ่มต้นให้ตรงกับเกม

### 15.5 ไฟล์ที่เพิ่ม/แก้จริง

**เพิ่ม**

| ไฟล์ | ทำอะไร |
|---|---|
| `supabase/migrations/20260826_word_puzzle.sql` | ตาราง 3 ตัว · คอลัมน์ใหม่ · normalize ไทย 2 ชั้น + edit distance · RPC 10 ตัว · แก้ของเดิม 4 ตัว |
| `src/lib/puzzleLayout.js` | จัดกริดรูปใบ้ · แยกพยางค์ · ตรวจก่อนบันทึก (ฟังก์ชันบริสุทธิ์) |
| `src/lib/puzzleHost.js` | ห่อ RPC ของเกมนี้ ใช้ token ร่วมกับ `quizHost.js` |
| `src/components/puzzle/ClueBoard.jsx` | กระดานรูปใบ้ — ใช้ตัวเดียวกันทั้ง 4 จอ |
| `src/pages/staff/PuzzleManager/Builder/Host/Stage.jsx` | ฝั่งทีมงาน |
| `src/pages/guest/Puzzle.jsx` | ฝั่งลูกทัวร์ |
| `src/lib/__tests__/puzzle-layout.test.mjs` | 24 เคส ต่อท้าย `npm test` |

**แก้ของเดิม** — ครบตามข้อ 10 บวก `useQuizSession.js` (ข้อ 15.1),
`quizMedia.js` (รับ preset ขนาดรูป), `quizHost.js` (`stageUrl` รับชนิดเกม),
`tailwind.config.js` (อนิเมชันสั่นตอนเดาผิด), locale ทั้งสามภาษา

### 15.6 ผลทดสอบ (Postgres 16 จริง ไม่ใช่การอ่านโค้ดเอา)

รัน `20260818_quiz.sql` → `quiz_teams` → `game_hardening` → `quiz_stage_skin` →
`20260826_word_puzzle.sql` ตามลำดับบน Postgres 16.13 แล้วจำลองการเล่นทั้งรอบ

| สิ่งที่ทดสอบ | ผล |
|---|---|
| รันซ้ำทั้งไฟล์ (idempotent) | ผ่าน |
| `จังหวัด ภูกระดึง` = `ภูกระดึง` | ผ่าน |
| `บุรีรัม` = `บุรีรัมย์` (ชั้นที่ 2) | ผ่าน — หลังแก้ข้อ 15.2 |
| `อ่างทอง` ไม่โดนกฎคำนำหน้ากิน `อ` | ผ่าน |
| เดาผิดแล้วเดาใหม่ได้ · ตอบถูกแล้วส่งซ้ำได้ `already` | ผ่าน |
| กดรัว < 400ms → `too_fast` · ส่งข้อความเดิม → `duplicate` | ผ่าน |
| เปิดคำใบ้ข้ามขั้น (กด 2 ก่อน 1) → ไม่เกิดอะไรขึ้น | ผ่าน |
| `ภูกะดึง` ขึ้นรายการเกือบถูก (ระยะ 1, 2 คน) | ผ่าน |
| กดรับ → 2 คนได้คะแนนย้อนหลัง + เก็บเข้า `answer_aliases` | ผ่าน |
| กดเฉลยซ้ำ → คะแนนไม่บวกรอบสอง | ผ่าน |
| ขึ้นข้อใหม่ → `hint_level = 0`, `hint_payload = []` | ผ่าน |
| alias ที่ตั้งไว้ตั้งแต่แรก (`กะละแม`) ใช้ได้ | ผ่าน |
| `npm run build` | ผ่าน (453 โมดูล) |
| `npm test` | ผ่าน — รวมชุดใหม่ 24 เคส |

### 15.7 ที่ยังไม่ได้ทำ และรู้ตัวว่าไม่ได้ทำ

- **หน้ารายงานหลังเกม** — `quiz_report` ยังไม่รู้จัก `kind = 'puzzle'` (เฟส 3 ตามแผน)
  ระหว่างนี้ `QuizReport.jsx` กรอง `game_kind` ออกไปก่อน
- **โหมดทีม** — โครงรองรับอยู่แล้ว แต่ยังไม่ได้ต่อ UI เลือกทีมในหน้าลูกทัวร์ของเกมนี้
- **`npm run lint` ใช้ไม่ได้อยู่แล้วตั้งแต่ก่อนงานนี้** — repo ไม่มีไฟล์ตั้งค่า ESLint
  (`eslint .` หาไม่เจอ) ไม่ได้แตะ แต่ควรรู้ไว้

### 15.8 ผลการรันบน Supabase จริง

รันแล้วเมื่อ 26 ส.ค. 2026 บนโปรเจกต์ `iirhnjoqpwwwdgoghnkc` (**Postgres 17**, collation `en_US.UTF-8`)
แบ่งเป็น 5 migration ตามหัวข้อของไฟล์ เพื่อให้ error ชี้จุดได้ตรงถ้ามี — ผ่านครบทั้ง 5

**อาการที่ทำให้รู้ว่ายังไม่ได้รัน:** `Could not find the 'game_kind' column of 'quiz_sets' in the schema cache`
เป็นข้อความจาก PostgREST ไม่ใช่จาก Postgres — แปลว่าโค้ดหน้าบ้านพร้อมแล้วแต่ฐานข้อมูลยังไม่มีคอลัมน์
(ถ้ารัน migration แล้วแต่ยังเจอข้อความนี้ ให้ `NOTIFY pgrst, 'reload schema';` เพื่อไล่แคชของ PostgREST)

| สิ่งที่ตรวจบนของจริง | ผล |
|---|---|
| ชุด/ห้องเดิมทั้งหมดยังเป็น `game_kind = 'quiz'` (ไม่ต้อง backfill) | 0 แถวผิด |
| normalize ทั้ง 10 เคส (รวม `บุรีรัม` = `บุรีรัมย์`, `อ่างทอง` ไม่โดนกฎคำนำหน้ากิน) | ผ่านครบ |
| ช่วง `[ก-ฮ]` ในกฎตัดการันต์ ใช้ได้บน collation `en_US.UTF-8` | ผ่าน |
| `quiz_puzzle_is_correct` — ตรงเป๊ะ / alias / มีคำนำหน้า / คำผิด | ผ่านครบ (ทดสอบใน transaction แล้ว ROLLBACK ไม่ทิ้งขยะ) |
| `quiz_puzzle_guesses` เปิดอ่านผ่าน REST ไม่ได้ | 0 grant |
| ฟังก์ชันใหม่ครบ | 10 `quiz_puzzle*` + 3 ตัว normalize |

**Security Advisor** — เพิ่มขึ้นตามที่คาดไว้เป๊ะ ไม่มีอะไรเกินคาด:

| เตือน | จำนวนที่เพิ่ม | สถานะ |
|---|---|---|
| `rls_enabled_no_policy` บน `quiz_puzzle_guesses` | +1 | **ตั้งใจ** — ตารางที่ไม่มี policy = ไม่มีใครอ่านผ่าน REST ได้ ซึ่งคือสิ่งที่ต้องการ |
| `anon/authenticated_security_definer_function_executable` | +8 ต่อ role | สถาปัตยกรรมทั้งระบบ เหมือน 37 ตัวเดิมของควิซ |
| `function_search_path_mutable` | **+0** | ฟังก์ชันใหม่ตั้ง `SET search_path = public` ครบทุกตัว (8 รายการที่ค้างอยู่เป็นของเก่าทั้งหมด) |

**ยังไม่ได้ทดสอบบนของจริง:** การเล่นครบรอบ (เปิดข้อ → เดา → เปิดคำใบ้ → รับคำเกือบถูก → เฉลย)
ทดสอบครบแล้วบน Postgres 16 ในเครื่อง แต่บน Supabase ตั้งใจไม่ทำ เพราะจะทิ้งห้อง/ผู้เล่น/คะแนนปลอม
ไว้ในฐานข้อมูลจริง — ควรทดสอบด้วยการเปิดห้องจริงในแอปแล้วเล่นกับคน 3-4 คนแทน

---

## 16. ภาคผนวก — รอบปรับปรุงหลังเล่นจริง (2026-09-10)

เจ้าของงานเล่นจริงแล้วสั่งแก้สี่เรื่อง ทั้งสี่เรื่องอยู่ในเอกสารนี้แล้ว
ยกเว้นข้อ 16.1 ที่เป็นกติกาใหม่

### 16.1 `quiz_sets.solve_limit` — ตอบถูกครบกี่คนแล้วเฉลยเอง

**อาการ:** คนแรกตอบถูกตั้งแต่วินาทีที่ 8 แล้วทั้งห้องนั่งรอจนครบ 90 วินาที
เพราะเฉลยขึ้นต่อเมื่อคนคุมเกมกดปุ่ม ซึ่งคนคุมเกมกำลังถือไมค์อยู่

**กติกา:** ตั้งที่ระดับ "ชุด" ไม่ใช่ระดับข้อ — โควตาต้องเท่ากันทั้งชุด
ไม่งั้นคนเล่นต้องเดาเองว่าข้อนี้ยังทันไหม

| ค่า | ผล |
|---|---|
| `1` | ใครตอบถูกก่อนได้คนเดียว แล้วเฉลยทันที — **ค่าเริ่มต้นของเกมนี้** |
| `N` | เปิดให้ตอบถูกได้ N คน คนที่ N ตอบถูกปุ๊บเฉลยปั๊บ |
| `NULL` | ไม่จำกัด — เฉลยเมื่อคนคุมเกมกดเท่านั้น (พฤติกรรมเดิม) |

ชุดปริศนาที่มีอยู่ก่อนหน้าถูก backfill เป็น `1` ตามที่สั่ง
ส่วนชุดควิซ/เปิดแผ่นป้ายไม่แตะ (เกมเปิดแผ่นป้ายจบข้อเองตั้งแต่คนแรกตอบถูกอยู่แล้ว)

**เหตุที่ต้องแตก `quiz_reveal` เป็นสองชั้น:** ตัวเดิมบังคับให้ส่ง token ของคนคุมเกม
ซึ่ง `quiz_puzzle_guess` (ลูกทัวร์เรียก) ไม่มีและต้องไม่มี — token ที่หลุดไปฝั่งมือถือ
แปลว่าลูกทัวร์คนเดียวสั่งข้ามข้อทั้งห้องได้ จึงย้ายเนื้อในไป
`quiz_reveal_internal(p_session_id)` ที่ `REVOKE` จาก anon/authenticated
แล้วให้ทั้ง `quiz_reveal` (ตรวจ token) และ `quiz_puzzle_guess` (ตัดสินจาก `solve_limit`)
เรียกผ่านตัวนั้น

**ด่านกันคนส่งพร้อมกัน:** `quiz_puzzle_guess` นับคนตอบถูกทั้งก่อนรับคำตอบและหลังบันทึก
ด่านแรกจำเป็นเพราะคนที่กดส่งในเสี้ยววินาทีเดียวกันจะเข้ามาก่อนที่ `state = 'reveal'`
ของคนก่อนหน้าจะ commit — ถ้าพึ่ง state อย่างเดียวจะมีคนที่ 2 แทรกเข้ามาได้

ผลทดสอบบน Supabase จริง (3 ค่า × ผู้เล่น 3 คน): `1` → เฉลยทันทีที่คนแรกถูก
คนที่ 2 ได้ `closed` · `2` → คนแรกถูกแล้วยัง `answering` คนที่ 2 ถูกแล้วเป็น `reveal`
คนที่ 3 ได้ `closed` · `NULL` → ตอบถูกกี่คนก็ไม่เฉลยเอง

### 16.2 จอเฉลยแบบเต็มจอ (`PuzzleStage.jsx`)

ข้อที่มีรูปเฉลย จอใหญ่เปลี่ยนเป็นเลย์เอาต์เฉพาะ: **รูปเฉลยเต็มจอ · คำเฉลยบนกลาง ·
ตัวสะกดล่าง** ไม่มีกระดานรูปใบ้ต่อท้าย (คนทั้งห้องเพิ่งจ้องมาเก้าสิบวินาที)
พื้นหลังเป็นรูปเดียวกันแบบเบลอ-ครอป จอ 16:9 กับรูป 4:3 จึงไม่เหลือแถบดำข้างๆ
โดยตัวรูปจริงไม่โดนตัดสักมิลลิเมตร มีไล่เฉดดำบน-ล่างกันตัวหนังสือขาวจมไปกับรูปสว่าง
ข้อที่ไม่มีรูปเฉลยยังใช้การ์ดเดิม (กระดานรูปใบ้ + คำกำกับ)

มือถือลูกทัวร์เรียงแบบเดียวกัน — เดิมรูปเฉลยถูกล็อกไว้ที่ 208px แล้วเหลือที่ว่างใต้รูปครึ่งจอ

### 16.3 ทางกลับของหน้าเกม

`lib/homeButton.js` ไม่เคยมี `puzzle`/`tiles` ใน `HIDE_GUEST_PAGES` ปุ่มลอย "หน้าหลัก"
จึงโผล่บนสองหน้านี้ และปุ่มนั้นเด้งไปหน้าแรกของทริป ไม่ใช่หน้ารวมเกม
แก้โดยใส่ทั้งสองชื่อเข้าไป แล้วเติม `<BackButton to={tp('games')} />` ที่หัวเรื่อง
"ตอนอยู่ในห้อง" ของทั้งสองเกม (ของเดิมมีเฉพาะหน้ารายการห้อง)

### 16.4 กรอบรูปใบ้ (`ClueBoard.jsx` + `puzzleLayout.js`)

สองอย่างที่ทำให้กรอบดูใหญ่เกินจริง:

1. **กรอบกินเต็มความกว้างช่องเสมอ** ส่วนรูปเป็น `object-contain` รูปแนวตั้งจึงได้
   กรอบขาวโล่งสองข้างที่กว้างกว่าตัวรูป และพาโนรามาได้กรอบขาวโล่งบน-ล่าง
   แก้ด้วย `clueFrameStyle(aspect, cellHeight)` — กรอบมี `aspect-ratio` เท่ารูปจริง
   และกว้าง `min(100%, cellHeight × aspect)` คือกรอบที่ใหญ่ที่สุดที่ยังหุ้มรูปพอดี
   ช่องยังสูงเท่ากันทุกใบ (กรอบลอยกลางช่อง) เพราะสิ่งที่ทำให้แถวเป็นระเบียบคือความสูงช่อง
2. **ความสูงช่องมากไปราว 1 ใน 5** — ย้ายไป `clueCellHeight(surface, rows)` เพื่อให้
   เทสต์จับได้ว่าใหญ่ขึ้นตอนไหน: จอเวที 46vh→38vh (แถวเดียว) · 26vh→21vh (หลายแถว)
   มือถือ 28vh→22vh · 18vh→15vh

ระหว่างทางเจอบั๊กเดิมด้วย: การจัดกึ่งกลางแถวสุดท้ายสั่ง `gridColumnStart` ไว้ที่
`<div>` กรอบ ซึ่งไม่ใช่ลูกของกริด (ลูกของกริดคือ `<figure>`) จึงไม่เคยทำงานเลย
และต่อให้สั่งถูกที่ grid ก็ทำได้แค่ "เยื้องขวา" ไม่ใช่กึ่งกลางจริง (2 ใบใน 3 คอลัมน์
ไม่มีคอลัมน์ตรงกลางให้ลง) เปลี่ยนกระดานเป็น `flex-wrap` + `justify-center`
ซึ่งจัดกึ่งกลางทุกแถวให้เองโดยไม่ต้องรู้ว่าแถวไหนไม่เต็ม
