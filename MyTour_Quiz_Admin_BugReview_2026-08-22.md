# ตรวจ bug ระบบควิซ (ฝั่งทีมงาน/แอดมิน) — 22 ส.ค. 2026

ขอบเขต: `QuizManager.jsx`, `QuizBuilder.jsx`, `QuizHost.jsx`, `QuizStage.jsx`, `QuizReport.jsx`,
`lib/quizHost.js`, `lib/useQuizSession.js`, `lib/quizMedia.js` + RPC ที่เกี่ยวข้องใน `supabase/migrations/`

ทุกข้อยืนยันกับโค้ดจริงแล้ว (ไม่ใช่ข้อสงสัยลอยๆ) — เรียงตามความรุนแรง

---

## 🔴 รุนแรง — กระทบหน้างานจริง

### 1. รายงานหลังเกมตีตรา "ข้อเดาตัวเลข" ว่ายากเกินไปทุกข้อ
**ที่:** `20260819_game_hardening.sql:198` + `20260818_quiz_library.sql:124` → แสดงผลที่ `QuizReport.jsx:135,189,264`

`quiz_reveal` ให้ข้อ numeric ตั้ง `is_correct = (rn = 1)` — **มีแค่คนที่เดาใกล้สุดคนเดียวที่ถือว่าถูก**
แต่ `quiz_report` คิด `correct_rate = 100 × count(is_correct) / count(answered)` แบบเดียวกับ mcq

**ผลลัพธ์:** ข้อ numeric ที่มีคนตอบ 40 คน จะได้ 100/40 = **3%** เสมอ → แถบแดง + ขึ้นเตือน
"ข้อยากเกินไป อาจกำกวม" + ดึงค่าเฉลี่ยทั้งเกมลง ทีมงานถูกชี้ให้ไปแก้ข้อที่จริงๆ ใช้ได้ดี

**แก้:** `quiz_report` ต้องแยก `kind = 'numeric'` ออกจากการตัดเกรดง่าย/ยาก
(หรือคิดตัวชี้วัดของมันเอง เช่น % ที่เดาคลาดไม่เกิน X) แล้ว branch ตาม `r.kind` ในหน้าจอ

---

### 2. จอเวทีเด้งเป็น "GAME OVER" กลางคัน
**ที่:** `QuizStage.jsx:204` (เงื่อนไข `&& question`) ตกไปที่ `return` ท้ายไฟล์บรรทัด 341

`useQuizSession` โหลดแถว session ก่อน แล้วค่อยยิงรอบสองไปดึง `quiz_questions`
ช่วงคาบเกี่ยวนั้น `phase === 'answering'` แต่ `question === null` → ไม่มี branch รองรับ
เลยหล่นไปบล็อกกระดานอันดับ ซึ่งขึ้นหัวข้อ **GAME OVER** พร้อมกระดานว่าง

**เคสจริง:** refresh จอโปรเจกเตอร์ระหว่างข้อ 4 หรือเพิ่งเปิดจอเวทีกลางเกม →
ทั้งรถเห็น "GAME OVER" ขึ้นเต็มจอแล้วค่อยเด้งกลับ ถ้า fetch คำถามพลาด จะค้างทั้งข้อ

**แก้:** เพิ่ม branch loading ก่อนบรรทัด 341
`if (['answering','locked','reveal','countdown'].includes(phase) && !question) return <จอโหลด/>`

---

### 3. ข้อเดาตัวเลขบันทึกเฉลยเป็น "0" ได้ ถ้าเว้นช่องว่าง
**ที่:** `QuizBuilder.jsx:221` — `Number(editing.correct_number)` และ `Number('') === 0`

ด่านตรวจก่อนบันทึก (บรรทัด 199–210) เช็คเฉพาะ `mcq` เท่านั้น และ `quiz_keys.correct_number`
ไม่มี CHECK ที่ DB → บันทึกผ่านเงียบๆ

**เคสจริง:** เพิ่มข้อ → เลือกเดาตัวเลข → พิมพ์คำถาม → ลืมใส่ตัวเลขเฉลย → บันทึก
ไม่มี error แถวในรายการขึ้น `· 0` เวลาเล่นจริงคนที่ตอบ 0 ชนะ

**แก้:** ถ้า `kind === 'numeric'` ให้ปฏิเสธเมื่อช่องว่างหรือไม่ใช่ตัวเลข เหมือนที่ทำกับ mcq

---

### 4. กด "ยกเลิก" ระหว่างอัปโหลด แล้วฟอร์มฟื้นคืนมาแบบพัง
**ที่:** `QuizBuilder.jsx:253` — `setEditing((s) => ({ ...s, media_url: url, media_kind: kind }))`

หลังกดยกเลิก `s` เป็น `null` และ `{...null}` = `{}` → `editing` กลายเป็นออบเจกต์ที่มีแค่ media
แต่ `editing &&` (บรรทัด 533) ยังจริง การ์ดแก้ไขเลยเด้งกลับมาแบบไม่มี kind/text/options

**เคสจริง:** แก้ข้อ → แนบวิดีโอ 9 MB → ระหว่างอัปโหลดกดยกเลิก → อีกวินาทีการ์ดเด้งกลับมาว่างเปล่า
กดบันทึกจะส่ง `p_kind: undefined` → ขึ้น error ดิบว่าหา function ไม่เจอ

**แก้:** `setEditing((s) => (s ? { ...s, media_url: url, media_kind: kind } : s))` + ลบไฟล์ที่เพิ่งอัปทิ้ง

---

### 5. ตัวเลขที่ลูกทัวร์ส่งหายเกลี้ยงทันทีที่กด "ปิดรับ"
**ที่:** `QuizStage.jsx:117–121` (เคลียร์ `liveNumbers` เมื่อ `phase !== 'answering'`) ขัดกับ 255, 269–285
(ที่ยังแสดงกลุ่มตัวเลขทั้งช่วง `answering` **และ** `locked`)

**เคสจริง:** ข้อเดาตัวเลข มี 38 คำตอบลอยเต็มจอ → ทีมงานกด "ปิดรับ" อย่างเดียว (ยังไม่เฉลย)
→ จอโปรเจกเตอร์ล้างตัวเลขทิ้งทั้งหมด ขึ้น **"0 ส่งแล้ว"** เหมือนคำตอบหายทั้งห้อง

**แก้:** ตอน lock ให้หยุดแค่ interval อย่าล้าง state — ล้างเฉพาะตอนเปลี่ยนข้อ

---

## 🟠 ปานกลาง

| # | เรื่อง | ที่ | ผล |
|---|-------|-----|-----|
| 6 | **ไม่มีกันกดซ้ำเลยทั้งหน้า QuizManager** (`handleCreateRoom` / `handleReplay` / `handleNewSet` / `handleClone` ไม่มี `busy` และปุ่มไม่ disable ต่างจาก QuizHost ที่ทำครบ) | `QuizManager.jsx:120,141,170,193` | เน็ตรถบัสช้า กดสร้างห้องซ้ำ = ได้ 2 ห้อง ห้องส่วนเกินค้างในรายการลูกทัวร์ตลอดไป (กด "สร้างชุด" ซ้ำ = ได้ 2 ชุด) |
| 7 | **เล่นซ้ำแล้วกระดานอันดับรอบเก่าติดมาด้วย** — `navigate` ไปห้องใหม่เปลี่ยนแค่ route param คอมโพเนนต์ไม่ remount และ effect กระดานมี early return ไม่เคยล้าง `board` | `QuizHost.jsx:75–78,384` | ห้องใหม่ยังไม่เริ่ม แต่โชว์อันดับของเกมที่แล้วอยู่ ค้างจนกว่าจะเฉลยข้อแรก |
| 8 | **เปลี่ยนชื่อชุดแล้วถูก poll 10 วิเขียนทับ** — `load()` ที่ยิงไปก่อน UPDATE อาจกลับมาทีหลังพร้อมชื่อเก่า | `QuizManager.jsx:220` vs `95` | ชื่อเด้งกลับเป็นของเดิมทั้งที่ DB ถูกแล้ว ดูเหมือนบันทึกไม่ติด |
| 9 | **`startSession` ล้มครึ่งทาง = ห้องผีที่ตั้งค่าไม่ครบ** — ห้องถูก INSERT แล้วก่อน `quiz_set_options` จะทำงาน ถ้าตัวหลังพลาดจะ throw และไม่ navigate | `quizHost.js:100–111` | ทีมงานเห็น error คิดว่าไม่เกิดอะไร แต่ห้องมีจริง โหมดทีมไม่ติด และลูกทัวร์เห็น/เข้าได้แล้ว |
| 10 | **host token รั่วผ่านลิงก์จอใหญ่** — `stageUrl` แนบ `#t=` และ `resolveHostToken` เซฟลง localStorage ของเครื่องที่เปิด แถมไม่มีการล้าง hash ออกจาก address bar เลย | `quizHost.js:52–67,306–311` | แปะลิงก์ในกลุ่มไลน์ให้คนขับแคสต์ขึ้นทีวี = ทุกคนในกลุ่มคุมเกมได้เต็มสิทธิ์ (จบเกม / เตะคน) ทั้งที่จอเวทีต้องใช้แค่สิทธิ์อ่าน |
| 11 | **ลบคำถามแล้วไฟล์สื่อเป็นกำพร้าถาวร** — `quiz_delete_question` คืน void ไม่ส่ง media_url กลับมา ต่างจาก `quiz_delete_set` ที่ตั้งใจเก็บกวาด + ไฟล์ที่อัปแล้วยกเลิกก็ไม่เคยถูกลบ | `QuizBuilder.jsx:266–273,239` | กิน quota Storage ไปเรื่อยๆ และ URL สาธารณะของรูปยังเปิดได้ |
| 12 | **แก้ข้อ True/False เป็นช้อยส์ ได้ช่องตัวเลือกแค่ 2 ช่อง** — `startEdit` ตั้ง `options: ['','']` และตอนสลับชนิดไม่ได้ reset options | `QuizBuilder.jsx:165,540` | ไม่มีทางใส่ตัวเลือก C/D ต้องลบข้อทิ้งสร้างใหม่ |
| 13 | **ลบตัวเลขในช่องเวลาแล้วขึ้น error ดิบจาก Postgres** — `Number('') = 0` และ `coalesce` ที่ RPC จับแค่ NULL → ชน CHECK 5–300 | `QuizBuilder.jsx:223` | ข้อความ `violates check constraint "quiz_questions_time_check"` ทีมงานอ่านไม่รู้เรื่อง |
| 14 | **`move()` / `remove()` ไม่ดู error เลย** — reorder เป็น optimistic ล้วน | `QuizBuilder.jsx:266,281` | PIN ถูกเปลี่ยน/เน็ตหลุด = ลำดับขยับบนจอแต่ DB ไม่เปลี่ยน refresh แล้วกลับที่เดิม เงียบสนิท |
| 15 | **รูปข้อเก่าค้างบนจอเวที** — `<img>` ไม่มี `key={question.id}` (ต่างจาก `<video>` ที่มี) | `QuizStage.jsx:247` | ข้อ 6 ขึ้นคำถามกับนาฬิกาแล้วแต่ยังโชว์รูปข้อ 5 บนเน็ตช้า |
| 16 | **ดึงคำถามแบบไม่มีตัวกันชนกัน/ไม่ retry** — `questionIdRef` ถูกเซ็ตก่อน await, ไม่ล้าง question ตอนเปลี่ยนข้อ, ถ้า fetch พลาดจะไม่มีวันยิงซ้ำ | `useQuizSession.js:193–210` | กดข้อถัดไปเร็วสองที = ข้อความข้อเก่ากับนาฬิกาข้อใหม่ / จอค้างตามข้อ 2 |
| 17 | **ปุ่ม "ข้อถัดไป" กับ "ข้ามข้อนี้" เรียกฟังก์ชันเดียวกันเป๊ะ** ในช่วง reveal/scoreboard | `QuizHost.jsx:317,347` | คนคุมเข้าใจผิดว่าข้ามข้อไปแล้ว พูดผิดข้อ — ควรซ่อนปุ่มข้ามเมื่อเฉลยไปแล้ว |

---

## 🟡 เล็กน้อย (ทยอยเก็บได้)

- **ตัดเกรดง่าย/ยากตั้งแต่มีคนตอบ 1 คน** — ทดสอบ 3 คนแล้วรายงานสั่งให้แก้ 2 ข้อที่ไม่ได้มีปัญหา (`QuizReport.jsx:134`) ควรมีขั้นต่ำ ~5 คน
- **`numeric_unit` ไม่ถูกแสดงบนจอเวทีเลย** — เฉลยขึ้นเป็น `2500` เปล่าๆ ไม่มี "เมตร" (`QuizStage.jsx:260`)
- **นาฬิกา 100 ms เดินตลอดเวลา** ไม่เช็ค visibility ไม่เช็ค state → จอเวทีค้างที่ lobby 15 นาที = re-render ~9,000 ครั้งบนแอนดรอยด์ทีวีเครื่องอ่อน (`useQuizSession.js:138`)
- **จอเวทีเทียบนาฬิกา server ครั้งเดียวตอนเปิด** — re-sync ผูกกับ `visibilitychange` ซึ่งจอโปรเจกเตอร์ไม่มีวันซ่อน เกมยาว 1 ชม. นาฬิกาเพี้ยนสะสม
- **error เน็ต/500 ถูกรายงานว่า "PIN ไม่ถูกต้อง"** — `loadQuestions` คืน false เหมือนกันหมด (`QuizBuilder.jsx:114`)
- **"เก็บเข้ากรุแทน" เด้งออกจากหน้าแม้บันทึกไม่สำเร็จ** — `saveSetMeta` กลืน error ไว้เอง แล้ว `navigate` ต่อทันที (`QuizBuilder.jsx:302`)
- **ล้างช่องเปลี่ยนชื่อจนว่างแล้วกด Enter = ปิดเงียบๆ** ไม่บอกอะไร (`QuizManager.jsx:212`)
- **ตัวนับคนตอบพาเลขข้อเก่ามาด้วย** — ข้อใหม่ยังนับถอยหลังอยู่แต่ขึ้น "12/12 ครบแล้ว" (`useQuizSession.js:232`)
- **preload วิดีโอถูก abort ตอนที่จะใช้พอดี** (`v.src=''` ใน cleanup) และลำดับ preload ไม่มี tiebreak `created_at` ทำให้โหลดผิดข้อได้ (`QuizStage.jsx:55,66`)
- **`summary.hardest` / `summary.easiest` คำนวณแล้วไม่ได้ใช้** และ PinGate ไม่ล้าง error ตอนพิมพ์ใหม่ (`QuizReport.jsx:45,132`)
- **poll 10 วิ ยิง 6 คิวรีทุกรอบ** รวมทั้ง `buses` / `destinations` ที่ไม่มีวันเปลี่ยนระหว่างทริป (`QuizManager.jsx:55`)

---

## ⚠️ เรื่องสิทธิ์ที่ควรตัดสินใจ

`quiz_sets` ยังเปิด `GRANT UPDATE ... TO anon` + `POLICY ... USING (true)` (จงใจไม่ปิดใน
`20260819_game_hardening.sql:21–24`) แปลว่า `updateSetMeta` ที่เพิ่งเพิ่มเข้าไปสำหรับ "เปลี่ยนชื่อชุด"
**ไม่มีการตรวจสิทธิ์ฝั่ง server เลย** — `can(session,'quiz.edit')` เป็นแค่ด่านฝั่งจอ และ UPDATE
ไม่ได้ scope ด้วย `org_id` ด้วย ทางเลือก:

1. ย้ายไปเป็น RPC ที่ตรวจ PIN แบบเดียวกับ `quiz_upsert_question` แล้ว REVOKE UPDATE (ปลอดภัยสุด)
2. อย่างน้อยใส่ `.eq('org_id', orgId)` กันการแก้ข้ามบริษัท (ครึ่งทาง)
3. ปล่อยไว้ตามเดิม (รับความเสี่ยงเหมือนที่เอกสาร hardening ระบุไว้)

หมายเหตุ: ข้อความ `SET_UPDATE_BLOCKED` ที่ผมเขียนไว้จึงยังไม่มีทางเกิดจาก RLS —
0 แถวแปลว่า "ชุดถูกลบไปแล้ว" ควรเปลี่ยนข้อความให้ตรงความจริง

---

## ลำดับที่แนะนำให้แก้

1. ข้อ 3, 4 (บันทึกข้อมูลผิด/ฟอร์มพัง — กระทบข้อมูลถาวร)
2. ข้อ 2, 5 (จอใหญ่โชว์ผิดต่อหน้าลูกทัวร์)
3. ข้อ 1 (รายงานชี้นำผิด)
4. ข้อ 6, 7, 8, 9 (ของที่เพิ่งเพิ่มเข้าไปรอบนี้)
5. ที่เหลือทยอยเก็บ
