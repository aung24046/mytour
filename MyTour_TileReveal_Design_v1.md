# เกม "เปิดแผ่นป้าย" — เอกสารออกแบบ v1

> **สถานะ:** เฟส 1 เขียนเสร็จและรัน migration บน Supabase แล้ว · 1 ก.ย. 2026
> ไมเกรชัน: `supabase/migrations/20260901_tile_reveal.sql`
> ยังไม่ได้ทดสอบเล่นครบรอบบนของจริงกับคนหลายคน · ต้อง `npm install` ก่อน build (มี dependency ใหม่)
> **ต่อยอดจาก:** `MyTour_WordPuzzle_Design_v1.md` (เครื่องยนต์ควิซ + การตรวจคำตอบไทย)
> **ไมเกรชันที่เกี่ยวข้อง:** `20260818_quiz.sql` · `20260819_game_hardening.sql` · `20260826_word_puzzle.sql`

---

## 1. เกมนี้คืออะไร

คนสร้างข้อแนบภาพมา 1 รูป ระบบแบ่งเป็นตารางแผ่นป้ายตามที่ตั้งค่า แล้วปิดไว้ทั้งหมด
คนคุมเกมค่อยๆ เปิดทีละแผ่น — เลือกเองว่าแผ่นไหน หรือกดสุ่มก็ได้ — จนมีคนตอบถูก
ตอนปิดอยู่ แต่ละแผ่นมีหมายเลขกำกับ และจะให้เห็นเป็นภาพที่ตั้งไว้เองก็ได้ (โลโก้บริษัท ลายทัวร์)

---

## 2. เกมนี้ไม่ใช่เกมใหม่ทั้งใบ — อ่านตรงนี้ก่อนเริ่มเขียนโค้ด

"เปิดแผ่นป้าย" กับ "ปริศนาใบ้คำ" คือเกมเดียวกันในเชิงโครงสร้าง ต่างกันแค่ **กลไกจ่ายคำใบ้**

| | ปริศนาใบ้คำ | เปิดแผ่นป้าย |
|---|---|---|
| สิ่งที่แสดง | รูปใบ้ 1-6 รูป | ภาพ 1 รูป แบ่ง N แผ่น |
| คนคุมเกมจ่ายอะไรทีละขั้น | คำใบ้ 3 ขั้น (`hint_level`) | แผ่นป้าย N แผ่น (`revealed_tiles`) |
| ผู้เล่นทำอะไร | พิมพ์ตอบ | เหมือนกัน |
| ตรวจคำตอบภาษาไทย | `quiz_norm_answer` / `quiz_loose_answer` / `quiz_edit_distance` | **ใช้ตัวเดิม 100% ไม่แตะเลย** |
| ห้อง ผู้เล่น นาฬิกา คะแนน ทีม | เครื่องยนต์ควิซ | เครื่องยนต์ควิซตัวเดียวกัน |

ของที่แพงที่สุดในเกมนี้ — การตรวจว่า "กะละแม" กับ "กาละแม" คือคำเดียวกัน — สร้างเสร็จและ
ผ่านสนามจริงมาแล้ว **ห้ามเขียนใหม่ ห้ามก็อปไปแก้** สิ่งที่ต้องเขียนจริงคือกระดานแผ่นป้าย
กับสถานะว่าเปิดแผ่นไหนไปแล้ว ประเมินแล้วราว 60% ของแรงงานที่ใช้ทำปริศนาใบ้คำ

### ชื่อที่ใช้

`game_kind = 'tiles'` · `quiz_questions.kind = 'tiles'` · RPC ขึ้นต้น `quiz_tiles_` · เส้นทาง `/staff/tiles/*`

**ห้ามใช้คำว่า `reveal` เป็นชื่อเกม** — ชนกับ `quiz_reveal` และ `reveal_payload` ซึ่งแปลว่า
"ช่วงเฉลยของทุกเกม" อยู่แล้ว ถ้าเอามาใช้เรียกเกมนี้ด้วย อีกหกเดือนจะอ่านโค้ดไม่ออกว่าอันไหนคืออันไหน

---

## 3. กติกา (เจ้าของโปรเจกต์เคาะแล้ว 1 ก.ย. 2026)

1. **ผู้เล่นตอบได้สองแบบ เลือกต่อชุด** (`quiz_sets.answer_mode`)
   - `type` — พิมพ์ตอบ ระบบตัดสินเอง (เฟส 1)
   - `none` — ไม่ต้องตอบ ไม่มีคะแนน คนคุมเกมเปิดภาพให้ดูเฉยๆ (เฟส 1 — เกือบฟรี ดูข้อ 11.3)
   - `buzz` — กดปุ่มขอตอบ คนคุมเกมตัดสิน (เฟส 2)
2. **คนแรก หรือทีมแรกที่ตอบถูก = จบข้อทันที** เปิดแผ่นป้ายทั้งหมด
   ต่างจากปริศนาใบ้คำที่ทุกคนตอบถูกได้คนละ 1 คะแนน — เกมนี้เป็นการแข่ง
3. **ตอบถูก = 1 คะแนนเท่ากันหมด ไม่ผูกกับจำนวนแผ่นที่เปิด** (ใช้ `quiz_sets.points_per_correct`)
4. **จำกัดจำนวนครั้งการตอบได้ — ตั้งที่ระดับชุด นับใหม่ทุกข้อ**
   เหตุผล: ปริศนามักเป็น "ทายว่าเพื่อนคนไหน" / "ผลไม้อะไร" ซึ่งคำตอบแคบมาก
   ถ้าเดาได้ไม่จำกัดจะไล่เดาจนถูกได้ · ทุกข้อในชุดเดียวกันใช้โควตาเท่ากัน
5. **คนคุมเกมเห็นคำตอบทุกคำเรียงตามเวลา พร้อมค่าความใกล้เคียง**
6. **ผู้เล่นเห็นแค่ถูก/ผิด และเหลืออีกกี่ครั้ง** ไม่เห็นความใกล้เคียงของตัวเอง
   บอกไปคือแจกคำใบ้ชั้นดี และขัดกับข้อ 4 โดยตรง

---

## 4. สคีมา

### 4.1 คอลัมน์ใหม่บนตารางเดิม

```sql
ALTER TABLE public.quiz_sets
  ADD COLUMN IF NOT EXISTS answer_mode text NOT NULL DEFAULT 'type';
  -- attempt_limit / points_per_correct มีอยู่แล้วจาก 20260826_word_puzzle.sql

ALTER TABLE public.quiz_sets ADD CONSTRAINT quiz_sets_answer_mode_check
  CHECK (answer_mode IN ('type', 'buzz', 'none'));
-- DEFAULT 'type' ทำให้ชุดควิซและชุดปริศนาใบ้คำเดิมทุกแถวถูกต้องเองโดยไม่ต้อง backfill
-- (แพตเทิร์นเดียวกับ game_kind) คอลัมน์นี้มีความหมายเฉพาะกับ game_kind = 'tiles' เท่านั้น

ALTER TABLE public.quiz_sessions
  ADD COLUMN IF NOT EXISTS revealed_tiles smallint[] NOT NULL DEFAULT '{}',
  ADD COLUMN IF NOT EXISTS last_tile_at   timestamptz,   -- ใช้จำกัดเวลาปุ่มยกเลิก (ข้อ 7.2)
  ADD COLUMN IF NOT EXISTS last_tile_n    smallint NOT NULL DEFAULT 0;
-- last_tile_n = การกดครั้งล่าสุดเปิดไปกี่แผ่น (open_step อาจเปิดได้น้อยกว่าที่ตั้งไว้
-- เมื่อเหลือแผ่นไม่พอ) ปุ่มยกเลิกต้องรู้ตัวเลขนี้ถึงจะถอยกลับได้ถูก
-- ★ ไม่ต้องอยู่ใน SESSION_COLS เพราะใช้ฝั่ง server เท่านั้น

ALTER TABLE public.quiz_puzzle_guesses
  ADD COLUMN IF NOT EXISTS closeness smallint;   -- 0-100 · NULL = ยังไม่เคยคำนวณ
```

**ทำไม `revealed_tiles` อยู่บน `quiz_sessions` ไม่ใช่ตารางแยก**

`useQuizSession` ส่งทั้งแถวผ่าน realtime อยู่แล้ว เปิด 1 แผ่น = อัปเดต 1 แถว = 1 event
ทุกจอเห็นพร้อมกัน และ poll ทุก 4 วินาทีซ่อมตัวเองถ้า websocket หลุด (บนรถบัสหลุดบ่อย)
ถ้าแยกเป็นตาราง `quiz_tile_opens` จะต้อง subscribe เพิ่มอีกช่อง แล้วได้ event เท่าเดิม
โดยแลกมากับความซับซ้อนและปัญหาการเรียงลำดับ

> ⚠️ **ต้องเติม `revealed_tiles` ใน `SESSION_COLS` ของ `src/lib/useQuizSession.js`**
> รายชื่อคอลัมน์นั้นระบุเอง ไม่ใช่ `select('*')` ถ้าลืมเติม realtime (ได้ครบทุกคอลัมน์)
> กับ polling (ได้เฉพาะที่ระบุ) จะให้ค่าไม่ตรงกัน อาการคือ **แผ่นป้ายเปิดแล้วปิดเองทุก 4 วินาที**
> เป็นบั๊กเดียวกับที่เคยเจอตอน `hint_payload` ของปริศนาใบ้คำ

### 4.2 ตารางเนื้อข้อ

```sql
CREATE TABLE IF NOT EXISTS public.quiz_tiles (
  question_id     uuid PRIMARY KEY REFERENCES public.quiz_questions(id) ON DELETE CASCADE,
  grid_rows       smallint NOT NULL DEFAULT 3,
  grid_cols       smallint NOT NULL DEFAULT 4,
  open_step       smallint NOT NULL DEFAULT 1,   -- กดหนึ่งครั้งเปิดกี่แผ่น
  cover_image_url text,                          -- ภาพตอนปิด · NULL = ช่องสีตามธีม
  crop_x real NOT NULL DEFAULT 0,                -- กรอบครอป 0.0-1.0 (ดูข้อ 6)
  crop_y real NOT NULL DEFAULT 0,
  crop_w real NOT NULL DEFAULT 1,
  crop_h real NOT NULL DEFAULT 1,
  created_at timestamptz NOT NULL DEFAULT now()
);

ALTER TABLE public.quiz_tiles ADD CONSTRAINT quiz_tiles_grid_check
  CHECK (grid_rows BETWEEN 2 AND 6 AND grid_cols BETWEEN 2 AND 6);
ALTER TABLE public.quiz_tiles ADD CONSTRAINT quiz_tiles_step_check
  CHECK (open_step BETWEEN 1 AND 4);
ALTER TABLE public.quiz_tiles ADD CONSTRAINT quiz_tiles_crop_check
  CHECK (crop_x >= 0 AND crop_y >= 0 AND crop_w > 0 AND crop_h > 0
         AND crop_x + crop_w <= 1.0001 AND crop_y + crop_h <= 1.0001);
```

ตารางนี้ **ไม่มี URL ของภาพจริง** โดยตั้งใจ — เหตุผลอยู่ข้อ 5

`cover_image_url` อยู่ที่นี่ได้เพราะภาพตอนปิดไม่ใช่เฉลย มันคือสิ่งที่ทุกคนควรเห็นอยู่แล้ว

### 4.3 ภาพจริงอยู่ที่ `quiz_keys`

```sql
ALTER TABLE public.quiz_keys
  ADD COLUMN IF NOT EXISTS tile_image_url text;
```

`quiz_keys` คือตารางที่ `anon` อ่านไม่ได้ ตั้งแต่ `20260819_game_hardening.sql` และเป็นที่อยู่ของ
`correct_text` / `answer_image_url` ของปริศนาใบ้คำอยู่แล้ว ไม่ต้องแก้ RLS ซ้ำ

### 4.4 RLS

```sql
ALTER TABLE public.quiz_tiles ENABLE ROW LEVEL SECURITY;
CREATE POLICY quiz_tiles_read ON public.quiz_tiles FOR SELECT USING (true);
-- เขียนผ่าน RPC ที่ตรวจ PIN เท่านั้น ตรงกับที่ทำกับ quiz_questions ไว้แล้ว
```

`quiz_puzzle_guesses` **คงสภาพเดิมทุกประการ: ไม่มี policy ใดๆ + REVOKE**
ถ้าเปิดให้อ่านผ่าน REST เมื่อไหร่ ลูกทัวร์จะเห็นคำที่คนอื่นเดาแล้วถูก = ได้เฉลยฟรี
Security Advisor เตือน `rls_enabled_no_policy` — ตั้งใจ เหมือนเดิม

---

## 5. ภาพคือเฉลย

นี่คือข้อที่พลาดง่ายที่สุดในเกมนี้ และพลาดแล้วไม่มีใครรู้ตัวจนกว่าจะมีลูกทัวร์คนหนึ่งเปิด Network tab

ถ้าเก็บ URL ภาพไว้ในตารางที่ `RLS read = true` (แบบ `quiz_puzzle_clues`) ทุกคนที่เข้าห้องได้
จะดึงภาพเต็มมาดูได้ตั้งแต่วินาทีแรก โดยไม่ต้องรอให้เปิดแผ่นป้ายสักแผ่น เกมจบตั้งแต่ยังไม่เริ่ม

**ทางออก: แยกของสาธารณะกับของลับออกจากกันตามความหมาย ไม่ใช่ตามความสะดวก**

| ข้อมูล | อยู่ที่ | ใครอ่านได้ |
|---|---|---|
| กริด · จำนวนแผ่น · หมายเลข · `open_step` | `quiz_tiles` | ทุกคน — ต้องเห็นกระดาน |
| ภาพตอนปิด (`cover_image_url`) | `quiz_tiles` | ทุกคน — เป็นสิ่งที่ตั้งใจให้เห็น |
| กรอบครอป | `quiz_tiles` | ทุกคน — ไม่มีความหมายถ้าไม่มีภาพ |
| **ภาพจริง (`tile_image_url`)** | **`quiz_keys`** | **จอเวทีและมือถือคนคุมเกม ผ่าน RPC ที่ตรวจ host token** |
| คำตอบ · alias | `quiz_keys` | เหมือนเดิม |

RPC `quiz_tiles_image(session_id, question_id, token)` เป็น `SECURITY DEFINER` คืน URL ให้เฉพาะ
คนที่ถือ token ของห้อง จอเวทีถือ token อยู่แล้ว (`stageUrl` พามาใน hash) และมือถือคนคุมเกมก็ถือ

### แล้วมือถือลูกทัวร์ล่ะ

**ไม่ได้ภาพ** — เห็นแค่กระดานแผ่นป้ายที่ปิดอยู่ (ภาพ cover + หมายเลข) กับช่องพิมพ์ตอบ
ภาพจริงดูจากจอใหญ่

นี่ไม่ใช่ข้อจำกัดที่คิดขึ้นใหม่ — `quizMedia.js` เขียนไว้ตั้งแต่ตอนทำควิซว่า "ตั้งใจไม่ส่งรูป
ให้มือถือเมื่อมีจอใหญ่" และ `Quiz.jsx` เช็ค `screen_mode !== 'none'` ทำแบบนี้อยู่แล้ว

ผลพลอยได้ที่สำคัญ: **ภาพเต็มเดินทางไปแค่จอเดียว ไม่ใช่ 40 จอ** ตัวเลข egress จึงเป็น
250 KB ต่อข้อ ไม่ใช่ 10 MB ต่อข้อ และนี่คือเหตุผลที่การเก็บกรอบครอปแบบไม่ทำลายต้นฉบับ (ข้อ 6)
แทบไม่มีต้นทุน

### กรณี `screen_mode = 'none'` (ไม่มีจอใหญ่)

ต้องส่งภาพให้มือถือ ไม่งั้นเล่นไม่ได้เลย → **ยอมรับความเสี่ยง แต่ต้องบอกให้รู้ตัว**
`TilesManager` ตอนเปิดห้องต้องขึ้นเตือนชัดๆ ว่า "ไม่มีจอใหญ่ = ลูกทัวร์ที่ตั้งใจแคะจะเห็นภาพเต็มได้"
ให้คนตัดสินใจเอง ดีกว่าปิดฟีเจอร์เงียบๆ หรือปล่อยรั่วเงียบๆ

ทางแก้ที่ถูกต้องจริง (ตัดภาพเป็น N ไฟล์ตอนอัปโหลด แล้วส่งเฉพาะแผ่นที่เปิด) อยู่ในข้อ 13
— ตั้งใจไม่ทำในเฟส 1

---

## 6. คณิตศาสตร์: ครอป + กริด

### 6.1 ไม่ตัดภาพจริง ตัดด้วย CSS

อัปโหลดไฟล์เดียว แล้วเรนเดอร์ `rows × cols` กล่อง แต่ละกล่องใช้ภาพเดียวกันเป็น background
โดยเลื่อนตำแหน่งให้เห็นคนละส่วน

ที่ไม่ทำ: ตัดภาพเป็น 12 ไฟล์แล้วอัปโหลด 12 ครั้ง เพราะจะได้ปัญหาสามอย่างฟรีๆ —
เปลี่ยนกริดทีหลังต้องอัปใหม่ทั้งชุด · storage บวมเป็น 12 object ต่อข้อ (Supabase free มี 1 GB) ·
และ Builder ต้องจัดการไฟล์กำพร้าตอนคนแก้กริด

### 6.2 กรอบครอปเก็บเป็นตัวเลข ไม่ทำลายต้นฉบับ

เก็บ `crop_x, crop_y, crop_w, crop_h` เป็นสัดส่วน 0.0-1.0 ของภาพที่อัปขึ้นไป แล้วให้
`TileBoard` เอาไปคิดรวมกับตำแหน่ง background ที่มันคำนวณอยู่แล้ว — **เป็นเลขชุดเดียวกัน
แทบไม่เพิ่มโค้ด**

ได้อะไร:

- **ปรับกรอบใหม่ได้ตลอด โดยไม่ต้องหาไฟล์ต้นฉบับในมือถืออีก** ← ข้อสำคัญที่สุด
  ถ้าครอปจริงตอนอัปโหลด วันหลังอยากขยับกรอบนิดเดียวต้องไปไล่หารูปในแกลเลอรีใหม่
  ซึ่งแปลว่าในทางปฏิบัติจะไม่มีใครแก้ ปล่อยกรอบเบี้ยวๆ ขึ้นจอไปเลย
- เปลี่ยนกริด 3×4 → 4×4 กรอบยังใช้ได้เหมือนเดิม
- ครอปได้แม้ภาพอัปไปแล้ว ไม่ต้องแนบใหม่

ต้นทุนตามปกติของวิธีนี้คือ "ทุกเครื่องต้องโหลดส่วนที่ครอปทิ้งไปด้วย" — **แต่เราตัดสินใจไปแล้ว
ในข้อ 5 ว่าภาพเต็มไปแค่จอเวทีเครื่องเดียว** ต้นทุนจึงเกือบศูนย์ ข้อเสียของวิธีนี้หายไปพอดี
เพราะการตัดสินใจก่อนหน้า

**ข้อควรระวัง:** `resizeImage()` ย่อภาพเหลือ 1600px ก่อนอัป ถ้าครอปเหลือเสี้ยวเดียว
ความละเอียดจะตกลงตามสัดส่วน สำหรับเกมนี้ให้ใช้ `TILE_IMAGE_WIDTH = 2000` แทน
`ANSWER_IMAGE_WIDTH` เดิม เพราะภาพขึ้นเต็มจอโปรเจกเตอร์และอาจถูกครอปอีกที

### 6.3 สูตร

ไฟล์ `src/lib/tileGrid.js` — ฟังก์ชันบริสุทธิ์ ไม่แตะ DOM มีเทสต์ที่
`src/lib/__tests__/tile-grid.test.mjs` (แบบเดียวกับ `puzzleLayout.js` และด้วยเหตุผลเดียวกัน:
พรีวิวใน Builder ต้องเรียกฟังก์ชันตัวเดียวกับจอจริง พรีวิวที่ไม่ตรงของจริงแย่กว่าไม่มีพรีวิว)

```
หมายเลขแผ่น   n = 1 .. rows*cols  เรียงซ้าย→ขวา บน→ล่าง (คงที่เสมอ)
แถว           r = floor((n-1) / cols)
คอลัมน์        c = (n-1) % cols

background-size     = (cols / crop_w) * 100%  ,  (rows / crop_h) * 100%
background-position = -(crop_x/crop_w + c/cols) * 100% ของความกว้างกล่อง  (แนวเดียวกันกับแนวตั้ง)
```

พูดเป็นภาษาคน: ขยายภาพให้กรอบครอปพอดีกับกระดาน แล้วเลื่อนให้แต่ละกล่องเห็นช่องของตัวเอง

```
สัดส่วนแผ่นป้าย = สัดส่วนภาพที่ครอปแล้ว × (rows ÷ cols)
```

สูตรบรรทัดนี้คือเหตุผลที่ **ไม่ล็อกอัตราส่วนตอนครอป** — ภาพ 16:9 กับกริด 4 คอลัมน์ 3 แถว
ได้แผ่นป้าย 4:3 ซึ่งสวยปกติ แผ่นป้ายไม่จำเป็นต้องเป็นจัตุรัส มันแค่ต้องไม่ยาวเป็นเส้น
ให้ Builder แสดงกระดานจริงบนภาพที่ครอปแล้วทันที คนสร้างข้อเห็นเองว่าโอเคไหม
ไม่ต้องมีกฎมาห้าม

### 6.4 การสุ่ม

**ตัดสินที่ RPC ฝั่ง server เท่านั้น** ห้าม `Math.random()` ที่ client แล้วค่อยเขียนกลับ
เหตุผล: กดรัวสองครั้งติดกัน หรือคนคุมเกมเปิดสองแท็บ จะได้ผลคนละอย่างแล้วทับกัน
ฝั่ง server เลือกจาก "แผ่นที่ยังไม่เปิด" ภายใต้ `FOR UPDATE` จึงไม่มีทางซ้ำและไม่มีทางแตกกัน

---

## 7. RPC

### 7.1 `quiz_tiles_open(session_id, token, tile_no)` → `quiz_sessions`

เปิดแผ่นที่ระบุ ถ้า `tile_no IS NULL` = สุ่มจากแผ่นที่ยังไม่เปิด

- ตรวจ `quiz_check_token`
- `SELECT ... FROM quiz_sessions WHERE id = ... FOR UPDATE`
- ถ้า `state NOT IN ('answering','locked')` → คืนแถวเดิม ไม่ทำอะไร
- เปิดทีละ `open_step` แผ่น (สุ่มไม่ซ้ำ) แต่ไม่เกินจำนวนที่เหลือ
- `revealed_tiles := revealed_tiles || แผ่นใหม่`
- แผ่นที่เปิดแล้วส่งซ้ำ = ไม่ทำอะไร ไม่ error (กดรัวเพราะคิดว่าเน็ตค้าง)

### 7.2 `quiz_tiles_undo(session_id, token)` → `quiz_sessions`

ตัดแผ่นที่เปิดล่าสุดออกจากท้าย array

**เงื่อนไข: ทำได้ภายใน 5 วินาทีหลังเปิดเท่านั้น** เกินกว่านั้นคนเห็นไปแล้ว ปิดกลับไม่มีความหมาย
และจะกลายเป็นเครื่องมือโกงจังหวะแทน (ต้องเก็บ `last_tile_at timestamptz` บน `quiz_sessions` ด้วย)

มีไว้เพราะนิ้วลั่นครั้งเดียวต่อหน้าคน 40 คนแล้วแก้ไม่ได้ = ข้อนั้นจบ

### 7.3 `quiz_tiles_guess(session_id, player_id, question_id, text)` → jsonb

ลอกด่านทั้งหมดจาก `quiz_puzzle_guess` มาตรงๆ แล้วเปลี่ยนสองจุด

**ด่านที่ต้องผ่าน เรียงจากถูกที่สุดก่อน (เหมือนเดิม):**

1. ข้อที่ส่งมาต้องเป็นข้อปัจจุบัน และอยู่ในช่วงเปิดรับ (+ผ่อนผัน 200ms)
2. ตอบถูกไปแล้ว → ไม่ต้องทำอะไร
3. rate limit 400ms
4. ข้อความซ้ำกับครั้งก่อน → ไม่นับ ไม่บันทึก **ต้องคงไว้** ไม่งั้นนิ้วลั่นทีเดียวเสียโควตา
5. โควตาของชุด (`attempt_limit`) และเพดานนิรภัย 40 ครั้ง

**จุดที่ต่างจากปริศนาใบ้คำ — จุดที่ 1: ต้องล็อกแถวห้อง**

```sql
SELECT * INTO v_s FROM public.quiz_sessions WHERE id = p_session_id FOR UPDATE;
```

`quiz_puzzle_guess` เดิม **ไม่ล็อก** เพราะปริศนาใบ้คำไม่ต้อง — ทุกคนตอบถูกได้คนละ 1 คะแนน
ไม่มีอะไรต้องแย่ง เกมนี้มีของชิ้นเดียวให้แย่ง ถ้าไม่ล็อก สองคนที่กดส่งห่างกัน 5 มิลลิวินาที
จะได้คะแนนทั้งคู่และสั่งจบข้อซ้อนกัน

ในธุรกรรมเดียวกัน เมื่อตอบถูกและยังไม่มีใครถูกมาก่อน:

- บวก 1 คะแนน (`points_per_correct`) — บวกทันที ไม่พักไว้ถึง `quiz_reveal`
- `revealed_tiles := ทุกแผ่น`
- `state = 'locked'`, `locked_at = now()`
- บันทึกแถวสรุปลง `quiz_answers` (แถวเดียวต่อคนต่อข้อ — กระดานอันดับ โหมดทีม รายงาน ทำงานต่อได้)

คนที่สองจะรอคิวแล้วเห็นว่ามีคนได้ไปแล้ว → คืน `status = 'already_solved'` ไม่ให้คะแนน

**จุดที่ต่าง — จุดที่ 2: คำนวณ `closeness` ตอนบันทึก**

```
closeness = 100 - round(dist / max(len(a), len(b)) * 100)
```

เทียบกับคำตอบและ alias **ทุกตัว เอาค่าดีที่สุด** และเทียบบนรูป `quiz_loose_answer`
(ตัดวรรณยุกต์และการันต์แล้ว) ไม่งั้น "บุรีรัม" กับ "บุรีรัมย์" จะได้คะแนนต่ำทั้งที่คือคนที่รู้คำตอบ

**คำนวณตอนบันทึก ไม่ใช่ตอนอ่าน** เพราะ RPC เปิด `quiz_keys` อ่านเฉลยอยู่แล้วเพื่อตัดสินถูก/ผิด
คำนวณตรงนั้นทีเดียวจบ ถ้าไปคำนวณตอนคนคุมเกม poll ทุก 1.5 วินาที = ยิง `quiz_edit_distance`
ใส่คำตอบเดิมซ้ำๆ ทุกรอบโดยเปล่าประโยชน์

**คืนค่า** `{ status, correct, attempts_used, attempts_left }`
`attempts_left = null` แปลว่าไม่จำกัด · `status`: `ok | already | already_solved | duplicate |
too_fast | closed | not_started | wrong_question | no_attempts | empty | too_long | no_session`

**ไม่คืน `closeness` กลับไปให้ผู้เล่นเด็ดขาด** (กติกาข้อ 3.6)

### 7.4 `quiz_tiles_feed(session_id, question_id, token, after_id, limit)` → setof

คำตอบทุกคำเรียงตามเวลา — `ผู้เล่น · คำที่พิมพ์ · closeness · ถูกไหม · เวลาที่ส่ง`

- `SECURITY DEFINER` + ตรวจ token — **ห้ามเปิด RLS ให้ `quiz_puzzle_guesses` เพื่อทำสิ่งนี้**
- `after_id` เป็น cursor ดึงเฉพาะของใหม่ ไม่ลากทั้งกองทุก 1.5 วินาที
  40 คน × โควตา 5 ครั้ง = 200 แถวต่อข้อ ถ้าดึงทั้งหมดทุกรอบคือ 8,000 แถวต่อนาที
- `limit` ตั้งต้น 40

**เป็นของกลางสองเกม** — `quiz_puzzle_guesses` ใช้ร่วมกัน ทำ feed ที่นี่ทีเดียว
ปริศนาใบ้คำได้ใช้ด้วยฟรี แทนที่ `quiz_puzzle_near_misses` ที่รวบคำซ้ำเป็นก้อนและไม่มีชื่อคน

> `quiz_puzzle_near_misses` **ไม่ต้องลบ** ปริศนาใบ้คำยังใช้อยู่ ให้ feed มาเป็นทางเลือกเพิ่ม
> แล้วค่อยดูทีหลังว่าจะยุบรวมไหม การลบของที่ผ่านสนามจริงมาแล้วเพื่อความสวยงามไม่คุ้ม

### 7.5 `quiz_tiles_stats(session_id, question_id, token, online_window_sec)` → record

```
solved · guesses · online · total · still_in
```

`still_in` = **จำนวนคนที่ยังตอบได้อยู่** (ยังไม่หมดโควตาและยังไม่ตอบถูก) — ตัวใหม่ที่ปริศนาใบ้คำไม่มี

จำเป็นเพราะกติกาข้อ 3.4 ทำให้ "คนหมดสิทธิ์" เป็นเรื่องปกติ ไม่ใช่เคสขอบ ให้โควตา 3 ครั้งกับ
"ผลไม้อะไร" คือคนครึ่งห้องหมดสิทธิ์ตั้งแต่เปิดไป 4 แผ่น **ถ้าเหลือคนตอบได้ 3 จาก 40
การเปิดแผ่นต่อไปไม่มีความหมายแล้ว** — นี่คือตัวเลขที่บอกว่าควรเปิดต่อหรือเฉลยเลย

### 7.6 `quiz_tiles_image(session_id, question_id, token)` → text

คืน `quiz_keys.tile_image_url` ให้เฉพาะคนที่ถือ token ของห้อง (ดูข้อ 5)

ใช้ทั้งจอเวทีและมือถือคนคุมเกม — ดูข้อ 11.1 ว่าทำไมมือถือคนคุมเกมต้องได้ด้วย

### 7.7 `quiz_tiles_upsert(...)` และ `quiz_tiles_set_for_edit(set_id, staff_id, pin)`

ลอกโครงจาก `quiz_puzzle_upsert` / `quiz_puzzle_set_for_edit` ตรงๆ เพราะติดกำแพงเดียวกัน:
เฉลยอยู่ใน `quiz_keys` ซึ่ง `anon` อ่านไม่ได้ จึงต้องเข้าทาง `SECURITY DEFINER` + ตรวจ PIN ทีมงาน

พารามิเตอร์: `set_id, question_id, text, time_limit_sec, grid_rows, grid_cols, open_step,
cover_image_url, crop_*, tile_image_url, answer, aliases, explain, sort_order`

### 7.8 แก้ของเดิม — อย่าลืมสี่ที่นี้

| ฟังก์ชัน | ต้องเพิ่มอะไร | ถ้าลืมจะเป็นยังไง |
|---|---|---|
| `quiz_next` | `revealed_tiles = '{}'`, `last_tile_at = NULL` | แผ่นที่เปิดของข้อเก่าค้างบนข้อใหม่ = เฉลยฟรี (บั๊กเดียวกับที่เคยเจอกับ `hint_payload`) |
| `quiz_lock` | สาย `kind = 'tiles'` → `quiz_tiles_close()` | ไม่มีแถวสรุป `timeout` ใน `quiz_answers` รายงานเพี้ยน |
| `quiz_reveal` | สาย `'tiles'` **ห้ามคิดคะแนน** | บวกคะแนนซ้ำทั้งห้อง — คะแนนลงไปแล้วตอนตอบถูก |
| `quiz_clone_set` | ก็อป `quiz_tiles` + `crop_*` + `tile_image_url` | สตาฟก็อปชุดภาพสวยๆ ไปใช้กรุ๊ปหน้าแล้วได้ชุดเปล่า ไปรู้ตัวตอนขึ้นรถ |

---

## 8. เรื่องเวลา — เกมนี้ไม่นับถอยหลัง

ควิซกับปริศนาใบ้คำจับเวลาต่อข้อ เกมนี้คนคุมเกมกำหนดจังหวะเอง ข้อจบเมื่อมีคนตอบถูก
หรือเมื่อคนคุมเกมกดปิดรับ ไม่ใช่เมื่อเวลาหมด

แต่ `quiz_next` เดิมคำนวณแบบนี้:

```sql
question_ends_at = now() + lead + make_interval(secs => v_q.time_limit_sec)
```

ถ้า `time_limit_sec` เป็น NULL จะได้ `question_ends_at` เป็น NULL แล้ว `derivePhase()` ฝั่งหน้าจอ
จะคิดเฟสไม่ออก

**ทางเลือกที่เลือก: ตั้ง `time_limit_sec = 300` (5 นาที) สำหรับข้อของเกมนี้ แล้วซ่อนแถบนับถอยหลังบนจอ**

> แก้จาก 600 ที่เขียนไว้ตอนร่าง — `quiz_questions_time_check` ของจริงบังคับ 5-300 วินาที
> จะใช้ 600 ต้องไปแก้ constraint ที่ควิซและปริศนาใบ้คำใช้อยู่ด้วย ซึ่งไม่คุ้ม

ไม่แก้ `quiz_next` เพราะมันเป็นทางผ่านของควิซและปริศนาใบ้คำด้วย การไปยุ่งกับเงื่อนไข NULL
ตรงนั้นแลกความเสี่ยงกับเกมที่ใช้งานจริงอยู่แล้วสองเกม เพื่อประหยัดโค้ดไม่กี่บรรทัดในเกมที่สาม

ผลข้างเคียงที่ยอมรับ: ถ้าคนคุมเกมค้างข้อไว้เกิน 5 นาทีจริงๆ ข้อจะปิดรับเอง
ซึ่งในทางปฏิบัติแปลว่าเขาเดินไปทำอย่างอื่นแล้ว — พฤติกรรมนี้ถือว่าถูกต้องกว่าปล่อยค้างตลอดกาล

หน้าจอทุกหน้าของเกมนี้ **ไม่แสดงแถบเวลาและไม่แสดงตัวเลขนับถอยหลัง** สิ่งที่แสดงแทนคือ
จำนวนแผ่นที่เปิดไปแล้ว (`7 / 12`) ซึ่งคือ "ความคืบหน้า" ที่มีความหมายจริงของเกมนี้

---

## 9. ImageCropper — ครอปภาพก่อนยืนยัน

### 9.1 ครึ่งหลังมีอยู่แล้ว

`resizeImage()` ใน `quizMedia.js` เรียก `canvas.drawImage(bitmap, 0, 0, w, h)` อยู่แล้ว
การครอปคือฟังก์ชันเดิมในรูปแบบ 9 อาร์กิวเมนต์:

```js
ctx.drawImage(bitmap, sx, sy, sw, sh, 0, 0, dw, dh)
//                    └── กรอบที่เลือก ──┘
```

แก้ราว 5 บรรทัด แล้วรับ `opts.crop` เข้ามาที่ `uploadQuizMedia()` — ท่อส่งไฟล์ ย่อภาพ
ตรวจขนาด ตั้งชื่อสุ่ม ลบไฟล์เก่า มีครบแล้วทั้งหมด

### 9.2 UI: ครอปอิสระ + preset 1:1 / 4:3 / 16:9

แบบเดียวกับแอปแต่งรูปในมือถือ — กรอบลากได้ มีมือจับมุม สลับ preset ได้ หมุนภาพได้

**ใช้ `cropperjs` (ผ่าน `react-cropper`)** เพราะมันคือโมเดลนั้นเป๊ะ
(`react-easy-crop` ทำไม่ได้ — เป็นกรอบตายที่เลื่อนภาพข้างใต้ ไม่มีมือจับมุม)

เขียนเองได้ไหม — ได้ แต่มือจับ 8 มุม + จำกัดขอบเขต + pinch-zoom + สลับ preset แล้วกรอบ
ต้องปรับตัวโดยไม่กระโดด คือ 2-3 วันเป็นอย่างน้อย ไม่คุ้ม

**ปุ่มหมุนภาพเป็นของแถมที่จำเป็นกว่าที่คิด** — รูปถ่ายจากมือถือที่ EXIF เพี้ยนแล้วมาตะแคง
ถ้าไม่มีปุ่มหมุนคือใช้รูปนั้นไม่ได้เลย ต้องไปแก้ในแอปอื่นก่อน ซึ่งจะไม่มีใครทำ

### 9.3 ⚠️ กับดัก: PWA precache

โปรเจกต์นี้ยังไม่มี `React.lazy` เลยสักที่ — ทุกอย่างอยู่ใน bundle ก้อนเดียว และ `vite.config.js`
ตั้งไว้ว่า:

```js
workbox: { globPatterns: ['**/*.{js,css,html,png,svg,ico,woff2}'] }
```

**service worker จะ precache ไฟล์ js ทุกก้อนที่ build ออกมา รวมถึง chunk ที่ lazy ด้วย**
ถ้าใส่ `cropperjs` เฉยๆ ลูกทัวร์ 40 คนบนรถจะโหลดไลบรารีแต่งรูปที่ไม่มีวันได้ใช้
ตั้งแต่เปิดแอปครั้งแรก บน 4G เสาเดียวกัน — และจะไม่มีใครรู้ตัว เพราะแอปทำงานปกติทุกอย่าง
แค่ทุกคนโหลดหนักขึ้นเงียบๆ

ต้องทำสามอย่างพร้อมกัน ไม่ใช่อย่างใดอย่างหนึ่ง:

1. `React.lazy` + `Suspense` ครอบ `ImageCropper` — **ตัวแรกของโปรเจกต์ ตั้งแบบแผนไว้ให้ของอื่นตามทีหลัง**
2. ตั้งชื่อ chunk ผ่าน `build.rollupOptions.output.manualChunks` ใน `vite.config.js`
3. **`workbox.globIgnores` กัน chunk นั้นออกจาก precache**

ผลลัพธ์: คนที่โหลดไลบรารีนี้คือสตาฟที่กดเข้า Builder เท่านั้น ซึ่งทำก่อนออกทริปบนไวไฟ

### 9.4 คืนสองอย่าง

`ImageCropper` คืน **ทั้งกรอบ (`{x,y,w,h}` 0-1) และไฟล์ที่ครอปแล้ว**

- เกมเปิดแผ่นป้าย ใช้ **กรอบ** (ไม่ทำลายต้นฉบับ — ข้อ 6.2)
- ที่อื่นที่ไม่มี `TileBoard` มาคิดเลขให้ (รูปควิซ รูปใบ้ปริศนา ภาพเฉลย โลโก้บริษัท) ใช้ **ไฟล์ที่ครอปจริง**

วางเป็น `src/components/common/ImageCropper.jsx` ตัวกลาง ได้ใช้กับรูปทุกที่ในแอป
ไม่ใช่แค่เกมนี้เกมเดียว

---

## 10. หน้าจอ

### 10.1 `TilesManager` — `/staff/tiles`

ลอกจาก `PuzzleManager` ตรงๆ **ทุก query ต้องกรอง `game_kind = 'tiles'`** เพราะใช้ตารางร่วมกับ
ควิซและปริศนาใบ้คำ ลืมกรองเมื่อไหร่ = ชุดของเกมอื่นโผล่มาปนทันที

ตอนเปิดห้อง: ถ้าเลือก `screen_mode = 'none'` ต้องขึ้นเตือนเรื่องภาพรั่ว (ข้อ 5)

### 10.2 `TilesBuilder` — `/staff/tiles/builder/:setId`

ลำดับการกรอกที่ตั้งใจ:

1. แนบภาพ → เปิด `ImageCropper` ทันที (ครอปอิสระ / preset / หมุน)
2. เลือกกริด `rows × cols` และ `open_step`
3. **พรีวิวกระดานจริง** — `TileBoard` ตัวเดียวกับจอเวที บนภาพที่ครอปแล้ว
   สลับดูได้ระหว่าง "ตอนปิด" กับ "ตอนเปิดหมด"
4. ภาพตอนปิด (ไม่ใส่ = ช่องสีตามธีม)
5. คำตอบ + alias
6. **ช่องทดสอบคำตอบ** — ใช้ `tryAnswer()` ที่มีอยู่แล้ว

ข้อ 6 ไม่ใช่ของประดับ ดูข้อ 14.2

ตั้งค่าระดับชุด (`answer_mode`, `attempt_limit`) อยู่ที่หัวหน้าจอ ไม่ใช่รายข้อ

### 10.3 `TilesHost` — `/staff/tiles/host/:sessionId`

มือถือคนคุมเกม จอสั่งงานอย่างเดียว วางจากบนลงล่าง:

- **กระดานแผ่นป้ายบนภาพจริงแบบหรี่แสง** (ข้อ 11.1) — นิ้วต้องถึง อยู่บนสุด
  แตะแผ่นไหน = เปิดแผ่นนั้น
- ปุ่ม `สุ่มเปิด` · `ยกเลิกแผ่นล่าสุด` (โผล่ 5 วินาที) · `ปิดรับ` · `เฉลย` · `ข้อถัดไป`
- ตัวเลข: `เปิดแล้ว 7/12` · `ยังตอบได้ 14 คน` · `เดามาแล้ว 38 ครั้ง` · `ออนไลน์ 37/40`
- **feed คำตอบ** พับเก็บได้ แต่ละแถว: เวลา · ชื่อ · คำที่พิมพ์ · แถบความใกล้เคียง · ปุ่ม `รับเป็นคำตอบถูก`
  สลับเรียงได้ระหว่าง **ตามเวลา** (ปกติ) กับ **ตามความใกล้เคียง** (ตอนห้องตัน)

**แถบความใกล้เคียงแสดงเฉพาะเมื่อ `closeness >= 60`** ต่ำกว่านั้นแสดงคำเปล่าๆ

เหตุผล: คำตอบของเกมนี้มักสั้นและแคบ ("ส้ม" กับ "มะม่วง" ห่างกัน 100% ทั้งที่ทั้งคู่คือคนเดามั่ว)
ต่างจาก "บุรีรัม" กับ "บุรีรัมย์" ที่ 92% แล้วมีความหมายจริง ถ้าโชว์ทุกแถวคนคุมเกมจะเห็น
แถบ 8% เต็มจอจนเลิกมอง **แถบที่โผล่มาต้องแปลว่า "คนนี้น่าสนใจ" ทุกครั้งที่เห็น ไม่ใช่ของประดับ**

feed ใช้ **poll ทุก 1.5 วินาที ไม่ใช่ realtime** — 40 คนเดารัวๆ = event ท่วมทุกเครื่องในห้อง
(กติกาเดิมของ `useQuizSession`: ห้าม subscribe `quiz_players` / `quiz_answers`)

พอข้อจบแล้ว feed ต้องค้างไว้อ่านได้ — เป็นหลักฐานตอนมีคนเถียงว่า "ผมตอบก่อนนี่"

### 10.4 `TilesStage` — `/staff/tiles/stage/:sessionId`

จอโปรเจกเตอร์หรือทีวีบนรถ **ไม่มี guard เหมือน `QuizStage`/`PuzzleStage`** เพราะถ้า guard ไว้
จะโดน redirect แล้ว hash ที่พา token มาจะหายไปด้วย

- กระดานแผ่นป้ายเต็มจอ + หมายเลขตัวใหญ่ (เช็ค `screen_mode === 'bus_tv'` แล้วขยายอีกขั้น)
- แผ่นที่เพิ่งเปิด: อนิเมชันพลิก คำนวณจาก diff ของ `revealed_tiles` เทียบกับค่าก่อนหน้า
- ตอนมีคนตอบถูก: เปิดทุกแผ่นแบบไล่คลื่นราว 1.2 วินาที **ไม่ใช่เปิดพรวดพร้อมกัน**
  แล้วขึ้นชื่อผู้ชนะ — ยืมกลไกจาก `DrawReveal.jsx` / `drawStage.js` / `winAlert.js` / `drawSound.js`
  ที่ทำไว้ให้ลัคกี้ดรอว์ นี่คือจังหวะพีคของข้อ อย่าทิ้ง
- **โหลดภาพข้อถัดไปไว้ล่วงหน้า** (ข้อ 11.2)

### 10.5 `Tiles` (ลูกทัวร์) — `/t/:code/tiles`

- กระดานแผ่นป้ายที่ปิดอยู่ (ภาพ cover + หมายเลข) — **ไม่มีภาพจริง** เมื่อมีจอใหญ่
- ช่องพิมพ์ตอบ + `เหลืออีก N ครั้ง` (ซ่อนถ้า `attempt_limit` เป็น NULL)
- ตอบแล้วขึ้น **ถูก / ผิด ทันที** จากค่าที่ RPC คืนมา ไม่ต้องรอ realtime
- **`answer_mode = 'none'` → ไม่มีช่องพิมพ์เลย** เหลือแค่กระดาน

**หน้าจอตอนหมดโควตา** ต้องไม่ใช่ช่องพิมพ์สีเทาพร้อมข้อความว่าคุณหมดสิทธิ์
ยังต้องเห็นกระดานเปิดต่อไปเรื่อยๆ เพราะเขายังดูเกมอยู่ แค่ตอบไม่ได้ —
กติกาข้อ 3.4 ทำให้สภาพนี้เป็นเรื่องปกติของทุกข้อ ไม่ใช่เคสขอบ

---

## 11. สี่อย่างที่เพิ่มเข้ามาจากการวิเคราะห์

### 11.1 คนคุมเกมต้องเห็นภาพเต็มบนมือถือตัวเอง

**สำคัญที่สุดในสี่ข้อ และเกือบไม่มีต้นทุน**

ถ้าคนคุมเกมเห็นแค่กระดานที่ปิดอยู่เหมือนลูกทัวร์ เขาก็เป็นแค่คนกดปุ่มสุ่ม แล้วเกมจะพัง
ด้วยอุบัติเหตุ: บังเอิญเปิดแผ่นที่มีใบหน้าอยู่พอดีตั้งแต่แผ่นที่สอง จบเกมทันที ไม่มีใครสนุก

ให้มือถือคนคุมเกมแสดง **ภาพเต็มแบบหรี่แสง มีตารางแผ่นป้ายทับอยู่ข้างบน** เขาจะเลือกได้ว่า
เปิดมุมที่เป็นท้องฟ้าก่อน เก็บใบหน้าไว้ท้ายสุด — เปลี่ยนจากสุ่มดวงเป็นการคุมจังหวะจริง

ต้นทุนเกือบศูนย์เพราะข้อ 5 วางไว้แล้วว่าภาพอยู่ใน `quiz_keys` แล้วส่งผ่าน RPC ที่ตรวจ token
มือถือคนคุมเกมถือ token ตัวเดียวกับจอเวที เรียก `quiz_tiles_image` ตัวเดิมได้เลย

### 11.2 โหลดภาพข้อถัดไปไว้ล่วงหน้า

เกมนี้ทั้งเกมแขวนอยู่กับภาพเดียว ถ้าจอเวทีขึ้นวงกลมหมุนตอนกดเปิดข้อ จังหวะพังทันที
และรถบัส 40 คนแชร์เสา 4G เดียวกันคือสภาพปกติ ไม่ใช่กรณีเลวร้าย

ตอนอยู่ข้อ N ให้จอเวทียิง `new Image().src` ของข้อ N+1 ทิ้งไว้เงียบๆ พอกดข้อถัดไป
ภาพอยู่ในแคชแล้ว — ไม่กี่บรรทัด แต่เป็นตัวตัดสินว่าเกมรู้สึกลื่นหรือกระตุก
ซึ่งคนดูรู้สึกได้แต่บอกไม่ถูกว่าเพราะอะไร

`quizMedia.js` เขียนกังวลเรื่องนี้ไว้แล้วตอนทำควิซ ("จอบนรถโหลดทัน คำถามขึ้นพร้อมนาฬิกา")
เกมนี้หนักกว่าเพราะภาพใหญ่กว่าและไม่มีข้อความคำถามมากลบเวลาโหลดให้

> ต้องดึงผ่าน `quiz_tiles_image` ของข้อถัดไป ซึ่งแปลว่าจอเวทีรู้เฉลยข้อถัดไปล่วงหน้า
> — ยอมรับได้ เพราะจอเวทีเป็นเครื่องของทีมงานและถือ token อยู่แล้ว
> **แต่ห้ามเรนเดอร์ลง DOM** เก็บไว้ในแคชเบราว์เซอร์เฉยๆ

### 11.3 โหมดไม่ต้องตอบ (`answer_mode = 'none'`)

เพิ่มค่าเดียวในคอลัมน์ที่จะมีอยู่แล้ว ไม่มีช่องพิมพ์ ไม่มีคะแนน คนคุมเกมเปิดแผ่นป้ายไปเรื่อยๆ
ให้คนบนรถทายกันปากเปล่า

คุ้มทั้งที่แทบไม่ต้องเขียนอะไร เพราะเปิดการใช้งานที่ไม่เกี่ยวกับเกมเลย:

- เฉลยว่าพรุ่งนี้จะพาไปไหน
- เปิดภาพผู้ชนะกิจกรรมอื่น
- ช่วงฆ่าเวลาบนรถที่ไม่อยากให้ทุกคนหยิบมือถือขึ้นมา

บริษัททัวร์ได้ของสามอย่างจากโค้ดชิ้นเดียว

### 11.4 ปุ่มยกเลิกแผ่นล่าสุด

ดูข้อ 7.2 นิ้วลั่นครั้งเดียวต่อหน้าคน 40 คนแล้วแก้ไม่ได้ = ข้อนั้นจบ

---

## 12. ลำดับงาน

### เฟส 1 — เล่นได้จริง

1. ไมเกรชัน `2026xxxx_tile_reveal.sql` (ข้อ 4 + 7) — รันผ่าน Supabase MCP `apply_migration`
   **แบ่งเป็นก้อนตามหัวข้อ** error จะชี้จุดได้ตรง
2. `src/lib/tileGrid.js` + `src/lib/__tests__/tile-grid.test.mjs` — เขียนเทสต์ก่อนต่อ UI
   เพิ่มบรรทัดใน `package.json` script `test`
3. `src/lib/tileHost.js` — คำสั่งทั้งหมด รวมที่เดียวเพื่อไม่ให้ลืมแนบ token (แบบ `puzzleHost.js`)
4. `src/components/tiles/TileBoard.jsx` — ตัวเดียว ใช้ทั้งจอเวที มือถือ host มือถือลูกทัวร์ และพรีวิว Builder
5. `useQuizSession.js` — เติม `revealed_tiles`, `last_tile_at` ใน `SESSION_COLS`
6. `permissions.js` — `tiles.host` (staff) · `tiles.edit` (lead) · `tiles.define` (admin)
7. หน้าจอ 5 หน้า (ข้อ 10) + routes ใน `App.jsx`
8. `staff/Games.jsx` + `guest/Games.jsx` — กรอง `game_kind` เพิ่มสาย `'tiles'`
9. `locales/th.json` · `en.json` · `zh.json`
10. `ImageCropper` + lazy + `manualChunks` + `globIgnores` (ข้อ 9)
11. ผูก `answer_mode = 'none'` (ข้อ 11.3)

### เฟส 2

- `answer_mode = 'buzz'` — ตารางคิวขอตอบ + จอคนคุมเกมที่เห็นชื่อเรียงตามเวลาแล้วกดถูก/ผิด
  ใช้ poll ไม่ใช่ realtime ด้วยเหตุผลเดียวกับ feed
- ยุบ `quiz_puzzle_near_misses` เข้า `quiz_tiles_feed` ถ้าใช้จริงแล้วพบว่าซ้ำซ้อน
- ตัดภาพเป็น N ไฟล์ฝั่ง server สำหรับกรณี `screen_mode = 'none'` (ข้อ 13)

---

## 13. สิ่งที่ตั้งใจไม่ทำ

| ไม่ทำ | เหตุผล |
|---|---|
| คะแนนผูกกับจำนวนแผ่นที่เปิด | เจ้าของโปรเจกต์ตัดสินว่าไม่ผูก · ขัดกับกติกา 1 คะแนนเท่ากันที่ยืนยันไว้ในปริศนาใบ้คำ · กระดานอันดับรวมทุกเกมยังเทียบกันได้ |
| บันทึกสถิติ "เปิดกี่แผ่นถึงมีคนตอบถูก" | เจ้าของโปรเจกต์ตัดสินว่าซีเรียสเกินไปสำหรับบรรยากาศบนรถ |
| ตัดภาพเป็น N ไฟล์ตอนอัปโหลด | ปลอดภัยกว่าจริง แต่ storage บวม 12 object ต่อข้อ และเปลี่ยนกริดทีหลังต้องตัดใหม่ · ข้อ 5 แก้ปัญหาได้แล้ว 95% ของกรณีใช้งาน |
| ล็อกอัตราส่วนครอปตามกริด | แผ่นป้ายไม่จำเป็นต้องเป็นจัตุรัส · พรีวิวสดใน Builder ทำหน้าที่นี้ได้ดีกว่ากฎ (ข้อ 6.3) |
| ดึงรูปโปรไฟล์ลูกทัวร์มาทำข้อ "ทายว่าใคร" อัตโนมัติ | ระบบยังไม่มีรูปโปรไฟล์ลูกทัวร์ · และการเอารูปคนขึ้นจอโดยเขาไม่ได้ตั้งใจให้ใช้ ควรให้สตาฟเลือกใส่เองทีละรูป |
| ตารางห้อง/ผู้เล่น/คะแนนชุดใหม่ | เครื่องยนต์ควิซทำได้หมดแล้ว · แยกเมื่อไหร่ = กระดานอันดับรวมทุกเกมพังทันที |

---

## 14. กับดักที่ต้องไม่เหยียบซ้ำ

### 14.1 บทเรียนที่ยกมาจากปริศนาใบ้คำ (ยังใช้ได้ทุกข้อ)

- **`SESSION_COLS` ใน `useQuizSession.js` เป็นรายชื่อคอลัมน์แบบระบุเอง ไม่ใช่ `select('*')`**
  คอลัมน์ใหม่ต้องเติมที่นั่น ไม่งั้น realtime กับ polling ให้ค่าไม่ตรงกัน
  อาการ: ของที่เพิ่งอัปเดตขึ้นแล้วหายทุก 4 วินาที
- **`[[:punct:]]` ของ Postgres กินทัณฑฆาต (์)** — อย่าใช้คลาสอักขระ POSIX กับภาษาไทย
  ถ้ายังไม่ได้ทดสอบด้วยคำจริง (เกมนี้ไม่ควรต้องแตะ normalize เลย แต่ถ้าจะแตะ อ่านข้อนี้ก่อน)
- **ห้ามพึ่ง extension นอก core ใน RPC** — บน Supabase ส่วนขยายอยู่ schema `extensions`
  ฟังก์ชันที่ตั้ง `SET search_path = public` จะมองไม่เห็น แล้วพังตอนรันจริง
  (บทเรียน `gen_random_bytes` และ `levenshtein`)
- **`Could not find the 'X' column ... in the schema cache`** = ข้อความของ PostgREST ไม่ใช่ Postgres
  แปลว่า migration ยังไม่ได้รัน หรือรันแล้วแคชค้าง → `NOTIFY pgrst, 'reload schema';`
- **`npm run lint` ของ repo นี้ใช้ไม่ได้อยู่แล้ว** (ไม่มีไฟล์ตั้งค่า ESLint) ไม่ใช่ของที่พังเพราะงานใหม่
- **`node_modules` ในโฟลเดอร์เป็นของ Windows** — build ใน Linux VM ไม่ได้ ต้อง build บนเครื่องผู้ใช้

### 14.2 กับดักใหม่ของเกมนี้

**ก. การแข่งกันที่ไม่ได้ล็อก**
`quiz_puzzle_guess` เดิมไม่ล็อกแถวห้องเพราะไม่ต้อง เกมนี้ต้อง (ข้อ 7.3)
บั๊กนี้จะไม่โผล่ตอนทดสอบคนเดียว และจะโผล่วันที่มีคน 40 คนกดพร้อมกัน

**ข. "เกือบถูก" กลายเป็นเรื่องความยุติธรรม ไม่ใช่แค่ความสะดวก**

ในปริศนาใบ้คำ ถ้า A พิมพ์ "กะละแม" แล้วระบบไม่รับ คนคุมเกมกดรับให้ทีหลังได้
ทุกคนได้ 1 คะแนนเหมือนกัน ไม่มีใครเสียหาย

เกมนี้ถ้า A พิมพ์ "กะละแม" ตอน 0:10 แล้ว B พิมพ์ตรงเป๊ะตอน 0:15 →
**B ชนะ ข้อจบ คนคุมเกมไม่มีวันได้เห็น feed ด้วยซ้ำ** ทั้งที่ A รู้คำตอบก่อน

**แก้ที่ต้นทาง ไม่ใช่ปลายทาง:** `TilesBuilder` ต้องตื๊อให้ใส่ alias ก่อนเซฟ และบังคับให้กด
ทดสอบคำตอบ (`tryAnswer()`) อย่างน้อยครั้งหนึ่งก่อนข้อขึ้นเวที
ถ้าปล่อยให้ข้อที่มี alias ตัวเดียวขึ้นเวที = เตรียมทะเลาะกันหน้ารถบัส

**ค. ลืมกรอง `game_kind`**
`quiz_sets` / `quiz_sessions` ใช้ร่วมกันสามเกมแล้ว ทุก query ที่แตะสองตารางนี้ต้องกรอง
ลืมที่เดียว = ชุดของเกมอื่นโผล่มาปน และจะดูเหมือนข้อมูลเสียมากกว่าดูเหมือนบั๊ก

**ง. ลืม `globIgnores` ตอนใส่ cropper**
ข้อ 9.3 — แอปทำงานปกติทุกอย่าง แค่ทุกคนโหลดหนักขึ้นเงียบๆ ไม่มีอะไรฟ้อง

**จ. ลืมต่อ `quiz_clone_set`**
ข้อ 7.8 — สตาฟก็อปชุดไปใช้กรุ๊ปหน้าแล้วได้ชุดเปล่า ไปรู้ตัวตอนขึ้นรถ

---

## 15. รายการไฟล์ที่แตะ

**เพิ่มใหม่**

```
supabase/migrations/2026xxxx_tile_reveal.sql
src/lib/tileGrid.js
src/lib/tileHost.js
src/lib/__tests__/tile-grid.test.mjs
src/components/tiles/TileBoard.jsx
src/components/common/ImageCropper.jsx
src/pages/staff/TilesManager.jsx
src/pages/staff/TilesBuilder.jsx
src/pages/staff/TilesHost.jsx
src/pages/staff/TilesStage.jsx
src/pages/guest/Tiles.jsx
```

**แก้ของเดิม**

```
src/App.jsx                 routes ทั้งฝั่งสตาฟและลูกทัวร์
src/lib/useQuizSession.js   SESSION_COLS += revealed_tiles, last_tile_at
src/lib/permissions.js      tiles.host / tiles.edit / tiles.define
src/lib/quizMedia.js        opts.crop ใน uploadQuizMedia + TILE_IMAGE_WIDTH
src/pages/staff/Games.jsx    กรอง game_kind เพิ่มสาย tiles
src/pages/guest/Games.jsx    กรอง game_kind เพิ่มสาย tiles
src/locales/{th,en,zh}.json
vite.config.js              manualChunks + workbox.globIgnores
package.json                react-cropper + บรรทัดเทสต์ใหม่ใน script test
```

**ของเดิมใน DB ที่ต้องแก้** — `quiz_next` · `quiz_lock` · `quiz_reveal` · `quiz_clone_set` (ข้อ 7.8)
