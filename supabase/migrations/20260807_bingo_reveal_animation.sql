-- =====================================================================
-- Bingo — สวิตช์เอฟเฟกต์สุ่มเลข (7 ส.ค. 2026)
-- =====================================================================
-- เก็บเป็นค่าระดับ "ห้องเกม" ไม่ใช่ค่าในเครื่อง staff เพราะต้องการให้
-- มือถือลูกทัวร์ทุกเครื่องหมุนเลขลุ้นพร้อมกัน — staff กดสวิตช์ครั้งเดียว
-- แล้วมีผลทุกเครื่องผ่าน realtime ที่ subscribe bingo_games อยู่แล้ว
--
-- default = true เพราะห้องที่มีอยู่เดิมควรได้เอฟเฟกต์เลย ไม่ต้องไปกดเปิดทีละห้อง
-- =====================================================================

ALTER TABLE public.bingo_games
  ADD COLUMN IF NOT EXISTS reveal_animation boolean NOT NULL DEFAULT true;
