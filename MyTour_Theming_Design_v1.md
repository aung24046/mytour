# MyTour — ระบบธีมต่อบริษัท (White-label Theming) v1

สถานะ: ร่างเพื่อพิจารณา · วันที่ 6 ส.ค. 2569

---

## 1. ที่มา

มีบริษัททัวร์รายใหม่สนใจใช้งาน แต่ไม่ต้องการโทนสีเดียวกับทริปแรก
ความต้องการจริงจึงไม่ใช่ "เปลี่ยนธีมของแอป" แต่คือ **"แต่ละบริษัทเห็นแอปเป็นสีของตัวเอง"**
ซึ่งเป็นฟีเจอร์ขายของ ไม่ใช่ของตกแต่ง — และเข้าทางที่ระบบเดินมาอยู่แล้ว
(`organizations.logo_url` มีอยู่แล้ว, `TourContext` ส่ง `orgId` ให้ฝั่งลูกทัวร์อยู่แล้ว)

### ข้อสรุปที่ตกลงกันแล้ว

| หัวข้อ | ข้อสรุป |
|---|---|
| ขอบเขตการปรับแต่ง | เลือกพรีเซ็ต + ปรับสีแบรนด์หลักได้ (ไม่ใช่ color picker อิสระทุก token) |
| ใครแก้ได้ | `owner` เท่านั้น |
| มีผลกับส่วนไหน | ทั้งแอป (ลูกทัวร์ + staff) |

---

## 2. หลักการที่ยึด

1. **ธีมคุมได้แค่ "สีแบรนด์" ไม่ใช่ "สีความหมาย"**
   เขียว = สำเร็จ, แดง = อันตราย, เหลือง = เตือน ต้องคงที่ทุกบริษัท
   ถ้าให้ลูกค้าเปลี่ยนสีปุ่ม SOS ได้ นั่นคือความเสี่ยงด้านความปลอดภัย ไม่ใช่ฟีเจอร์

2. **ระบบต้องรับประกันคอนทราสต์ ไม่ใช่ฝากไว้กับรสนิยมลูกค้า**
   ลูกค้าเลือกสีแบรนด์ → ระบบคำนวณเฉดอ่อน/เข้มและสีตัวหนังสือบนพื้นนั้นให้เอง
   ถ้าคู่ไหนไม่ผ่าน WCAG AA ระบบต้องขยับเฉดเองจนผ่าน แล้วแจ้งให้ผู้ใช้เห็นว่าขยับไป

3. **เอกสารพิมพ์ไม่ตามธีม**
   A4 ที่พิมพ์ขาวดำต้องอ่านออกเสมอ `printProfiles.js` คุมสีของตัวเองต่อไป

4. **แก้ token ที่เดียว**
   ถ้ายังมีสีดิบกระจายอยู่ในหน้า การเปลี่ยนธีมจะได้ผลแค่ครึ่งจอ (ดู §6)

---

## 3. สถาปัตยกรรม

### 3.1 เปลี่ยน token เป็น CSS variable

ปัจจุบัน `tailwind.config.js` ฝัง hex ตรงๆ ซึ่งเป็นค่าตอน build เปลี่ยนตอนรันไทม์ไม่ได้
ต้องเปลี่ยนให้ Tailwind อ้างตัวแปรแทน

```css
/* index.css — ค่าเริ่มต้น (พรีเซ็ตของระบบ) */
:root {
  --c-brand: 8 145 178;
  --c-brand-hover: 14 116 144;
  --c-brand-light: 207 250 254;
  --c-brand-lighter: 236 254 255;
  --c-ink: 15 43 53;
  --c-ink-muted: 91 117 128;
  /* ... */
}
```

```js
// tailwind.config.js
brand: {
  DEFAULT: 'rgb(var(--c-brand) / <alpha-value>)',
  hover:   'rgb(var(--c-brand-hover) / <alpha-value>)',
}
```

**ข้อบังคับ: ต้องเก็บเป็นเลขสามช่อง (`8 145 178`) ไม่ใช่ `#0891b2`**
เพราะโค้ดปัจจุบันใช้ opacity modifier บน token อยู่ราว 50 จุด เช่น
`bg-success-bg/40`, `bg-brand-light/70`, `bg-ink-faint/20`
ถ้าเก็บเป็น hex ตัว `<alpha-value>` จะใช้ไม่ได้ และจุดเหล่านี้พังเงียบๆ ทั้งหมด

### 3.2 ที่เก็บค่าธีม

```sql
ALTER TABLE public.organizations
  ADD COLUMN IF NOT EXISTS theme_preset text NOT NULL DEFAULT 'ocean',
  ADD COLUMN IF NOT EXISTS theme_brand_color text,   -- '#2f5d8c' — null = ใช้ค่าของพรีเซ็ต
  ADD COLUMN IF NOT EXISTS theme_updated_at timestamptz;
```

เก็บแค่ **ชื่อพรีเซ็ต + สีแบรนด์ที่ override** ไม่เก็บ token ทั้งชุด
เหตุผล: ถ้าเก็บทั้งชุด วันที่เราปรับพรีเซ็ตให้ดีขึ้น (เช่นแก้คอนทราสต์) บริษัทเก่าจะไม่ได้อานิสงส์
และ schema จะผูกกับรายชื่อ token ซึ่งยังต้องเปลี่ยนอีกแน่นอน

พรีเซ็ตนิยามในโค้ด (`src/lib/themes.js`) ไม่ใช่ใน DB — เหตุผลเดียวกับที่ `permissions.js`
เลือก hardcode ตารางสิทธิ์ไว้ในโค้ด: สิ่งที่ config เองได้คือสิ่งที่ทดสอบไม่ได้

พรีเซ็ตเริ่มต้น 4 ชุด (มีไฟล์ตัวอย่างพร้อมแล้วใน `design-preview/`):

| key | ชื่อ | โทน |
|---|---|---|
| `ocean` | ฟ้าทะเล (ของเดิม) | ค่าปริยาย ของเดิมไม่กระทบ |
| `slate` | Slate professional | น้ำเงินเข้ม + เขียวเทอร์ควอยซ์ |
| `mono` | Monochrome signal | เทาเกือบดำ + อินดิโก |
| `navy` | Navy brass | กรมท่า + ทองเหลืองด้าน |

### 3.3 การคำนวณเฉดจากสีแบรนด์

เมื่อ owner เลือกสีแบรนด์เอง ระบบสร้าง token ที่เหลือจากสีนั้น:

```
brand         = สีที่เลือก
brand-hover   = ลดความสว่าง ~12%
brand-light   = ผสมขาวจนได้ความสว่าง ~92%
brand-lighter = ผสมขาวจนได้ความสว่าง ~97%
brand-deep    = ลดความสว่าง ~28%
```

จากนั้น **guard คอนทราสต์** ตรวจสามคู่บังคับ แล้วขยับจนผ่าน:

| คู่สี | เกณฑ์ | ใช้ที่ |
|---|---|---|
| ขาว บน `brand` | ≥ 4.5:1 | ปุ่มหลัก, หัวแถบ, ปุ่ม QR กลางแถบ |
| `brand-hover` บน `brand-lighter` | ≥ 4.5:1 | ปุ่มรอง, ชิปเมนูล่างที่ active |
| `brand` บน `surface` ขาว | ≥ 3:1 | ไอคอนและเส้นขอบ |

ถ้าสีที่เลือกสว่างเกินจนขาวอ่านไม่ออก (เช่นเหลืองสด) ระบบไม่ปฏิเสธ
แต่เข้มสีลงเองจนผ่าน แล้วแสดงตัวอย่างพร้อมข้อความว่า "ปรับให้เข้มขึ้นเพื่อให้อ่านออกบนปุ่ม"
— ต้องเห็นก่อนบันทึก ไม่ใช่ไปเจอทีหลังบนมือถือลูกทัวร์

### 3.4 การฉีดธีมตอนรันไทม์

```
โหลด org theme → เขียน CSS variable ลง <html> ผ่าน style property → เสร็จ
```

จุดเชื่อม:

- **ฝั่งลูกทัวร์** — `TourContext` มี `orgId` อยู่แล้ว โหลดธีมพร้อมกับข้อมูลทริป
- **ฝั่ง staff** — `staffSession` มี `org_id` อยู่แล้ว โหลดตอน login

**ปัญหาสีกระพริบ (FOUC):** ระหว่างรอ DB ตอบ แอปจะโชว์สีปริยายก่อนแล้วค่อยเด้งเป็นสีบริษัท
แก้ด้วยการ cache ธีมไว้ใน `localStorage` (ใช้ `offlineCache.js` ที่มีอยู่)
แล้วฉีดจาก cache ทันทีตอนบูตก่อน DB ตอบ — จำเป็นอยู่แล้วเพราะแอปต้องทำงานตอนออฟไลน์

### 3.5 สิทธิ์

เพิ่มใน `permissions.js`:

```js
'org.theme': 'owner',   // แก้ธีมของบริษัท — กระทบทุกทริปและลูกทัวร์ทุกคน
```

วางไว้กลุ่มเดียวกับ `org.profile` ที่เป็น owner อยู่แล้ว
UI ไปอยู่ใน `CompanyProfile.jsx` เป็นแท็บ/การ์ดใหม่ ไม่สร้างหน้าใหม่

> หมายเหตุตามที่ `permissions.js` เตือนไว้เอง: นี่คือการคุมสิทธิ์ฝั่ง client เท่านั้น
> ผู้ใช้ที่ยิง Supabase API ตรงยังแก้ได้ การป้องกันจริงต้องรอ RLS (Design v2 §9)
> สำหรับธีม ความเสียหายสูงสุดคือ "สีเพี้ยน" ไม่ใช่ข้อมูลรั่ว จึงรับความเสี่ยงนี้ได้ในเฟสนี้

---

## 4. สิ่งที่ต้องไม่ตามธีม

| สิ่งของ | เหตุผล |
|---|---|
| `success` / `warning` / `danger` | สีความหมาย เปลี่ยนแล้วอันตราย โดยเฉพาะปุ่ม SOS |
| `printProfiles.js` และหน้าใน `pages/staff/docs/` | เอกสาร A4 ต้องอ่านออกตอนพิมพ์ขาวดำ |
| `genderColor.js` | สีสื่อความหมายในผังห้อง/ที่นั่ง |
| `guideCategoryStyle.js` (8 จุด) | สีประจำหมวดคู่มือ ต้องคงที่เพื่อให้จำได้ |
| QR code | ต้องดำบนขาวเสมอ ไม่งั้นสแกนไม่ติด |

---

## 5. UI ฝั่ง owner

การ์ดใหม่ในหน้า **ข้อมูลบริษัท** ประกอบด้วย:

1. เลือกพรีเซ็ต 4 แบบ แสดงเป็นแถบสีให้เห็นทันที
2. ช่องสีแบรนด์ (ไม่บังคับ) — ว่างไว้ = ใช้สีของพรีเซ็ต
3. **ตัวอย่างสด** — แสดงหน้าลูกทัวร์ย่อส่วน (แบบเดียวกับ `design-preview/preview.html`)
   อัปเดตทันทีที่เปลี่ยนสี ไม่ต้องบันทึกก่อนดู
4. แถบเตือนคอนทราสต์ ถ้าระบบขยับสีให้
5. ปุ่มคืนค่าเริ่มต้น

หลักคือ **เห็นผลก่อนบันทึก** เพราะคนกดคือเจ้าของบริษัท ไม่ใช่ดีไซเนอร์
และผลกระทบตกกับลูกทัวร์ทุกคนที่กำลังเดินทางอยู่

---

## 6. งานเก็บกวาดที่ต้องทำก่อน

นี่คือส่วนที่กินเวลาที่สุด และต้องทำอยู่ดีไม่ว่าจะเลือกทางไหน
ตัวเลขจากการนับในโค้ดจริงวันนี้:

| สิ่งที่ต้องแก้ | ลูกทัวร์ | staff | components | รวม |
|---|---:|---:|---:|---:|
| `gray-*` ดิบ | 51 | 506 | 25 | **582** |
| `sky-*` (อาศัย remap ใน config) | 11 | 110 | 0 | **121** |
| hex ฝังใน JSX | — | — | — | **59 จุด / 19 ไฟล์** |

hex ส่วนใหญ่คือ prop `color` ของ `<Icon>` เช่น `<Icon color="#0e7490">` (10 จุด)
วิธีแก้คือให้ `Icon` ใช้ `currentColor` เป็นค่าปริยาย แล้วคุมสีจากคลาสของ parent แทน
— แก้ที่ component เดียวได้ผลหลายสิบจุด

`index.css` ยังมี gradient พื้นหลัง, `.hero-gradient`, focus ring, และ scrollbar
ที่ฝังสีฟ้าไว้ตรงๆ ต้องย้ายมาใช้ตัวแปรทั้งหมด

---

## 7. แผนเป็นเฟส

แต่ละเฟสจบในตัว ปล่อยขึ้นจริงได้ ไม่ต้องรอเฟสถัดไป

### เฟส 0 — เลือกธีมของทริปแรกก่อน
เลือกจาก 5 แบบที่ร่างไว้ (A/B/C/D/E) แล้วอัปเดต `tailwind.config.js` ชุดเดียว
ได้ผลลัพธ์ที่ตาเห็นทันที และเป็นการทดสอบว่าโทนใหม่ใช้งานจริงได้
**ยังไม่แตะเรื่อง multi-theme**

### เฟส 1 — เก็บกวาดสีดิบ ✅ เสร็จแล้ว (6 ส.ค. 2569)

ผลจริง: แก้ 46 ไฟล์ · แทนสีดิบ 786 จุด

| สิ่งที่ต้องแก้ | ก่อน | หลัง |
|---|---:|---:|
| `gray-*`/`slate-*` ในหน้าแอป | 449 | **0** |
| `sky-*` | 121 | **0** |
| hex ใน JSX (หน้าแอป) | 59 | 16 (สีเชิงหมวดหมู่ล้วน) |
| `Icon color="#hex"` | 28 | **0** |
| hex ใน `index.css` | 9 | **0** |

เพิ่ม token ใหม่: `line-{subtle,DEFAULT,strong}` (เส้นขอบเดิมกระจายสามแบบ)
และ `warning-ink` (ข้อความบนแถบประกาศด่วน — ถ้าใช้ `warning-text` คอนทราสต์ตกจาก 12.6:1 เหลือ 6.4:1)

ลบ remap `sky` ออกจาก config แล้ว พร้อมคอมเมนต์ห้ามใส่กลับ

**ยังไม่ได้แตะโดยตั้งใจ:**

- เอกสารพิมพ์ 131 จุด (`components/document/`, `pages/staff/docs/`) — ต้องไม่ตามธีม
- สีเพศ (ฟ้า/ชมพู) และประเภทที่นั่ง (`emerald-500`) — สีเชิงหมวดหมู่
- `CATEGORY_STYLE` ใน `formFieldGroups.js`, `guideCategoryStyle.js`, `DocumentHub` tint — ชุดสีประจำหมวด

**หนี้ที่เจอระหว่างทาง:** `DietarySummary`, `GuestManager`, `SeatMap` ก็อปตรรกะสีเพศไปเขียนซ้ำ
แทนที่จะเรียก `genderColor.js` ควรรวมในรอบหน้า ไม่งั้นแก้สีเพศทีต้องไล่แก้สี่ที่

---

### เฟส 1 (เดิม) — รายละเอียดที่วางไว้
- `<Icon>` ใช้ `currentColor`
- แทน `gray-*` ด้วย token `ink-*` / `surface-*` / `line-*`
- แทน `sky-*` ด้วย `brand-*` แล้ว **ลบ remap `sky` ออกจาก config** (ตัวนี้คือกับดัก
  เพราะมันทำให้โค้ดที่ใช้สีผิดยังดูถูกต้องอยู่)
- ย้ายสีใน `index.css` มาเป็นตัวแปร

ทำทีละโฟลเดอร์ เริ่มที่ `components/` (25 จุด) → `pages/guest/` (62 จุด) → `pages/staff/` (616 จุด)
ฝั่ง staff ใหญ่ที่สุดแต่เสี่ยงน้อยที่สุด เพราะคนใช้คือทีมงานที่แจ้งเราได้ทันทีถ้าเพี้ยน

**เกณฑ์ผ่าน:** `grep -r "gray-[0-9]" src --include=*.jsx` ต้องเหลือศูนย์

### เฟส 2 — token เป็น CSS variable ✅ เสร็จแล้ว (6 ส.ค. 2569)

`tailwind.config.js` ไม่มีค่าสีจริงแล้ว ทุก token อ้าง `rgb(var(--c-*) / <alpha-value>)`
ค่าสีจริงอยู่ที่ `:root` ใน `src/index.css` ที่เดียว เป็นเลขสามช่องทั้งหมด (33 ตัวแปร)

**สิ่งที่ต้องแปลงด้วย ไม่ใช่แค่ตาราง colors:**

- `backgroundImage` ทั้งสามตัว (`brand-gradient`, `brand-soft`, `app`) — ถ้าลืม แถบหัวกับปุ่ม QR จะค้างเป็นสีฟ้าเดิมทั้งที่สีอื่นเปลี่ยนหมดแล้ว
- `boxShadow` — เงาการ์ดมีสีแบรนด์ผสมอยู่ (`rgba(8,145,178,0.28)`) ไม่ใช่เงาดำ
- `index.css` เอง: พื้นหลัง body, แถบเลื่อน, `.hero-gradient`, เส้นโฟกัส

**ผลการตรวจ:**

| การตรวจ | ผล |
|---|---|
| สีที่ render ออกมาเปลี่ยนไปไหม | 93 rule เทียบค่า RGBA แล้วตรงกันทุกตัว |
| คลาสสีดิบที่ยังค้างในหน้าแอป | 0 |
| ตัวแปรที่ประกาศแล้วไม่มีใครใช้ | ไม่มี |
| rule ที่ผูกกับตัวแปรธีม | 127 (76 ตัวเปลี่ยนตามสีแบรนด์) |
| opacity modifier ยังทำงาน | ยืนยันแล้ว — `bg-brand/40` ได้ `rgb(47 93 140 / 0.4)` |

**จุดรั่วที่การตรวจจับได้ (regex กวาดรอบแรกมองไม่เห็น):**

- `border-l-green-500`, `border-l-red-400`, `border-l-amber-500` ใน `CheckIn`/`SOSMonitor`
  — เพราะ pattern เดิมจับแค่ `border-` ไม่ได้เผื่อ `border-l-`
- `text-red-500` 10 จุดใน `DynamicField`, `hover:bg-red-700` ใน `Button`,
  `bg-amber-*` ใน `TourSwitcher` และ `App.jsx` — component ที่ใช้ร่วมกัน เลยหลุดจากการกวาดรายโฟลเดอร์

บทเรียน: การกวาดด้วย regex รายโฟลเดอร์ไม่พอ ต้องตรวจจาก **CSS ที่ compile ออกมา** ว่ามีสีเขียนตรงๆ
เหลืออยู่ที่คลาสไหน แล้วไล่กลับไปหาไฟล์ต้นทาง — วิธีนี้เท่านั้นที่จับของที่หลุดได้ครบ

**หน้าทดสอบ:** `design-preview/theme-switch-test.html` เปิดในเบราว์เซอร์แล้วกดสลับ 4 ธีมได้
ใช้ `app.css` ที่ compile จากโค้ดจริง ไม่ใช่ CSS ที่เขียนมือ

### เฟส 3 — เก็บลง DB + UI ฝั่ง owner ✅ เสร็จแล้ว (6 ส.ค. 2569)

| ไฟล์ | หน้าที่ |
|---|---|
| `supabase/migrations/20260806_org_theme.sql` | เพิ่ม `theme_preset`, `theme_brand_color`, `theme_updated_at` |
| `src/lib/themes.js` | พรีเซ็ต 4 ชุด, คำนวณเฉด, guard คอนทราสต์, ฉีดตัวแปร, cache |
| `src/lib/useOrgTheme.js` | โหลดธีมจาก org แล้วฉีด — cache ก่อน DB ทีหลัง |
| `src/components/common/ThemeSettings.jsx` | การ์ดตั้งค่า + ตัวอย่างสด |
| `src/lib/permissions.js` | เพิ่ม capability `org.theme` = `owner` |
| `src/lib/__tests__/` | ทดสอบ 2 ไฟล์ · `npm test` |

**⚠️ พบระหว่างทาง: สีแบรนด์เดิมไม่ผ่าน WCAG AA**

`#0891b2` กับตัวหนังสือขาวได้คอนทราสต์แค่ **3.68:1** ต่ำกว่าเกณฑ์ AA (4.5:1)
กระทบปุ่มหลักและปุ่ม QR กลางแถบ ซึ่งลูกทัวร์ต้องกดกลางแดด

แก้เป็น **`#077c99`** (4.83:1) — เข้มขึ้นเล็กน้อย สังเกตได้ถ้าวางเทียบกัน
ต้องแก้ทั้ง `THEME_PRESETS.ocean.brandColor` และค่าเริ่มต้นใน `index.css` ให้ตรงกัน

**บทเรียนเรื่องสูตรคำนวณเฉด:** ตอนแรกทำ `brand-start` (หัว gradient) ด้วยการผสมขาว
ผลคือฟ้าทะเลกลายเป็นฟ้าหม่น (`#8ac1cf`) เพราะผสมขาวลดความสด
ต้องเพิ่มความสว่างใน HSL แทนเพื่อคงเฉดสีไว้ → ได้ `#0bbfec` ซึ่งใกล้ `#22d3ee` เดิม
เฉดอ่อนที่ใช้เป็นพื้นหลัง (`brand-light/lighter`) ยังใช้ผสมขาวถูกแล้ว เพราะต้องการให้จางจริง

**ผลทดสอบ guard คอนทราสต์** — ทดสอบ 11 สีรวมเคสสุดโต่ง (ขาวล้วน ดำล้วน เหลืองสด):

- ทุกสีให้ผลผ่าน AA ทั้งคู่ "ขาวบนแบรนด์" และ "ปุ่มรอง" หลังผ่าน guard
- สีที่โดนขยับจะมีข้อความบอกผู้ใช้ พร้อมโชว์สีที่ใช้จริง
- พรีเซ็ตทั้ง 4 ชุดของเราเองผ่านโดยไม่ต้องขยับ

**ที่ยังไม่ได้ทำ:** ยังไม่มีการเตือนตอนบันทึกว่า "มีลูกทัวร์กำลังเดินทางอยู่"
(อยู่ในตาราง §8) — ต้องนับทริปที่ `status = 'active'` ก่อน

### เฟส 4 — โหมดมืด ✅ เสร็จแล้ว (6 ส.ค. 2569)

| ไฟล์ | หน้าที่ |
|---|---|
| `src/index.css` | บล็อก `:root[data-mode='dark']` — ชุดสีโหมดมืดทั้งชุด |
| `src/lib/colorMode.js` | จัดการโหมด (สว่าง/มืด/ตามระบบ) + จำค่าต่อเครื่อง |
| `src/components/common/ColorModeToggle.jsx` | สวิตช์สามตัวเลือก |
| `index.html` | สคริปต์ตั้งโหมดก่อน React render — กันจอขาวแวบ |
| `src/lib/__tests__/contrast.test.mjs` | ตรวจคู่สีที่ใช้จริง ทั้งสองโหมด |

**โหมดสีเป็นค่าต่อเครื่อง ไม่ใช่ต่อบริษัท** — ธีมคือแบรนด์ของบริษัท (owner คุม)
แต่โหมดมืดคือความชอบของคนใช้ ลูกทัวร์คนหนึ่งเปิดโหมดมืดไม่ควรทำให้คนทั้งทริปจอมืดตาม
ค่าเริ่มต้นตามการตั้งค่าของเครื่อง และมีตัวเลือก "ตามระบบ" เสมอ

**⚠️ กับดักที่ต้องรู้: inline style ชนะ CSS ของโหมดมืด**

ธีมบริษัทฉีดตัวแปรเป็น inline style บน `<html>` ซึ่ง specificity สูงกว่ากฎ
`:root[data-mode='dark']` ใน stylesheet เสมอ ถ้าคำนวณเฉดชุดเดียวแล้วฉีดไปตรงๆ
โหมดมืดจะได้เฉดแบรนด์ของโหมดสว่างค้างอยู่ (ชิปพื้นอ่อนสว่างจ้าบนพื้นดำ)

ทางแก้: `resolveTheme(row, mode)` รับโหมดเข้าไปด้วย และ `useOrgTheme` ฉีดใหม่ทุกครั้งที่สลับโหมด

**เฉดแบรนด์สลับบทบาทตามโหมด** — ชื่อ token ไม่เปลี่ยนความหมาย แต่ค่ากลับด้าน:

| token | โหมดสว่าง | โหมดมืด |
|---|---|---|
| `brand-light/lighter` | พื้นชิปสีจาง | พื้นชิปสีเข้ม |
| `brand-hover` | ตัวหนังสือสีเข้ม | ตัวหนังสือสีสว่าง |
| `brand` | เท่ากันทั้งสองโหมด (ปุ่มทึบยังใช้ตัวหนังสือขาว) | |

**ผลการตรวจคอนทราสต์ — เจอปัญหาที่มีมาแต่เดิม 12 คู่**

เขียนสคริปต์ตรวจคู่สีที่ใช้จริง 20 คู่ × 2 โหมด อ่านค่าจาก `index.css` โดยตรง
ไม่ใช่ copy มาไว้ในเทสต์ ผลคือเจอของเสียที่มีอยู่ก่อนแล้ว ไม่ใช่แค่ของใหม่:

| ปัญหา | เดิม | แก้เป็น |
|---|---|---|
| ข้อความจาง (`ink-faint`) บนการ์ด | 2.50 | 4.66 |
| ข้อความรองบนพื้นจม | 4.40 | 5.5 |
| ขาวบนป้าย "ตอนนี้" (`success`) | 3.30 | 4.7 |
| ขาวบนปุ่มเน้น (ส้มพีช) | 2.74 | ใช้ตัวหนังสือเข้มแทน |
| ขาวบนป้ายทีมงาน (emerald-500) | 2.54 | emerald-700 → 5.5 |
| ขาวบนป้าย VIP (amber-500) | 2.15 | amber-700 → 5.0 |

**ทางแก้ที่เลือกสำหรับพื้นส้ม/เหลือง:** เพิ่ม token `on-accent` / `on-warning`
ที่เป็นสีเข้มทั้งสองโหมด แทนที่จะบังคับให้ส้มพีชเข้มขึ้นจนกลายเป็นสีอิฐ
ตัวหนังสือเข้มบนพื้นเหลืองคือรูปแบบมาตรฐานอยู่แล้ว

ทางเลือกที่ไม่เลือก: เปลี่ยน `text-white` 54 จุดที่อยู่บนพื้นแบรนด์ให้เป็น token `on-brand`
— ถูกต้องกว่าในเชิงทฤษฎี แต่แตะโค้ดเยอะโดยไม่จำเป็น เพราะบังคับให้ `brand`
เข้มพอสำหรับตัวขาวทั้งสองโหมดก็ได้ผลเดียวกัน (guard ทำให้อยู่แล้ว)

---

## 8. ความเสี่ยงและการรับมือ

| ความเสี่ยง | การรับมือ |
|---|---|
| ลูกค้าเลือกสีที่อ่านไม่ออก | guard บังคับ AA + ตัวอย่างสดก่อนบันทึก |
| แก้ 700+ จุดแล้วสีเพี้ยนไม่รู้ตัว | ทำทีละโฟลเดอร์ + เทียบภาพหน้าจอก่อน/หลัง + `design-preview/` เป็นตัวอ้างอิง |
| สีกระพริบตอนเปิดแอป | cache ธีมใน localStorage ฉีดก่อน DB ตอบ |
| ธีมหายตอนออฟไลน์ | ใช้ `offlineCache.js` ชุดเดียวกับข้อมูลทริป |
| ลูกค้าเปลี่ยนสีกลางทริป | เปลี่ยนได้ทันที แต่เตือนก่อนบันทึกว่ามีลูกทัวร์ที่กำลังเดินทางอยู่ |
| staff สับสนว่าอยู่บริษัทไหน | แสดงโลโก้ + ชื่อบริษัทบนแถบหัวฝั่ง staff |

---

## 9. สิ่งที่ยังไม่ตัดสิน

1. **ให้ปรับสี accent ได้ด้วยไหม** — ตอนนี้ผูกกับพรีเซ็ต ถ้าลูกค้ามี CI สองสีอาจไม่พอ
   เสนอให้รอฟีดแบ็กจากลูกค้ารายนี้ก่อน
2. **ธีมระดับทริป** — เช่นทริปพิเศษอยากใช้สีอื่น ยังไม่มีใครขอ ไม่ควรทำล่วงหน้า
3. **โลโก้บนแถบหัวฝั่งลูกทัวร์** — มี `logo_url` แล้ว แต่ต้องคิดเรื่องโลโก้พื้นเข้ม/พื้นอ่อน
   อาจต้องเก็บสองไฟล์
