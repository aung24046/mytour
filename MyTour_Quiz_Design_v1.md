# MyTour — Quiz (เกมตอบคำถามสด) Design v1

**วันที่:** 18 ส.ค. 2026 · **แก้ครั้งที่ 8** · **สถานะ:** เฟส 1-4 เสร็จครบ + รันบน Supabase จริงแล้วทั้งหมด

**แก้ครั้งที่ 1:** เปลี่ยนวิธีเข้าเกมเป็น **โมเดลห้องแบบบิงโก** (ข้อ 6.0) · ตัดเรื่องเชื่อมกับสุ่มรางวัลออก · ขยายเรื่องเวลาของข้อ `numeric` (ข้อ 12.1)

**แก้ครั้งที่ 8:** เพิ่มลบชุดคำถาม — ข้อ 17.3.1

**แก้ครั้งที่ 7:** เฟส 4 (คลัง + สรุปหลังเกม) เสร็จ — ข้อ 17 · สรุปสถานะทั้งโครงการ — ข้อ 18

**แก้ครั้งที่ 6:** เฟส 3 (โหมดทีม) เสร็จ — ข้อ 16

**แก้ครั้งที่ 5:** ย่อรูปอัตโนมัติก่อนอัปโหลด — ข้อ 14.4

**แก้ครั้งที่ 4:** เฟส 2 (รูป/วิดีโอ) เสร็จ — ข้อ 14 · ปัญหาที่เจอตอนรันของจริง — ข้อ 15

**แก้ครั้งที่ 3:** เขียนเฟส 1 เสร็จ — จุดที่ต่างจากแบบและผลทดสอบอยู่ที่ **ข้อ 13**

**แก้ครั้งที่ 2:** `numeric` = 45 วิ + **ทีมงานกดเฉลยก่อนหมดเวลาได้** (ข้อ 5.1) · **ไม่มี `checkin_only`** ใครเข้าห้องได้ก็เล่นได้ รวมถึงทีมงาน/คนขับ/ไกด์ที่ขึ้นรถกลางทาง (ข้อ 4.5) · **ไม่เหลือคำถามค้างแล้ว — พร้อมเขียน migration เฟส 1**

**สิ่งที่จะได้:** เกมแบบ Kahoot ในแอป — จอใหญ่ขึ้นคำถาม ลูกทัวร์กดตอบในมือถือ ให้คะแนนตามความถูก + ความเร็ว ขึ้นกระดานอันดับระหว่างรอบ

**หลักการ:** ADDITIVE เท่านั้น — ไม่แตะตาราง/หน้าเดิม ไม่แก้ `bingo_*` / `draw_*` เกมเดิมต้องทำงานต่อได้ทั้งหมด

---

## 0. สรุปการตัดสินใจ

| หัวข้อ | ตัดสินใจ | เหตุผลสั้นๆ |
|---|---|---|
| สถานการณ์หลัก | งานเลี้ยง/กาล่า + **บนรถบัส (ต่อทีวี)** | จอบนรถเล็กและเน็ตแย่กว่า → เป็นตัวกำหนดดีไซน์ ไม่ใช่กรณียกเว้น |
| ชนิดคำถาม v1 | `mcq` (4 ตัวเลือก), `tf` (ถูก/ผิด), `numeric` (เดาเลข ใกล้สุดชนะ) | ครอบคลุมที่ขอไว้ครบ |
| รูป/วิดีโอในคำถาม | มี — ใช้ bucket ใหม่ `quiz-media` | ทำตาม pattern `org-assets` / `receipt-photos` ที่มีอยู่ |
| ทีม | **ยังไม่ทำ** แต่เผื่อ `team_id` ไว้ใน schema ตั้งแต่แรก | ไม่ต้อง migrate ใหญ่ตอนเพิ่มทีหลัง |
| เข้าเกมยังไง | **ทีมงานสร้าง "ห้อง" → แขกกดเข้าร่วม** เหมือนบิงโกเป๊ะ ไม่มี PIN แบบ Kahoot | แขกคุ้นกับ flow นี้อยู่แล้ว + ได้หลายห้องพร้อมกันฟรี (ดูข้อ 6.0) |
| นาฬิกา | **เวลาเริ่มเป็นเวลาสัมบูรณ์จาก server** ทุกจอนับถอยหลังไปที่วินาทีเดียวกัน | มือถือคนละเครื่องนาฬิกาไม่ตรงกัน — ห้ามเชื่อเวลาฝั่ง client |
| เฉลย | เก็บแยกตาราง `quiz_keys` ที่ `anon` อ่านไม่ได้ | RLS ตอนนี้เปิดหมด (`USING (true)`) — ถ้าเก็บรวมจะเปิด DevTools ดูเฉลยได้ |
| ให้คะแนน | RPC `SECURITY DEFINER` คิดจาก `now()` ฝั่ง server | เวลาที่ client ส่งมาปลอมได้ |
| ควบคุมรอบ | มี `host_token` ต่อ session | ตอนนี้ไม่มี Supabase Auth — ต้องมีความลับอย่างน้อยหนึ่งอย่าง ไม่งั้นแขกกด "ข้ามข้อ" ได้ |
| ใครเล่นได้ | **ทุกคนที่กดเข้าห้องได้** — ลูกทัวร์ + ทีมงาน + คนขับ/ไกด์ **ไม่เช็คว่าเช็คอินหรือยัง** | ไกด์ท้องถิ่นขึ้นรถกลางทางต้องเล่นได้ทันที ไม่ต้องไปเพิ่มชื่อในระบบก่อน |
| เวลาข้อ `numeric` | 45 วิ **แต่ทีมงานกดเฉลยก่อนได้ทุกเมื่อ** | 45 วิเผื่อคนพิมพ์ช้าที่สุด ไม่ใช่บังคับให้ทุกคนรอ — ปกติจะจบใน 20 วิ |

---

## 1. ทำไมไม่ใช้ Kahoot จริงๆ ไปเลย

| | Kahoot | ในแอป |
|---|---|---|
| แขกต้องทำอะไร | โหลดแอป/เปิดเว็บ พิมพ์ PIN พิมพ์ชื่อเล่น | เปิดหน้าเกม → เห็นห้องที่เปิดอยู่ → กดเข้าร่วม (เหมือนบิงโก) |
| ชื่อบนกระดาน | แขกพิมพ์เอง (ตลกบ้าง หยาบบ้าง) | ดึงชื่อเล่นจริงจาก `guests.nickname` |
| แจกรางวัล | ต้องจดชื่อจากจอมาเทียบเอง | ผูก `guest_id` ตรง → ส่งต่อเข้าสุ่มรางวัลได้ |
| เน็ตบนรถ | เว็บนอกโหลดช้า | payload เล็กมาก + มี fallback polling ที่ทำไว้แล้ว |
| ค่าใช้จ่าย | จ่ายรายเดือนต่อ host | ไม่มี |
| ธีมบริษัท | ไม่ได้ | ใช้ `org_theme` ที่ทำไว้แล้ว |

**สรุป:** คุ้มที่จะทำเอง เพราะของยากที่สุด (realtime host→guest, จอเวที, ตัวตนแขก) มีอยู่ในโปรเจกต์แล้วทั้งหมด

---

## 2. ของเดิมที่เอามาใช้ซ้ำได้ (สำคัญมาก — อย่าเขียนใหม่)

| ต้องการ | ของที่มีอยู่แล้ว | เอามาใช้ยังไง |
|---|---|---|
| realtime host→guest | `src/lib/useDrawRoom.js` | copy โครงมาทำ `useQuizSession.js` — ฟัง UPDATE ของ session แถวเดียว |
| กัน state กระพริบ | `src/pages/guest/BingoCard.jsx` (`pendingWritesRef`) | ใช้แนวคิดเดียวกันตอนแขกกดตอบแล้ว realtime วิ่งสวน |
| fallback ตอน realtime ต่อไม่ติด | `src/components/common/AnnouncementBanner.jsx` | คัด logic polling ชั้นที่ 2 มาใช้ — **จำเป็นมากบนรถบัส** |
| จอเวที | `src/pages/staff/LuckyDrawStage.jsx` | ทำ `QuizStage.jsx` โครงเดียวกัน (แท็บแยก ไม่มีปุ่มสั่งงาน) |
| อนิเมชันเผยผล | `src/lib/useNumberReveal.js`, `NumberReveal.jsx` | ใช้ตอนนับคะแนน/ขึ้นอันดับ |
| ตัวตนแขก | `src/lib/guestSession.js` (`getGuestId(tourId)`) | ไม่ต้องมีระบบ join ใหม่ |
| หน้ารวมเกม | `pages/staff/Games.jsx`, `pages/guest/Games.jsx` | เพิ่ม `GameCard` ใบที่ 3 — ไฟล์ออกแบบมาให้เพิ่มอยู่แล้ว |
| อัปโหลดรูป | pattern ของ `ExpenseTracker` (bucket `receipt-photos`) | สร้าง bucket `quiz-media` แบบเดียวกัน |
| สิทธิ์ | `src/lib/permissions.js` | เพิ่ม capability ใหม่ 3 ตัว (ข้อ 7) |
| ธีม/หน้าตา | `tailwind.config.js` + `org_theme` | จอเวทีใช้สีบริษัทได้ทันที |

---

## 3. ปัญหาที่ต้องแก้ให้ได้ 4 ข้อ (นี่คือแก่นของเอกสารนี้)

### 3.1 นาฬิกาไม่ตรงกัน → คะแนนความเร็วเพี้ยน

มือถือแต่ละเครื่องตั้งเวลาต่างกันได้เป็นนาที ถ้าให้ client ส่ง "ผมตอบตอน 12:00:03.2" มาเอง คะแนนจะมั่ว **และปลอมได้ด้วย**

**วิธีแก้ — 3 ชั้น**

```
1) host กด "เริ่มข้อนี้"
   → server ตั้ง question_started_at = now() + 3 วินาที   ← เวลาสัมบูรณ์ในอนาคต
                question_ends_at     = started_at + time_limit

2) ทุกจอ (ทีวี + มือถือทุกเครื่อง) นับถอยหลัง "3.. 2.. 1.. เริ่ม!"
   ไปที่วินาทีเดียวกัน — 3 วินาทีนี้คือ buffer กลบ latency ของ realtime
   ระหว่างนี้แสดง "คำถาม" ได้ แต่ปุ่มตัวเลือกยังกดไม่ได้

3) เวลาที่ใช้คิดคะแนน = now() ตอน RPC ทำงาน − question_started_at
   client ไม่มีสิทธิ์ส่งเวลาเข้ามาเลย
```

จอนับถอยหลังให้ตรงกันได้ด้วย RPC `quiz_now()` — client เรียกครั้งเดียวตอนเข้าเกม วัด round-trip แล้วเก็บ `offset` ไว้ใช้ทั้งเกม (`serverNow = Date.now() + offset`)

### 3.2 เปิด DevTools แล้วเห็นเฉลย

**สภาพตอนนี้:** RLS ของ `draw_*` / `bingo_*` เป็น `USING (true)` และ grant ให้ `anon` หมด — ใครก็ยิง REST อ่านได้ทุกแถว (`permissions.js` เขียนเตือนไว้เองว่าเป็นการกันฝั่ง client เท่านั้น)

สำหรับบิงโกไม่มีปัญหา เพราะไม่มีอะไรให้ปิด แต่ควิซมี "เฉลย" ที่ต้องปิดจริง

**วิธีแก้ — แยกเฉลยออกจากคำถาม**

```
quiz_questions   ← ข้อความ, รูป, ตัวเลือก 4 ข้อ, เวลาจำกัด     [anon อ่านได้]
quiz_keys        ← correct_index, correct_number, คำอธิบาย     [anon อ่านไม่ได้เลย]
                   REVOKE ALL ... FROM anon, authenticated
                   เข้าถึงได้ทางเดียว: RPC SECURITY DEFINER
```

- **แขกตอบ** → `quiz_submit(...)` อ่าน key ข้างใน แล้วคืนแค่ `{accepted: true}` — **ไม่บอกว่าถูกหรือผิด** จนกว่าจะถึงจังหวะเฉลย
- **ถึงจังหวะเฉลย** → host เรียก `quiz_reveal(...)` ซึ่ง copy เฉลยลง `quiz_sessions.reveal_payload` (jsonb ที่อ่านได้) → ทุกจอเห็นพร้อมกันผ่าน realtime
- **สตาฟแก้คำถาม** → `quiz_set_for_edit(p_set_id, p_staff_id, p_pin)` ตรวจ PIN ข้างในด้วย pattern เดียวกับ `verify_tour_staff_pin` ที่มีอยู่แล้ว

### 3.3 แขกสั่งงานเกมเองได้

ถ้า host กดข้ามข้อด้วยการ UPDATE ธรรมดา แขกก็ UPDATE ได้เหมือนกัน

**วิธีแก้:** `quiz_sessions.host_token` (random 32 hex) — `REVOKE SELECT (host_token)` และคำสั่งควบคุมทุกตัว (`next`, `lock`, `reveal`, `end`) เป็น RPC ที่ต้องส่ง token มาด้วย token คืนให้ครั้งเดียวตอน `quiz_start_session` แล้วเก็บใน `sessionStorage` ของเครื่อง host

จอเวทีเปิดด้วย URL ที่มี token ติดไปด้วย (`/staff/quiz/stage/:id#t=...`) — จอเวทีไม่ต้องสั่งงาน แต่ต้องเห็นสถิติคำตอบก่อนเฉลย

### 3.4 80 คนกดพร้อมกันในวินาทีเดียว

**อย่าทำ:** ให้ทุกคน `UPDATE quiz_sessions SET answer_count = answer_count + 1` — 80 transaction แย่ง lock แถวเดียว และยิง realtime 80 event ใส่ทุกเครื่อง

**ให้ทำ:**

| ข้อมูล | วิธี | เหตุผล |
|---|---|---|
| คำตอบ | INSERT `quiz_answers` แถวใครแถวมัน | ไม่มี lock ชนกัน |
| คะแนนสะสม | UPDATE `quiz_players` **แถวของตัวเอง** | ไม่มี lock ชนกัน |
| จำนวนคนตอบแล้ว (host เห็น) | host **poll** `quiz_answer_count()` ทุก 1.5 วิ ระหว่างเปิดรับ | ยิง query 13 ครั้งต่อข้อ ถูกกว่า realtime 80 event มาก |
| แขกเห็นอะไร | subscribe แถว `quiz_sessions` **แถวเดียว** | 1 event ต่อการเปลี่ยนสถานะ ไม่ใช่ต่อคนตอบ |

payload ที่แขกได้รับต่อข้อ ≈ ไม่กี่ร้อยไบต์ — พอสำหรับ 4G บนรถ

---

## 4. Schema

```
quiz_sets ──┬── quiz_questions ──── quiz_keys        (1:1, เฉลย, anon อ่านไม่ได้)
            │
            └── quiz_sessions ──┬── quiz_players ──── (guest_id, score)
              (การเล่นหนึ่งรอบ)  │
                                 └── quiz_answers
```

### 4.1 `quiz_sets` — ชุดคำถาม (คลังกลาง)

ทำตาม pattern **Definition ↔ Assignment** ของ Design v2: ชุดคำถามอยู่ระดับบริษัท/ปลายทาง ไม่ใช่ระดับทริป → ชุด "รู้จักญี่ปุ่นแค่ไหน" ทำครั้งเดียวใช้ได้ทุกกรุ๊ปที่ไปญี่ปุ่น

| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| `id` | uuid PK | |
| `org_id` | uuid → organizations | |
| `destination_id` | uuid NULL → destinations | NULL = ใช้ได้ทุกปลายทาง |
| `title` / `description` | text | |
| `lang` | text default `'th'` | ชุดคำถามภาษาไทย/อังกฤษ/จีน แยกชุดกัน ไม่ทำ i18n รายข้อ (ดูข้อ 8) |
| `cover_url` | text | |
| `default_time_limit` | int default 20 | วินาที ใช้เป็นค่าตั้งต้นของข้อใหม่ |
| `is_archived` | boolean | |
| `created_by` | uuid → staff | |

### 4.2 `quiz_questions`

| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| `id` | uuid PK | |
| `set_id` | uuid → quiz_sets ON DELETE CASCADE | |
| `sort_order` | int | |
| `kind` | text CHECK `('mcq','tf','numeric')` | |
| `text` | text | |
| `media_url` / `media_kind` | text / `('image','video')` | bucket `quiz-media` |
| `options` | jsonb `["ก","ข","ค","ง"]` | `tf` = 2 ตัว, `numeric` = `[]` |
| `numeric_unit` | text | เช่น "เมตร", "บาท" — โชว์ข้างช่องกรอก |
| `time_limit_sec` | int default 20 | |
| `points_factor` | numeric default 1.0 | ข้อยากให้ 2 เท่าได้ |

> **ทำไม `options` เป็น jsonb ไม่ใช่ตารางแยก:** ตัวเลือกไม่เคยถูก query แยก ไม่มี FK ชี้เข้ามา และ 4 ตัวเลือกต้องมาพร้อมคำถามเสมอ — ตารางแยกจะได้ join เปล่าๆ ทุกครั้ง (เหตุผลเดียวกับที่ `draw_rooms.extras` เป็น jsonb)

### 4.3 `quiz_keys` — เฉลย (หัวใจของการกันโกง)

| คอลัมน์ | ชนิด |
|---|---|
| `question_id` | uuid PK → quiz_questions ON DELETE CASCADE |
| `correct_index` | int NULL — สำหรับ `mcq` / `tf` |
| `correct_number` | numeric NULL — สำหรับ `numeric` |
| `explain` | text — คำอธิบายที่โชว์ตอนเฉลย |

```sql
ALTER TABLE public.quiz_keys ENABLE ROW LEVEL SECURITY;
-- ไม่สร้าง policy ใดๆ = ไม่มีใครอ่านได้ผ่าน REST
REVOKE ALL ON public.quiz_keys FROM anon, authenticated;
-- เข้าถึงได้เฉพาะผ่าน SECURITY DEFINER function เท่านั้น
```

### 4.4 `quiz_sessions` — **หนึ่งแถว = หนึ่งห้อง** (เทียบเท่า `bingo_games`)

| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| `id` | uuid PK | |
| `tour_id` | uuid → tours | |
| `set_id` | uuid → quiz_sets | ชุดคำถามที่ห้องนี้ใช้ |
| `name` | text | เช่น "ควิซหลังอาหาร", "ควิซรถคันที่ 1" |
| `bus_id` | uuid NULL → buses | **NULL = ทั้งทริปเห็น** / ระบุ = เฉพาะคนบนรถคันนั้น (ดูข้อ 6.0) |
| `state` | text CHECK | `lobby / countdown / answering / locked / reveal / scoreboard / finished`<br>`lobby` = ห้องเปิดรอคน · `finished` = ห้องปิด (หลุดจากรายการของแขก)<br>เทียบกับบิงโก: `waiting` → `lobby`, `playing` → 5 สถานะกลาง, `finished` → `finished` |
| `current_index` | int | ข้อที่เท่าไหร่ |
| `current_question_id` | uuid | |
| `question_started_at` | timestamptz | **เวลาสัมบูรณ์ที่ทุกจอนับถึง** |
| `question_ends_at` | timestamptz | |
| `reveal_payload` | jsonb | เฉลย + สถิติ ใส่ตอน state=`reveal` เท่านั้น |
| `join_open` | boolean | ปิดรับคนเข้าใหม่กลางเกมได้ |
| `late_join` | boolean default true | เข้าช้าได้ แต่ข้อที่ผ่านไปแล้วได้ 0 |
| `stage_theme` | text default `'led'` | ใช้ค่าเดียวกับ `draw_rooms.stage_theme` |
| `screen_mode` | text CHECK `('projector','bus_tv','none')` | **ดูข้อ 6** |
| `host_token` | text | REVOKE SELECT |
| `created_at` / `ended_at` | timestamptz | |

### 4.5 `quiz_players`

| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
**ไม่มี `checkin_only`** — ตัดสินใจแล้วว่าใครกดเข้าห้องได้ก็เล่นได้ ผลคือผู้เล่นมี 3 แบบ ไม่ใช่แบบเดียว:

| แบบ | เป็นใคร | ตัวตนมาจากไหน |
|---|---|---|
| `guest` | ลูกทัวร์ | `guestSession.getGuestId(tourId)` |
| `staff` | ทีมงาน/คนขับที่อยู่ในระบบ | `staffSession` — ล็อกอินอยู่แล้ว |
| `visitor` | **ไกด์ท้องถิ่นที่ขึ้นรถกลางทาง** ไม่มีชื่อในระบบเลย | สแกน QR ห้อง → พิมพ์ชื่อครั้งเดียว |

| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| `id` | uuid PK | |
| `session_id` | uuid → quiz_sessions ON DELETE CASCADE | |
| `kind` | text CHECK `('guest','staff','visitor')` | |
| `guest_id` | uuid NULL → guests ON DELETE SET NULL | ใส่เมื่อ `kind='guest'` |
| `staff_id` | uuid NULL → staff ON DELETE SET NULL | ใส่เมื่อ `kind='staff'` |
| `device_key` | text NULL | ใส่เมื่อ `kind='visitor'` — random เก็บใน `localStorage` ใช้จำเครื่องตอนรีเฟรช |
| `display_name` | text | snapshot จาก `guests.nickname` / `staff.name` / ที่พิมพ์เอง |
| `team_id` | uuid NULL | **เผื่อไว้ Phase 3 ยังไม่ใช้** |
| `score` / `streak` / `correct_count` | int | |
| `last_seen_at` | timestamptz | **ใช้ตัดสินว่า "ตอบครบแล้ว" หรือยัง — ดูข้อ 5.1** |

```sql
-- ต้องมีตัวตนอย่างใดอย่างหนึ่งเท่านั้น
CHECK (num_nonnulls(guest_id, staff_id, device_key) = 1)

-- กันคนเดียวเปิด 2 แท็บแล้วได้ 2 สิทธิ์ตอบ — partial index เพราะคอลัมน์ NULL ได้
CREATE UNIQUE INDEX quiz_players_guest_uniq  ON quiz_players (session_id, guest_id)   WHERE guest_id  IS NOT NULL;
CREATE UNIQUE INDEX quiz_players_staff_uniq  ON quiz_players (session_id, staff_id)   WHERE staff_id  IS NOT NULL;
CREATE UNIQUE INDEX quiz_players_device_uniq ON quiz_players (session_id, device_key) WHERE device_key IS NOT NULL;
```

> **ทำไมทีมงานเล่นได้ด้วยไม่แปลก:** คนขับกับไกด์อยู่บนรถคันเดียวกับลูกทัวร์ ถ้าเล่นไม่ได้จะดูแปลกกว่า — และคนที่ **คุมเกม** ไม่ควรเล่นอยู่แล้ว ระบบไม่ต้องบังคับ ปล่อยให้เป็นมารยาทหน้างาน
>
> **`visitor` เป็นทางเลี่ยงเดียวที่ต้องพิมพ์ชื่อ** — จำเป็นเพราะไกด์ท้องถิ่นไม่มีแถวในระบบ ถ้าเลี่ยงได้ควรเพิ่มเขาเข้า `tour_staff` แทนจะสะอาดกว่า (ชื่อจะไปโผล่ในเอกสารอื่นด้วย)

### 4.6 `quiz_answers`

| คอลัมน์ | ชนิด |
|---|---|
| `id`, `session_id`, `question_id`, `player_id` | |
| `choice_index` | int NULL |
| `number_value` | numeric NULL |
| `elapsed_ms` | int — **คำนวณฝั่ง server เท่านั้น** |
| `is_correct` | boolean NULL — `numeric` ยังเป็น NULL จนกว่าจะเฉลย |
| `points` | int |

UNIQUE `(session_id, question_id, player_id)` — **ตอบได้ครั้งเดียว เปลี่ยนใจไม่ได้** (เหมือน Kahoot)

---

## 5. สูตรคะแนน

```
mcq / tf:
  ตอบผิด        → 0
  ตอบถูก        → round( 1000 × (1 − 0.5 × elapsed/limit) × points_factor )
                   เร็วสุด = 1000, ช้าสุด (หมดเวลาพอดี) = 500

numeric (ใกล้สุดชนะ) — คิดตอนเฉลย ไม่ใช่ตอนตอบ:
  เรียงทุกคนตาม |ตอบ − เฉลย|
  ที่ 1 = 1000, ที่ 2 = 800, ที่ 3 = 600, ที่เหลือที่ตอบมา = 300
  ตอบตรงเป๊ะ    → +200 โบนัส
  เท่ากัน        → คนที่ส่งก่อนได้อันดับดีกว่า

streak (ถูกติดกัน) — เปิด/ปิดได้ที่ระดับ set:
  ถูกติดกัน 2, 3, 4, 5+ ข้อ → +100, +200, +300, +400
```

### 5.1 "ครบแล้วเฉลยเลย" — กติกาการปิดรับก่อนหมดเวลา

45 วิคือ **เพดาน** ไม่ใช่เวลาที่ต้องรอจริง หน้าทีมงานต้องดันให้กดปิดได้ทันทีที่ครบ

```
QuizHost — ระหว่าง state = 'answering'
┌────────────────────────────────────┐
│ ข้อ 7/10 · numeric · เหลือ 31 วิ    │
│                                     │
│ ส่งแล้ว 41 / 41 คน   ✓ ครบแล้ว      │  ← poll ทุก 1.5 วิ
│                                     │
│ ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓  │
│ ┃  ปิดรับ + เฉลยเลย            ┃  │  ← ปุ่มเปลี่ยนเป็นสีเด่น + สั่น 1 ครั้ง
│ ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛  │     ตอนตัวเลขครบพอดี
│ [ รอต่อ ]                           │
└────────────────────────────────────┘
```

**รายละเอียดที่พลาดง่าย 4 ข้อ:**

| เรื่อง | ต้องทำ |
|---|---|
| "ครบ" นับจากอะไร | นับจาก `quiz_players` ที่ `last_seen_at` ภายใน 60 วิ — **ไม่ใช่จำนวนคนในห้องทั้งหมด** ไม่งั้นคนที่แบตหมด/ปิดแอปไปแล้ว 1 คน จะทำให้ไม่ครบตลอดกาล |
| คนที่ยังไม่ส่ง | โชว์ชื่อให้ทีมงานเห็น (2-3 คนแรก) — จะได้เรียกชื่อบนไมค์ว่า "รออีกคนเดียวนะครับ พี่สมชาย" ซึ่งเป็นบรรยากาศที่ดีของงานเลี้ยง |
| กดปิดตอนยังไม่ครบ | ได้เสมอ ไม่ต้องยืนยัน — แต่คนที่ยังไม่ส่งได้ 0 และมือถือเด้งเป็น `locked` ทันที |
| แข่งกันตอนเส้นยาแดง | คนกดส่งพอดีตอนทีมงานกดปิด → RPC `quiz_submit` ผ่อนผัน **+200ms** หลัง `question_ends_at` / หลัง state เปลี่ยนเป็น `locked` (กติกาเดียวกับตอนหมดเวลาปกติ) |

> `quiz_lock` + `quiz_reveal` มีอยู่แล้วในข้อ 9 — "ปิดรับ + เฉลยเลย" คือการเรียกสองตัวนี้ติดกัน ไม่ต้องเพิ่ม RPC ใหม่

**ทำไมช้าสุดยังได้ 500:** ถ้าให้คะแนนไล่ลงถึง 0 คนที่เน็ตช้าบนรถจะแพ้เพราะเน็ต ไม่ใช่เพราะไม่รู้คำตอบ — เสียบรรยากาศมากกว่าได้ความยุติธรรม (Kahoot ใช้เกณฑ์เดียวกัน)

---

## 6. หน้าจอ

### 6.0 โมเดล "ห้อง" — ลอกบิงโกมาทั้งดุ้น

**ตัดสินใจแล้ว:** ทีมงานสร้างห้อง → แขกเห็นห้องที่เปิดอยู่ในหน้าเกม → กดเข้าร่วม
ไม่มี PIN ไม่มีรหัสห้อง ไม่มีการพิมพ์ชื่อ — เหมือน `bingo_games` ทุกประการ

```
ทีมงาน                                      ลูกทัวร์
──────                                      ────────
เลือกชุดคำถาม + ตั้งชื่อห้อง
กด "สร้างห้อง"
  → INSERT quiz_sessions (state='lobby')
                                            เปิด /t/:code/games
                                            เห็นการ์ด "ควิซ · เปิดอยู่ 1 ห้อง"
                                            กดเข้า → เห็นรายชื่อห้อง
                                            กดห้อง → quiz_join()
                                              → มีแถว quiz_players แล้ว
                                            จอค้างที่ "รอทีมงานเริ่ม"
เห็นจำนวนคนเข้าห้องขึ้นเรื่อยๆ
กด "เริ่มเกม"
  → state = 'countdown'                     ทุกเครื่องเด้งเข้าเกมพร้อมกัน
...
กด "จบเกม"
  → state = 'finished'                      ห้องหลุดจากรายการอัตโนมัติ
```

**สิ่งที่ได้ฟรีจากการทำแบบนี้ — และเป็นคำตอบของคำถามข้อ 2 เดิม (รถ 2 คัน):**

โมเดลห้องรองรับหลายห้องพร้อมกันในทริปเดียวอยู่แล้ว (บิงโกก็ query `.in('status', ['waiting','playing'])` คืนมาเป็น list) → รถ 2 คันก็แค่**สร้าง 2 ห้อง** ไม่ต้องออกแบบอะไรเพิ่ม

- อยากให้แข่งข้ามรถ → สร้างห้องเดียว `bus_id = NULL` ทั้งทริปเห็น
- อยากแยกคัน → สร้าง 2 ห้อง ใส่ `bus_id` คนละคัน แขกเห็นเฉพาะห้องของรถตัวเอง (ไม่ต้องเลือกเอง = ไม่กดผิดห้อง)

> **`bus_id` เป็น filter ของ "ใครเห็นห้อง" เท่านั้น ไม่ใช่การแบ่งทีม** — คนละเรื่องกับโหมดทีมในเฟส 3

**รายละเอียดที่ลอกมาด้วยเพราะบิงโกเจ็บมาแล้ว:**

| เรื่อง | บิงโกทำยังไง | ควิซทำตาม |
|---|---|---|
| กดเข้าร่วมซ้ำ/กดรัวๆ | `bingo_ensure_card` เป็น upsert ฝั่ง DB (unique `game_id+guest_id`) เพราะเดิม select-then-insert แล้วได้การ์ดซ้ำ | `quiz_join` เป็น upsert + UNIQUE `(session_id, guest_id)` |
| ห้องจบแล้ว | realtime UPDATE มา ถ้า status ไม่ใช่ waiting/playing → กรองออกจาก list ทันที | เหมือนกัน เช็ค `state = 'finished'` |
| การ์ดเกมในหน้ารวม | `guest/Games.jsx` query นับห้องที่เปิดอยู่ → โชว์จุดเขียวกะพริบ | เพิ่ม query ที่ 3 นับ `quiz_sessions` ที่ยังไม่ finished |
| เข้าห้องช้า | เข้าได้ตลอด | `late_join` — เข้าได้ แต่ข้อที่ผ่านไปแล้วได้ 0 |

### 6.1 `/staff/quiz` — หน้ารวม + สร้างห้อง (`QuizManager.jsx`)

2 ส่วนในหน้าเดียว — โครงเดียวกับ `BingoHost.jsx` ตอนยังไม่มีเกม

1. **ห้องที่เปิดอยู่** — ชื่อห้อง, ชุดคำถาม, กี่คนเข้าแล้ว, ปุ่ม "เข้าไปคุม" / "ปิดห้อง"
2. **คลังชุดคำถาม** — รายการชุด + ปุ่ม "สร้างห้องจากชุดนี้" (เลือก `bus_id` ได้ตอนสร้าง) + ปุ่มแก้ไข

เพิ่ม `GameCard` ใบที่ 3 ใน `pages/staff/Games.jsx` ชี้มาที่นี่ (ไฟล์นั้นเขียนคอมเมนต์รองรับไว้แล้ว)

### 6.2 `/staff/quiz/builder/:setId` — สร้างชุดคำถาม (`QuizBuilder.jsx`)

ลอกโครงจาก `FormBuilder.jsx` (ลาก-เรียง, เพิ่ม-ลบ, พรีวิว)

- เลือกชนิดข้อ → ฟอร์มเปลี่ยนตาม (`mcq` = 4 ช่อง + ติ๊กข้อถูก, `numeric` = ช่องเลข + หน่วย)
- อัปโหลดรูป/วิดีโอ → `quiz-media` (จำกัดวิดีโอ 15 วิ / 10 MB — **บนรถบัสยาวกว่านี้โหลดไม่ทัน**)
- **ปุ่มพรีวิวจอเวที** — ดูว่าข้อความยาวไปไหมก่อนขึ้นจริง
- บันทึกผ่าน RPC (เพราะต้องเขียน `quiz_keys` ที่ REST เขียนไม่ได้)

### 6.3 `/staff/quiz/host/:sessionId` — มือถือทีมงาน (`QuizHost.jsx`)

จอสั่งงานอย่างเดียว — คนคุมเกมถือเครื่องนี้

```
┌──────────────────────────────┐
│ ข้อ 3/10 · เหลือ 12 วิ       │
│ ตอบแล้ว 34 / 41 คน  ●●●●○    │  ← poll ทุก 1.5 วิ
│                              │
│ [ ปิดรับคำตอบเลย ]           │  ← ไม่ต้องรอหมดเวลา ถ้าทุกคนตอบแล้ว
│ [ เฉลย ]                     │
│ [ ข้อถัดไป ]                 │
│ [ ข้ามข้อนี้ ]               │  ← รูปไม่ขึ้น/คำถามผิด ต้องหนีได้
│ [ หยุดพัก ] [ จบเกม ]        │
└──────────────────────────────┘
```

ปุ่ม "ปิดรับคำตอบเลย" คือหัวใจของข้อ 5.1 — ต้องเด่นขึ้นอัตโนมัติเมื่อทุกคนที่ online ส่งครบ

**ต้องมีปุ่มหนีทุกสถานการณ์** — บทเรียนจาก `BingoHost`: หน้างานจริงมีเรื่องไม่คาดคิดเสมอ (ไมค์ดับ อาหารมาเสิร์ฟกลางเกม แขกเข้าห้องน้ำ)

### 6.4 `/staff/quiz/stage/:sessionId` — จอใหญ่ (`QuizStage.jsx`)

เปิดแท็บแยกเหมือน `LuckyDrawStage` — **ไม่มีปุ่มสั่งงานเลย**

`screen_mode` เปลี่ยนหน้าตาจริง ไม่ใช่แค่ขนาดฟอนต์:

| | `projector` (งานเลี้ยง) | `bus_tv` (บนรถ) |
|---|---|---|
| ขนาดจอที่ออกแบบให้ | 16:9 ใหญ่ ห่าง 10 เมตร | ทีวี 15-19" ห่าง 2-8 เมตร ที่นั่งหลังไกลกว่า |
| ฟอนต์คำถาม | 48-64px | **72px+ ตัวหนา** ตัดคำถามที่ยาวเกินออก |
| ตัวเลือก | 4 ช่องเต็มจอ + ข้อความ | 4 ช่อง **สี+รูปทรง** (▲■●◆) ข้อความสั้น — อ่านไม่ทันก็ดูสีจากมือถือได้ |
| กระดานอันดับ | Top 10 + อนิเมชันไต่อันดับ | **Top 3 เท่านั้น** จอเล็กเกิน |
| อนิเมชัน | เต็มที่ (ใช้ `useNumberReveal`) | ลดเหลือ fade — Android TV บนรถ CPU อ่อน กระตุกจะดูแย่กว่าไม่มี |
| เสียง | มี | ปิดโดยปริยาย (ลำโพงรถใช้ประกาศ) |

**สีตัวเลือกต้องตรงกันเป๊ะระหว่างจอกับมือถือ** — นี่คือกลไกทั้งหมดของ Kahoot: จอบอกว่าอะไร มือถือมีแค่สี่ปุ่มสี ใช้ token สีจาก `tailwind.config.js` ที่มีอยู่ (ห้าม hardcode hex) และต้อง**ผ่านเกณฑ์คนตาบอดสี** → จึงต้องมีรูปทรงกำกับด้วย ไม่ใช่สีอย่างเดียว

### 6.5 `/t/:code/quiz` — มือถือแขก (`Quiz.jsx`)

**เข้าหน้านี้ยังไง:** `/t/:code/games` → การ์ด "ควิซ" → ถ้ามีห้องเดียวเข้าเลย ถ้ามีหลายห้องให้เลือกก่อน (ลอก `activeGameId` ของ `BingoCard.jsx`)

```
(เลือกห้อง) → กด "เข้าร่วม" → quiz_join()
lobby      → "รอทีมงานเริ่มเกม" + จำนวนคนที่เข้ามาแล้ว
countdown  → "3.. 2.. 1.." (นับตาม serverNow)
answering  → 4 ปุ่มสีเต็มจอ + แถบเวลา
             ★ ไม่ต้องแสดงข้อความคำถามซ้ำถ้ามีจอใหญ่ (screen_mode ≠ 'none')
               ให้คนเงยหน้าดูจอ = บรรยากาศงานเลี้ยง
             ★ ถ้า screen_mode = 'none' → แสดงคำถามเต็มบนมือถือ
locked     → "ส่งคำตอบแล้ว รอเฉลย..." + ตัวเลือกที่กดไป
reveal     → ถูก/ผิด + คะแนนที่ได้ + อันดับตัวเอง
scoreboard → อันดับตัวเอง (ไม่ต้องโชว์ top 10 ซ้ำจอใหญ่)
finished   → สรุปส่วนตัว "ตอบถูก 7/10 · อันดับ 4"
```

**ปุ่มต้องใหญ่พอกดตอนรถโยก** — ขั้นต่ำ 25% ของความสูงจอต่อปุ่ม

---

## 7. Routes + สิทธิ์

```jsx
// src/lib/permissions.js — เพิ่ม 3 บรรทัด
'quiz.host': 'staff',    // คุมเกมหน้างาน — เท่ากับ bingo.host
'quiz.edit': 'lead',     // แก้ชุดคำถามของทริปนี้
'quiz.define': 'admin',  // สร้าง/แก้ชุดในคลังกลาง (กระทบทุกทริป — เหมือน form.define)
```

```jsx
// src/App.jsx
<Route path="/staff/quiz"               element={staffRoute('quiz.host', <QuizManager />)} />
<Route path="/staff/quiz/builder/:setId" element={staffRoute('quiz.edit', <QuizBuilder />)} />
<Route path="/staff/quiz/host/:id"      element={staffRoute('quiz.host', <QuizHost />)} />
<Route path="/staff/quiz/stage/:id"     element={staffRoute('quiz.host', <QuizStage />)} />

// guest — ใต้ /t/:code เหมือนหน้าอื่น
<Route path="quiz" element={<GuestQuiz />} />
```

ไม่ต้องเพิ่ม legacy redirect เพราะเป็นของใหม่ ไม่มี QR เก่าชี้มา

---

## 8. เรื่อง i18n

แอปมี `th.json` / `en.json` / `zh.json` **UI** แปลตามปกติ แต่ **ตัวคำถามไม่แปล** — ชุดคำถามหนึ่งชุดมีภาษาเดียว (`quiz_sets.lang`)

เหตุผล: ถ้าทำ i18n รายข้อ สตาฟต้องกรอก 3 ภาษาต่อ 1 ข้อ = ไม่มีใครใช้จริง ทริปหนึ่งมีลูกค้าภาษาเดียวเป็นหลักอยู่แล้ว ถ้ากรุ๊ปผสมจริงๆ ให้ทำ 2 ชุดแล้วเล่น 2 รอบ

---

## 9. RPC ทั้งหมด

| ฟังก์ชัน | ใคร | ทำอะไร |
|---|---|---|
| `quiz_now()` | ทุกคน | คืน `now()` — ใช้ปรับ offset นาฬิกา |
| `quiz_join(session_id, kind, guest_id, staff_id, device_key, name)` | ใครก็ได้ | สร้าง/คืน `quiz_players` — **upsert ฝั่ง DB** แบบเดียวกับ `bingo_ensure_card` (กดรัวๆ ก็ได้แถวเดียว) · ไม่เช็คสถานะเช็คอิน · ปฏิเสธถ้า `join_open = false` |
| `quiz_heartbeat(player_id)` | ผู้เล่น | อัปเดต `last_seen_at` ทุก 20 วิ — เป็นตัวป้อนให้ "ตอบครบแล้ว" ในข้อ 5.1 แม่น |
| `quiz_pending_players(session_id, question_id, token)` | host | คืนชื่อคนที่ยัง**ไม่ส่ง** และยัง online — ใช้เรียกชื่อบนไมค์ |
| `quiz_submit(session_id, player_id, question_id, choice, number)` | แขก | ✱ ตรวจ `state='answering'` และ `now() ≤ ends_at` (+200ms ผ่อนผัน)<br>✱ คิด `elapsed_ms` จาก server<br>✱ อ่าน `quiz_keys` ตรวจถูก/ผิด<br>✱ UPDATE คะแนนแถวตัวเอง<br>✱ **คืนแค่ `{accepted}` ไม่บอกถูกผิด** |
| `quiz_answer_count(session_id, question_id)` | host | จำนวนคนตอบแล้ว (ไม่บอกว่าตอบอะไร) |
| `quiz_start_session(set_id, tour_id, ...)` | host | สร้าง session + คืน `host_token` **ครั้งเดียว** |
| `quiz_next(session_id, token)` | host | ตั้ง `question_started_at = now() + 3s`, state → `countdown` |
| `quiz_lock(session_id, token)` | host | state → `locked` ปิดรับทันที |
| `quiz_reveal(session_id, token)` | host | จัดอันดับ `numeric`, ใส่ `reveal_payload`, state → `reveal` |
| `quiz_finish(session_id, token)` | host | state → `finished`, `ended_at` |
| `quiz_set_for_edit(set_id, staff_id, pin)` | สตาฟ | คืนคำถาม **พร้อมเฉลย** — ตรวจ PIN แบบ `verify_tour_staff_pin` |
| `quiz_upsert_question(..., staff_id, pin)` | สตาฟ | เขียน question + key พร้อมกันใน transaction เดียว |

ทุกตัวเป็น `SECURITY DEFINER SET search_path = public` และ `GRANT EXECUTE ... TO anon, authenticated` — pattern เดียวกับ `draw_claim_prize`

---

## 10. แผนแบ่งเฟส

| เฟส | ได้อะไร | ไฟล์ใหม่ |
|---|---|---|
| **1. เล่นได้จริง** | `mcq` + `tf`, เดี่ยว, จอเวที, คะแนนความเร็ว, ห้องแบบบิงโก, ผู้เล่น 3 แบบ + heartbeat | migration 1 ไฟล์, `useQuizSession.js`, `QuizManager/Builder/Host/Stage.jsx`, `guest/Quiz.jsx` |
| **2. คำถามรวย** | รูป/วิดีโอ, `numeric`, streak | migration `quiz_media`, bucket `quiz-media` |
| **3. โหมดทีม** | จัดทีมจาก `bus_id` หรือกำหนดเอง, คะแนนรวมทีม | migration `quiz_teams` (ใช้ `team_id` ที่เผื่อไว้) |
| **4. คลัง + สถิติ** | ผูกชุดกับ `destination_id`, clone ข้ามทริป, สรุปหลังเกมลง ระบบเอกสาร (`document_presets`) | ต่อยอด pattern Design v2 |

**แนะนำ: หยุดดู feedback หลังเฟส 1** — เกมสดต้องผ่านสนามจริงก่อนถึงจะรู้ว่าเฟส 2-4 ควรทำอันไหนก่อน

---

## 11. ความเสี่ยง

| เรื่อง | ผลถ้าเกิด | กัน |
|---|---|---|
| Realtime ต่อไม่ติดบนรถ | แขกค้างที่ข้อเก่า | polling ชั้น 2 ทุก 3 วิ (ลอกจาก `AnnouncementBanner`) — **จำเป็น ไม่ใช่ nice-to-have** |
| แขกหลุดกลางเกม | เสียคะแนนข้อนั้น | `quiz_join` idempotent — กลับเข้ามาเล่นข้อถัดไปได้ทันที |
| เปิด 2 แท็บ | ได้ 2 สิทธิ์ตอบ | UNIQUE `(session_id, guest_id)` |
| กด "ข้อถัดไป" 2 ที | ข้ามข้อ | `quiz_next` ตรวจ `current_index` ที่คาดไว้ (optimistic lock) |
| host_token หายเพราะปิดแท็บ | คุมเกมต่อไม่ได้ | เก็บใน `sessionStorage` + ให้ `lead` ขึ้นไปขอ token ใหม่ได้ผ่าน RPC ที่ตรวจ PIN |
| คนที่แบตหมด/ปิดแอป ทำให้ "รอครบ" ไม่ครบสักที | ทีมงานรอเก้อทุกข้อ | นับเฉพาะคนที่ `last_seen_at` ภายใน 60 วิ (ข้อ 5.1) — **ถ้าลืมข้อนี้ ฟีเจอร์ "เฉลยเลย" จะใช้ไม่ได้จริง** |
| `visitor` พิมพ์ชื่อมั่ว/หยาบ | ขึ้นจอใหญ่ต่อหน้าทุกคน | ทีมงานลบผู้เล่นออกจากห้องได้จาก `QuizHost` (ปุ่มเดียว) |
| แขกไม่มีมือถือ/แบตหมด | ตกรอบ | โหมดทีม (เฟส 3) แก้ได้ — 1 เครื่องต่อทีม |
| วิดีโอโหลดไม่ทัน | เงียบกลางงาน | preload ล่วงหน้า 1 ข้อ + จำกัด 15 วิ/10 MB |
| RLS เปิดหมดทั้งระบบ | โกงทางอื่นได้อยู่ | เอกสารนี้กันเฉพาะ "เฉลย" กับ "เวลา" ซึ่งพอสำหรับควิซชิงรางวัลในงานเลี้ยง — การกันจริงต้องรอ Supabase Auth ตาม Design v2 §9 |

---

## 12. บันทึกการตัดสินใจ (ปิดครบแล้ว)

| # | คำถาม | ตัดสิน | อยู่ในเอกสารข้อ |
|---|---|---|---|
| 1 | เข้าเกมยังไง | ห้องแบบบิงโก — ทีมงานสร้าง แขกกดเข้าร่วม | 6.0 |
| 2 | รถ 2 คัน | ไม่ต้องออกแบบเพิ่ม — สร้าง 2 ห้อง ใส่ `bus_id` | 6.0 |
| 3 | เวลาข้อ `numeric` | **45 วิ + ทีมงานกดเฉลยก่อนได้** | 5.1, 12.1 |
| 4 | ใครเล่นได้ | **ทุกคนที่เข้าห้องได้ ไม่เช็คการเช็คอิน** (guest / staff / visitor) | 4.5 |
| 5 | เชื่อมกับสุ่มรางวัล | ไม่ทำ | — |

### 12.1 เหตุผลเบื้องหลังข้อ 3 (เก็บไว้อ้างอิง)

ปัญหาคือ **"พิมพ์" กับ "กด" ไม่ใช่การกระทำแบบเดียวกัน**

| | `mcq` / `tf` | `numeric` |
|---|---|---|
| แขกทำอะไร | มองจอ → กดสี 1 ครั้ง | มองจอ → คิดเลข → เรียกคีย์บอร์ด → พิมพ์ → กดส่ง |
| เวลาที่ใช้จริง | 1-3 วิ | 8-20 วิ (บนรถที่โยกไปมา นานกว่านี้อีก) |
| คีย์บอร์ดมือถือ | ไม่ขึ้น | **ขึ้นมาบังครึ่งจอ** — ถ้าแถบเวลาอยู่ล่างจะโดนบัง |

ถ้าใช้ 20 วิเท่าข้ออื่น คนพิมพ์ช้าจะแพ้เพราะนิ้ว ไม่ใช่เพราะไม่รู้คำตอบ — ผิดเจตนาของเกม

**3 ทางเลือก:**

| แนวทาง | กติกา | ข้อดี | ข้อเสีย |
|---|---|---|---|
| **A. ให้เวลามากกว่า** *(แนะนำ)* | 45 วิ, คะแนนยังคิดตามความเร็ว | ตรงไปตรงมา ไม่ต้องแก้สูตร | จังหวะเกมสะดุด — 45 วิบนเวทีถือว่านาน ต้องมีอะไรให้ดูระหว่างรอ |
| **B. ตัดความเร็วออก** | 30 วิ, **ให้คะแนนตามความใกล้อย่างเดียว** ใครส่งเมื่อไหร่ก็เท่ากัน | ยุติธรรมที่สุด คนพิมพ์ช้าไม่เสียเปรียบเลย | เสียความตื่นเต้น "รีบตอบ" · ต้องอธิบายกติกาว่าข้อนี้ไม่เหมือนข้ออื่น |
| **C. ไม่นับเวลาพิมพ์** | 30 วิ, จับเวลาตั้งแต่เปิดข้อ **ถึงตอนแตะช่องกรอกครั้งแรก** ไม่ใช่ตอนกดส่ง | ได้ทั้งความเร็วและความยุติธรรม | ซับซ้อน · โกงได้ (แตะช่องไว้ก่อนแล้วค่อยคิด) · ต้องยิง RPC 2 ครั้งต่อข้อ |

**เลือก A** (B ทำให้กติกาไม่สม่ำเสมอ แขกงงว่าทำไมข้อนี้ตอบเร็วแล้วไม่ได้เปรียบ · C โกงง่ายจนไม่คุ้มความซับซ้อน) **+ เพิ่มการปิดรับก่อนหมดเวลาในข้อ 5.1** ซึ่งแก้ข้อเสียเดียวของ A ไปในตัว — 45 วิกลายเป็นเพดานที่แทบไม่ได้ใช้จริง

**ต้องทำเพิ่ม 3 อย่าง:**

1. **จอเวทีต้องมีอะไรให้ดูระหว่าง 45 วิ** — โชว์ตัวเลขที่คนอื่นทยอยส่งมาเป็นจุดๆ บนเส้นจำนวน (ไม่บอกว่าใครตอบอะไร ไม่บอกเฉลย) เห็นแล้วสนุกและกดดันดี
2. **มือถือต้องดันแถบเวลาขึ้นเหนือคีย์บอร์ด** — ใช้ `visualViewport` API ไม่ใช่ `100vh`
3. **ปุ่มส่งต้องอยู่เหนือคีย์บอร์ดเสมอ** — ไม่งั้นแขกกดส่งไม่ได้แล้วหมดเวลา (เจ็บมาแล้วในฟอร์มลงทะเบียน)

ค่านี้เก็บใน `quiz_questions.time_limit_sec` อยู่แล้ว — เปลี่ยนแค่ **ค่าตั้งต้นตอนสร้างข้อ `numeric`** ให้เป็น 45 ไม่ต้องแก้ schema

### 12.2 เหตุผลเบื้องหลังข้อ 4 (เก็บไว้อ้างอิง)

**ไม่เอา `checkin_only`** ต่างจาก `draw_rooms` โดยตั้งใจ เพราะสองเกมนี้เดิมพันไม่เท่ากัน:

| | สุ่มรางวัล | ควิซ |
|---|---|---|
| คนไม่อยู่ในงานติดมาด้วย | **รางวัลลอย** ต้องสุ่มใหม่ เสียจังหวะบนเวที | ได้ 0 คะแนนแล้วหายไปเอง ไม่กระทบใคร |
| จึงต้องกรอง | ✅ | ❌ ไม่จำเป็น |

ประตูเดียวที่เหลือคือ **`join_open`** — ทีมงานปิดรับคนเข้าใหม่ได้เมื่อไหร่ก็ได้ ซึ่งคุมได้ตรงกว่าและเข้าใจง่ายกว่าเงื่อนไขเช็คอิน

**ผลข้างเคียงที่ต้องรับ:** คนที่ลงทะเบียนแต่ไม่ได้มางานเล่นจากที่บ้านได้ — ถ้าวันไหนกลายเป็นปัญหาจริง เพิ่ม `checkin_only` ทีหลังได้ ไม่ต้อง migrate ข้อมูลเก่า

---

## 13. ภาคผนวก — สิ่งที่เปลี่ยนตอนลงมือเขียนจริง (เฟส 1 เสร็จแล้ว)

เอกสารข้างบนคือแบบที่ตกลงกัน ส่วนนี้บันทึกจุดที่ต้องเปลี่ยนตอนเขียนจริง พร้อมเหตุผล — ของที่รันอยู่คือตามส่วนนี้

| # | ที่ออกแบบไว้ | ที่ทำจริง | ทำไม |
|---|---|---|---|
| 1 | `quiz_answers` อ่านได้ (คะแนนเป็นของสาธารณะ) | **ปิดทั้งอ่านและเขียน** + เพิ่ม RPC `quiz_my_answer` | แถวคำตอบมี `is_correct` ติดมาตั้งแต่ตอนส่ง ถ้าเปิดอ่าน ลูกทัวร์ยิง REST ดูแถวคนอื่นที่ `is_correct = true` = **รู้เฉลยทั้งข้อ** · `quiz_my_answer` คืนแค่ "ตอบอะไรไป" ไว้กู้สถานะตอนรีเฟรชกลางข้อ |
| 2 | บวกคะแนนเข้า `quiz_players` ตอนส่งคำตอบ | **บวกตอนเฉลยเท่านั้น** | `quiz_players` ต้องเปิดให้อ่าน (ใช้ทำกระดานอันดับ) ถ้าคะแนนขยับทันทีที่ส่ง ลูกทัวร์ poll แถวตัวเองก็รู้ว่าตอบถูกก่อนเวลา — รูรั่วชนิดเดียวกับข้อ 1 · ผลพลอยได้: โบนัส streak คิดง่ายขึ้นเพราะคิดทีเดียวจบ |
| 3 | `host_token` เป็นคอลัมน์ใน `quiz_sessions` | **แยกตาราง `quiz_session_tokens`** | PostgREST ยิง `select *` — ปิดเฉพาะคอลัมน์ในแถวที่ต้องให้อ่านไม่ได้ |
| 4 | มี state `countdown` ใน DB | **ไม่มี** — `answering` ครอบทั้งช่วง จอคำนวณเองจาก `question_started_at` | ถ้ามี state นี้จริงต้องมีตัวตั้งเวลาฝั่ง server มาพลิกให้ ซึ่งไม่มีและไม่ควรมี |
| 5 | (ไม่ได้ออกแบบไว้) | เพิ่ม `quiz_heartbeat` | ถ้าไม่มี ปุ่ม "ปิดรับ + เฉลยเลย" จะไม่มีวันเด้ง เพราะแยกไม่ออกว่าใครยังอยู่ |
| 6 | (ไม่ได้ออกแบบไว้) | เพิ่ม `quiz_live_numbers` (ต้องมี token) | ตัวเลขที่ทยอยขึ้นบนจอใหญ่ระหว่าง 45 วิของข้อเดาตัวเลข — จอใหญ่เท่านั้นที่ควรเห็น |

### ผลการทดสอบ (Postgres 16 จริง ไม่ใช่การอ่านโค้ดเอา)

| ทดสอบ | ผล |
|---|---|
| รัน migration ซ้ำสองรอบ | ✅ ผ่าน (idempotent) |
| เล่นทั้งเกมในฐานะ role `anon` (เหมือนแอปจริง) | ✅ ผ่าน |
| คะแนนความเร็ว: ตอบทันที vs ช้า 2 วิ (จาก 20 วิ) | ✅ 1000 / 950 |
| โบนัส streak 1→2→3 ข้อติด | ✅ 1000 → 2100 → 3300 |
| คนไม่ตอบข้อหนึ่ง streak ต้องขาด | ✅ กลับเป็น 0 |
| เดาตัวเลข: ตรงเป๊ะ / ที่ 2 / ที่ 3 | ✅ 1200 / 800 / 600 |
| **ก่อนเฉลย คะแนนทุกคนต้องยังเป็น 0** | ✅ ไม่รั่ว |
| ตอบซ้ำข้อเดิม | ✅ `already` ไม่นับให้ |
| ส่งหลังปิดรับ / หลังเฉลย | ✅ `closed` |
| กดเฉลยซ้ำ | ✅ ไม่คิดคะแนนรอบสอง |
| หมดข้อแล้วกด "ข้อถัดไป" | ✅ จบเกมเอง |
| `anon` อ่าน `quiz_keys` / `quiz_session_tokens` / `quiz_answers` | ✅ `permission denied` ทั้งสามตาราง |
| `anon` INSERT `quiz_answers` เองเพื่อยัดคะแนน | ✅ `permission denied` |
| `anon` เรียก `quiz_check_token` / `quiz_check_staff` เพื่อเดาความลับ | ✅ `permission denied` |
| token ปลอมสั่ง `quiz_next` | ✅ ปฏิเสธ |
| PIN ผิดแก้คำถาม | ✅ ปฏิเสธ |
| เข้าห้องซ้ำ / กดรัวๆ | ✅ ได้ผู้เล่นคนเดิม |
| เข้าห้องหลัง `join_open = false` | ✅ ปฏิเสธ |

---

## 14. เฟส 2 — รูป/วิดีโอในคำถาม (เสร็จแล้ว)

`numeric` กับ streak ทำไปแล้วตั้งแต่เฟส 1 เฟส 2 จึงเหลือแค่เรื่องสื่อประกอบ

### 14.1 ถัง `quiz-media`

`supabase/storage_quiz_media.sql` — public read แบบเดียวกับ `org-assets` / `receipt-photos`

| ข้อจำกัด | ค่า | ทำไม |
|---|---|---|
| ขนาดไฟล์ (บังคับที่ถัง) | 10 MB | คุมที่ชั้น bucket ไม่ใช่แค่ UI — คนยิง API ตรงก็โดนเหมือนกัน |
| ชนิดไฟล์ (บังคับที่ถัง) | jpeg, png, webp, gif, mp4, webm | |
| รูป (บังคับที่แอป) | 5 MB | รูปจากมือถือปกติ 2-4 MB ผ่านสบาย |
| วิดีโอ (บังคับที่แอป) | 15 วินาที | คำถามหนึ่งข้อมีเวลา 20 วิ คลิปยาวกว่านั้นก็ดูไม่จบอยู่ดี · ตรวจความยาวจาก metadata **ก่อน** อัปโหลด ผู้ใช้จะได้รู้ผลทันทีไม่ต้องรอโหลดจบ |

> ⚠️ ถังนี้ public — ใครเดา URL ถูกก็เปิดได้ ชื่อไฟล์จึงถูกสุ่มใหม่ทั้งหมด ไม่ใช้ชื่อเดิมของผู้ใช้
> ไม่งั้นไฟล์ชื่อ `คำตอบคือโตเกียว.jpg` จะกลายเป็นการแจกเฉลย

### 14.2 preload ข้อถัดไป — ข้อที่สำคัญที่สุดของเฟสนี้

จอใหญ่โหลดสื่อของ **ข้อถัดไป** ไว้ตั้งแต่ข้อปัจจุบันยังเล่นอยู่

ถ้าไม่ทำ: พอเปิดข้อที่มีวิดีโอบนรถบัส จอจะว่างหลายวินาทีระหว่างโหลด — และนั่น**กินเวลาตอบของลูกทัวร์ไปด้วย** เพราะนาฬิกาเริ่มเดินแล้วตั้งแต่ `question_started_at` ไม่ได้รอไฟล์

จอใหญ่ดึงรายการสื่อทั้งชุดมาครั้งเดียว (`quiz_questions` เปิดให้อ่านอยู่แล้ว เฉลยอยู่คนละตารางจึงไม่รั่ว) แล้ว preload ตัวถัดไปทุกครั้งที่ `current_index` ขยับ

### 14.3 อื่นๆ

- วิดีโอบนจอใหญ่ **ต้อง `muted`** ไม่งั้นเบราว์เซอร์บล็อก autoplay แล้วจอค้างที่เฟรมแรก (เสียงบนรถไม่ควรดังอยู่แล้ว ลำโพงใช้ประกาศ)
- ลบไฟล์เก่า**หลังบันทึกสำเร็จเท่านั้น** — ถ้าลบตอนกดเปลี่ยนรูปแล้วผู้ใช้กดยกเลิก ข้อเดิมจะเหลือ URL ที่ชี้ไปไฟล์ที่ไม่มีแล้ว
- ลบไฟล์เก่าไม่สำเร็จ ไม่ทำให้บันทึกคำถามล้มเหลว — แค่มีไฟล์กำพร้าค้างในถัง
- มือถือลูกทัวร์แสดงสื่อ **เฉพาะตอน `screen_mode = 'none'`** ถ้ามีจอใหญ่ให้เงยหน้าดูจอ

---

## 15. ปัญหาที่เจอตอนรันบน Supabase จริง

**`gen_random_bytes()` ไม่มีตัวตน** — `quiz_start_session` สร้าง token ด้วยฟังก์ชันของ pgcrypto แต่บน Supabase ส่วนขยายติดตั้งไว้ที่ schema `extensions` ไม่ใช่ `public` พอฟังก์ชันตั้ง `SET search_path = public` ไว้เพื่อความปลอดภัย ก็มองไม่เห็น → **เปิดห้องไม่ได้เลย**

ทดสอบบนเครื่องไม่เจอ เพราะ Postgres ที่ติดตั้งเองมี pgcrypto อยู่ใน `public`

**แก้:** ต่อ `gen_random_uuid()` สองก้อน = สุ่ม 256 bit ไม่พึ่งส่วนขยายอะไรเลย (`gen_random_uuid` เป็นของ core ตั้งแต่ PG13)

**บทเรียน:** ฟังก์ชันที่ตั้ง `SET search_path` แล้วเรียกอะไรนอก core ต้องเขียน schema กำกับเสมอ หรือเลี่ยงไปใช้ของ core

### Security Advisor จะเตือน 40 รายการ — ตั้งใจทั้งหมด

| เตือน | ทำไมถึงปล่อยไว้ |
|---|---|
| `rls_enabled_no_policy` บน `quiz_keys`, `quiz_session_tokens`, `quiz_answers` | **นี่คือฟีเจอร์ ไม่ใช่บั๊ก** — ตารางที่ไม่มี policy = ไม่มีใครอ่านได้ผ่าน REST ซึ่งคือสิ่งที่ต้องการ |
| `anon_security_definer_function_executable` (37 ตัว) | สถาปัตยกรรมทั้งระบบ — แอปใช้ anon key ตัวเดียว ฟังก์ชันคือด่านตรวจ ไม่ใช่ RLS |
| `function_search_path_mutable` บน `quiz_now` | **อันนี้พลาดจริง** แก้แล้ว |

### 14.4 ย่อรูปอัตโนมัติก่อนอัปโหลด (เพิ่มหลังคุยเรื่องโควตา)

ตกลงว่าจะใช้รูปอย่างเดียว ไม่ใช้วิดีโอ → ตัวแปรที่สำคัญที่สุดกลายเป็น **ขนาดรูป**

รูปจากมือถือทุกวันนี้ราว 3-4 MB (กว้าง ~4000px) แต่จอโปรเจกเตอร์กับทีวีบนรถแสดงได้จริงแค่ ~1920px ส่วนที่เหลือคือข้อมูลที่ไม่มีใครได้เห็น จึงย่อในเบราว์เซอร์ก่อนอัปโหลด (1920px, JPEG q=0.82) เหลือราว 200-300 KB

| | ไม่ย่อ (4 MB) | ย่อแล้ว (~250 KB) |
|---|---|---|
| เก็บได้กี่รูปใน Storage 1 GB | ~250 รูป (≈ 12 ชุดคำถาม) | ~4,000 รูป |
| Egress ต่อเกม 20 ข้อ (มีจอใหญ่) | 80 MB | 5 MB |
| Egress ต่อเกม 20 ข้อ (ไม่มีจอใหญ่) | 3.2 GB ← **หมดโควตาเดือนในเกมเดียว** | 205 MB |
| จอบนรถโหลดทันไหม | เสี่ยงขึ้นช้ากว่านาฬิกา | ทัน |

**ข้อที่สำคัญที่สุดคือ Storage ไม่ใช่ Egress** — egress รีเซ็ตทุกเดือน แต่รูปสะสมถาวร และคลังชุดคำถามถูกออกแบบมาให้โตขึ้นเรื่อยๆ ข้ามทริป (ข้อ 4.1) เพดาน 250 รูปจึงมาถึงเร็วกว่าที่รู้สึก

รายละเอียดที่พลาดง่าย:

- **ย่อก่อนแล้วค่อยตรวจขนาด** — ถ้าตรวจ 5 MB ก่อนย่อ รูปจากมือถือส่วนใหญ่จะโดนปฏิเสธทั้งที่ระบบย่อให้ผ่านได้อยู่แล้ว
- **GIF ไม่ย่อ** — canvas จะเหลือแค่เฟรมแรก ภาพเคลื่อนไหวหายหมด
- ย่อแล้วไม่เล็กลง (รูปเล็กอยู่แล้ว) → ใช้ไฟล์เดิม
- `createImageBitmap` ใช้ไม่ได้ (เบราว์เซอร์เก่า) → ส่งไฟล์เดิมไป ดีกว่าอัปโหลดไม่ได้เลย

---

## 16. เฟส 3 — โหมดทีม (เสร็จแล้ว)

### 16.1 รูปแบบที่เลือก

| หัวข้อ | ตัดสินใจ |
|---|---|
| ใครตอบ | **ทุกคนตอบเองจากมือถือตัวเอง** ไม่ใช่แบบ Kahoot ที่หนึ่งเครื่องต่อทีม |
| ใครจัดทีม | **ลูกทัวร์ตั้งทีมกันเอง** ทีมงานไม่ต้องจัดให้ |
| เข้าทีมยังไง | **ใครกดเข้าทีมไหนก็ได้** ไม่มีเชิญ ไม่มีรหัสทีม |
| คะแนนทีม | **เฉลี่ยต่อคน** ไม่ใช่ผลรวม |

เหตุผลที่ไม่มีระบบเชิญ: คนที่จะจับกลุ่มกันเขานั่งข้างกันอยู่แล้ว **คุยกันด้วยปากเร็วกว่าระบบเชิญใดๆ**

### 16.2 ผลข้างเคียงของ "เลือกทีมเอง" และวิธีรับมือ

ให้เลือกเองแปลว่าคุมอะไรไม่ได้เลย — 4 อย่างนี้จะเกิดแน่นอน ต้องเตรียมไว้ตั้งแต่ต้น

| จะเกิดอะไร | รับมือยังไง |
|---|---|
| **ทีมขนาดไม่เท่ากันแน่นอน** (เพื่อนกระจุกกันเป็นก้อน) | คะแนนคิดเป็น **เฉลี่ยต่อคน** — ถ้าใช้ผลรวม ทีม 20 คนชนะทีม 4 คนตั้งแต่ยังไม่เริ่มเล่น เกมจะไม่มีความหมาย · มี `team_size_limit` ให้จำกัดได้ถ้าอยากบังคับ |
| **ย้ายทีมกลางเกม = กระโดดเกาะทีมที่กำลังนำ** | ล็อก ย้ายได้เฉพาะตอน `state = 'lobby'` · ยกเว้นคนที่ *ยังไม่เคยมีทีม* (เข้าสายต้องเลือกได้) |
| **ชื่อทีมลูกทัวร์พิมพ์เอง แล้วขึ้นจอใหญ่ต่อหน้าทุกคน** | ทีมงานเปลี่ยนชื่อ/ลบทีมได้จาก `QuizHost` (ต้องมี host_token) · ลบทีมแล้วสมาชิกไม่หายไปด้วย (`ON DELETE SET NULL`) เขาเลือกทีมใหม่ได้ |
| **ชื่อทีมซ้ำกัน** | UNIQUE บน `(session_id, lower(btrim(name)))` — บนจอใหญ่มี "ทีมเสือ" สองอันแล้วงงกันทั้งงาน |

ที่ต้องมีเพิ่มอีกอย่าง: **`quiz_teams` ต้อง publish realtime** ต่างจาก `quiz_players` / `quiz_answers` ที่ตั้งใจไม่ publish — เพราะหน้า lobby ของทุกคนต้องเห็นทีมใหม่โผล่ทันทีที่มีคนสร้าง ไม่งั้นจะตั้งชื่อซ้ำกันเพราะไม่เห็นของคนอื่น ปริมาณก็คนละระดับ (5-8 ทีมต่อเกม ไม่ใช่ 40 คน × 20 ข้อ)

### 16.3 สีทีม — ระบบแจก ไม่ให้เลือกเอง

8 สีที่ต่างกันทั้งเฉดและความสว่าง แจกเรียงตามลำดับที่สร้าง ถ้าให้เลือกเองเดี๋ยวได้สองทีมสีฟ้าใกล้กันจนแยกไม่ออกจากท้ายรถ และมีอิโมจิสีกำกับด้วยเผื่อคนตาบอดสี — เหตุผลเดียวกับรูปทรงของปุ่มตัวเลือก (ข้อ 6.4)

เก็บเป็น `color_index` ไม่ใช่ hex — วันหนึ่งเปลี่ยนจานสี ของเก่าจะเปลี่ยนตามเอง

### 16.4 ปัญหาที่เจอตอนรันบน Supabase จริง (รอบที่สอง)

**`GRANT SELECT` เฉยๆ ไม่ได้จำกัดอะไรเลย**

ตั้งใจให้ `quiz_teams` อ่านได้อย่างเดียว เขียนต้องผ่าน RPC — เขียน `GRANT SELECT ON quiz_teams TO anon` แล้วคิดว่าจบ ทดสอบบนเครื่องก็ขึ้น `permission denied` ตามคาด

แต่บน Supabase **`anon` ได้สิทธิ์เขียนมาตั้งแต่ `CREATE TABLE`** เพราะมี default privileges ตั้งไว้ให้ทุกตารางใหม่ใน schema `public` — `GRANT SELECT` จึงเป็นการเพิ่มสิ่งที่มีอยู่แล้ว ไม่ใช่การจำกัด

ครั้งนี้ RLS กันไว้อีกชั้นจึงยังไม่รั่วจริง (ไม่มี policy สำหรับ INSERT/UPDATE/DELETE = ปฏิเสธ) แต่ถ้าวันหนึ่งมีคนเผลอเพิ่ม policy กว้างๆ จะเปิดช่องทันที

**แก้:** `REVOKE INSERT, UPDATE, DELETE, ... FROM anon, authenticated` ก่อนเสมอ แล้วค่อย GRANT เฉพาะที่ต้องการ

**บทเรียน (ข้อเดียวกับปัญหา `gen_random_bytes` ในข้อ 15):** สภาพแวดล้อมของ Supabase ไม่เหมือน Postgres เปล่าๆ ที่ติดตั้งเอง — เรื่องสิทธิ์และส่วนขยายต้องยืนยันบนของจริงเสมอ ผ่านบนเครื่องตัวเองไม่ได้แปลว่าผ่าน

### 16.5 ผลทดสอบบน Supabase จริง

| ทดสอบ | ผล |
|---|---|
| ลูกทัวร์ตั้งทีมเอง คนตั้งเข้าทีมทันที | ✅ |
| ทีมไม่เท่ากัน 4 คน vs 1 คน → **ผลรวม 1300 แพ้เฉลี่ย 900** | ✅ ทีม 1 คนชนะ ตามที่ตั้งใจ |
| ชื่อทีมซ้ำ (ต่างแค่ช่องว่าง/ตัวพิมพ์) | ✅ ถูกปฏิเสธ |
| ย้ายทีมหลังเกมเริ่ม | ✅ ถูกปฏิเสธ |
| ตั้งทีมใหม่หลังเกมเริ่ม | ✅ ถูกปฏิเสธ |
| คนเข้าสายที่ยังไม่มีทีม เลือกทีมกลางเกม | ✅ ทำได้ |
| ลูกทัวร์ยิง REST แก้/ลบ `quiz_teams` ตรงๆ | ✅ ถูกปฏิเสธ (หลังแก้ข้อ 16.4) |
| ทีมงานเปลี่ยนชื่อ/ลบทีม | ✅ ลบแล้วสมาชิกไม่หาย |

---

## 17. เฟส 4 — คลังชุดคำถาม + สรุปหลังเกม (เสร็จแล้ว)

เฟสนี้ไม่ได้เพิ่มอะไรตอนเล่น แต่ทำให้ควิซ **ใช้ซ้ำได้จริง** แทนที่จะเป็นของใช้แล้วทิ้งรอบเดียว

### 17.1 ผูกชุดคำถามกับปลายทาง

`quiz_sets.destination_id` มีมาตั้งแต่เฟส 1 แต่ยังไม่มี UI — เฟสนี้ต่อให้ครบตาม pattern Definition ↔ Assignment ของ Design v2

- **QuizBuilder** เลือกปลายทางได้ + เก็บชุดเข้ากรุ (`is_archived`)
- **QuizManager** กรองรายการตามปลายทาง

> **ชุดที่ไม่ผูกปลายทาง (`NULL`) ต้องเห็นเสมอไม่ว่ากรองอะไร** — เพราะเป็นชุดกลางแบบ "ความรู้ทั่วไป / เกี่ยวกับบริษัท" ที่ใช้ได้ทุกทริป ถ้ากรองหายไปด้วยจะกลายเป็นของที่หาไม่เจอ

### 17.2 ก็อปชุดคำถาม (`quiz_clone_set`)

**ทำไมจำเป็น ไม่ใช่ของฟุ่มเฟือย:** คลังออกแบบให้ใช้ซ้ำข้ามทริป แต่บางครั้งอยากได้ "ชุดญี่ปุ่นแบบง่าย" แยกจาก "ชุดญี่ปุ่น" เดิม ถ้าไม่มี clone สตาฟต้องพิมพ์ใหม่ 20 ข้อ — ซึ่งไม่มีใครทำ แล้วจบลงที่ **แก้ของเดิมทับ** → ทริปอื่นที่กำลังใช้ชุดนั้นอยู่โดนกระทบไปด้วยโดยไม่มีใครรู้

**บั๊กที่เกือบพลาด:** เขียนครั้งแรกเป็น `INSERT ... SELECT` ก้อนเดียว แล้วจับคู่ข้อเก่า-ข้อใหม่ด้วย `row_number()` เพื่อก็อปเฉลย — ใช้ได้ตอนทดสอบ แต่**ถ้าสองข้อมี `sort_order` เท่ากัน** (เกิดได้จริง เพราะการเรียงข้อไม่ได้บังคับว่าห้ามซ้ำ) ลำดับของฝั่ง source กับฝั่ง `RETURNING` ไม่จำเป็นต้องตรงกัน ผลคือ **เฉลยสลับข้อ** — บั๊กที่เงียบสนิทจนกว่าจะเล่นจริงหน้างานแล้วทั้งห้องตอบถูกกลายเป็นผิด

แก้เป็นลูปทีละข้อ ช้ากว่านิดเดียวแต่รู้ทั้ง id เก่าและ id ใหม่ในรอบเดียวกัน จับคู่ผิดไม่ได้ · ทดสอบด้วยชุดที่จงใจให้ `sort_order` ซ้ำกันแล้ว ผ่าน

### 17.3 สรุปหลังเกม (`quiz_report` + `QuizReport.jsx`)

ค่าของควิซไม่ได้จบตอนเกมจบ ของที่เอาไปใช้ต่อได้จริงคือ **"ข้อไหนใช้ไม่ได้"** เพื่อแก้ก่อนเอาชุดนี้ไปใช้กรุ๊ปหน้า

| อัตราตอบถูก | แปลว่า | ทำไมถึงแย่ |
|---|---|---|
| **≥ 90%** | ง่ายเกินไป | ทุกคนได้คะแนนเท่ากันหมด ไม่ได้คัดใครเลย เสียเวลาไปหนึ่งข้อ |
| **≤ 20%** | ยากเกิน หรือคำถามกำกวม | คนเดามั่วกันทั้งห้อง คะแนนกลายเป็นเรื่องดวง |

ทั้งสองแบบทำให้เกมจืด แต่คนละสาเหตุ หน้ารายงานจึงติดป้ายแยกให้เห็นทันทีว่าเป็นแบบไหน พร้อมสรุปด้านบนว่ามีกี่ข้อในแต่ละกลุ่ม

นอกจากนั้นยังโชว์ **คนตอบผิดไปทางไหน** (จำนวนคนต่อตัวเลือก) — ตัวเลือกที่ดูดคนไปเยอะทั้งที่ผิด มักแปลว่า**คำถามกำกวม** ไม่ใช่คนตอบไม่รู้ ซึ่งเป็นคนละการแก้กัน

### 17.3.1 ลบชุดคำถาม (`quiz_delete_set`)

ปุ่มอยู่ท้ายหน้า QuizBuilder แยกเส้นออกมาเป็น "โซนอันตราย" — ไม่ให้อยู่ในระยะนิ้วโป้งเดียวกับปุ่มที่ใช้บ่อย เพราะสตาฟกดหน้านี้ตอนรีบ

**สิทธิ์เป็น `quiz.define` (แอดมินบริษัท) ไม่ใช่ `quiz.edit`** — ชุดในคลังใช้ร่วมกันทุกทริป ลบทีเดียวหายทั้งบริษัท ไม่ใช่แค่ทริปที่กำลังเปิดอยู่ · หัวหน้าทัวร์แก้ชุดได้แต่ลบไม่ได้

**กติกาที่สำคัญที่สุด: ชุดที่เคยเปิดห้องเล่นไปแล้ว ลบไม่ได้**

`quiz_sessions.set_id` เป็น `ON DELETE RESTRICT` มาตั้งแต่เฟส 1 โดยตั้งใจ — ถ้าลบได้ **รายงานหลังเกมของทริปเก่าจะพังทันที** เพราะไม่เหลือคำถามให้อ้างอิงว่าถามอะไรไป

แต่ RPC **ไม่ raise error** ในกรณีนี้ — คืน `status: 'in_use'` พร้อมจำนวนครั้งที่เคยเล่น เพราะนี่ไม่ใช่ความผิดพลาด แต่เป็นคำตอบที่ต้องเอาไปเสนอทางเลือกที่ถูกต้อง: **"เก็บเข้ากรุแทนไหม"** (ซ่อนจากรายการ ประวัติยังอยู่ครบ) ถ้า raise ไปเฉยๆ ผู้ใช้จะเจอข้อความแดงๆ ที่ทำอะไรต่อไม่ได้ แล้วก็ไม่รู้ว่าควรทำยังไง

**ลบไฟล์สื่อตามด้วย** — RPC คืนรายชื่อ `media_url` ของทุกข้อกลับมาก่อนลบแถว (หลังลบแล้วตามหาไม่เจออีก) แล้วฝั่งแอปเอาไปลบออกจากถัง `quiz-media` ไม่งั้นรูปกลายเป็นไฟล์กำพร้าที่ไม่มีใครรู้ว่ามีอยู่ แล้วกินโควตา Storage 1 GB ไปเรื่อยๆ · ลบไฟล์ไม่สำเร็จไม่ทำให้ "ลบชุด" ล้มเหลวตาม เพราะแถวหายไปจริงแล้ว

ทดสอบบน Supabase จริง: ชุดใหม่ → `deleted` (คำถาม+เฉลย cascade หายครบ) · ชุดที่เคยเล่น → `in_use` ไม่ถูกลบ · ชุดที่ไม่มีอยู่ → `not_found` · PIN ผิด → ปฏิเสธ

### 17.4 ทำไมรายงานใช้ PIN ไม่ใช่ `host_token`

`host_token` อยู่ใน `sessionStorage` ของแท็บที่เปิดห้อง — พอจะมาเปิดรายงานทีหลัง (คนละวัน คนละเครื่อง) token หายไปแล้ว แต่ **PIN ยังอยู่กับตัวคน**

จึงใช้ pattern เดียวกับ `quiz_set_for_edit` และรวม PIN ไว้ที่ `src/lib/quizPin.js` ให้ QuizBuilder / QuizManager / QuizReport ใช้ร่วมกัน — พิมพ์ครั้งเดียวต่อแท็บ ไม่ใช่ทุกหน้า

### 17.5 ผลทดสอบบน Supabase จริง

| ทดสอบ | ผล |
|---|---|
| clone ชุดที่มี `sort_order` ซ้ำกัน | ✅ 3 ข้อครบ **เฉลยตรงทุกข้อ** |
| clone พา `destination_id` / ตั้งค่าชุดไปด้วย | ✅ |
| รายงานอัตราตอบถูกรายข้อ | ✅ 100% / 0% / 0% ตรงกับที่เล่นจริง |
| ข้อที่ยังไม่ได้เล่น | ✅ ขึ้น "ข้ามไป" ไม่ใช่ 0% ปนกับข้อที่ตอบผิดจริง |
| PIN ผิด → clone / ดูรายงาน | ✅ ปฏิเสธทั้งคู่ |

---

## 18. สรุปสถานะทั้งโครงการ

| เฟส | ขอบเขต | สถานะ |
|---|---|---|
| 1 | ระบบหลัก — ห้อง, mcq/tf/numeric, จอเวที, คะแนนความเร็ว, streak | ✅ รันบน Supabase แล้ว |
| 2 | รูป/วิดีโอในคำถาม + ย่อรูปอัตโนมัติ + preload | ✅ รันบน Supabase แล้ว |
| 3 | โหมดทีม (ลูกทัวร์ตั้งทีมเอง, คะแนนเฉลี่ยต่อคน) | ✅ รันบน Supabase แล้ว |
| 4 | คลังตามปลายทาง, clone ชุด, สรุปหลังเกม | ✅ รันบน Supabase แล้ว |

**8 ตาราง · 28 ฟังก์ชัน · 6 หน้าจอ · 3 ภาษา**

### สิ่งที่ยังไม่ได้ทำ และรู้ตัวว่าไม่ได้ทำ

1. **ยังไม่เคยผ่านสนามจริง** — ทุกอย่างในเอกสารนี้ทดสอบบนฐานข้อมูลจริงแล้ว แต่ยังไม่เคยมีลูกทัวร์ 40 คนกดพร้อมกันบนรถที่เน็ตแย่ ตัวเลขอย่าง "poll ทุก 4 วิ" กับ "ผ่อนผัน 200ms" เป็นค่าที่ประมาณเอา ต้องดูของจริงแล้วปรับ
2. **Supabase keep-alive ยังไม่ได้ตั้งค่า** — ดู `AGENTS.md`
3. **สิทธิ์ยังกันที่ชั้นแอปเป็นหลัก** — ควิซปิดเฉพาะ "เฉลย" กับ "เวลา" ซึ่งพอสำหรับเกมชิงรางวัลในงานเลี้ยง การกันจริงทั้งระบบยังต้องรอ Supabase Auth ตาม Design v2 §9

---

## 19. เฟส 5 — หน้าตาจอเวที + ฟอนต์ (เสร็จแล้ว)

### 19.1 สกินจอเวที — สองแบบ ไม่ใช่สาม

ร่างไว้สามทิศทางใน `design-preview/quiz-design-options.html` แล้วเลือกสอง:

| สกิน | เมื่อไหร่ | หน้าตา |
|---|---|---|
| `day` **(ค่าเริ่มต้น)** | ทั่วไป | พื้นเข้มไล่เฉด ไม่ดำสนิท ตัวเลือกทึบ มุมมน 22px ฟอนต์ Noto Sans Thai Looped |
| `neon` | งานเลี้ยงกลางคืนที่ปิดไฟ | พื้นดำสนิท ตัวเลือกเป็น **กรอบเรืองแสงพื้นโปร่ง** มุม 10px ฟอนต์ Chakra Petch |

**ทำไมตัดแบบที่สาม ("ตั๋วกระดาษ" พื้นกระดาษอุ่น หมึกเข้ม) ทิ้ง:** จอเวทีพื้นสว่าง
ในห้องจัดเลี้ยงที่ปิดไฟจะแยงตาคนแถวหน้า ส่วนเคส "จอกลางแดด" ที่แบบนั้นเกิดมาแก้
ถูกจัดการด้วยแกน `screen_mode = 'bus_tv'` อยู่แล้ว — คนละแกนกัน ไม่ต้องมีสกินมาซ้ำ

**ทำไม neon ต้องเป็นกรอบโปร่ง ไม่ใช่บล็อกสีทึบ:** บล็อกสีเต็มจอบนโปรเจกเตอร์
ในห้องมืดสว่างจนอ่านข้อความ*ในบล็อก*ไม่ออก — ปัญหาที่ไม่เจอบนจอคอมตอนพัฒนา

**เก็บที่ไหน:** `quiz_sessions.stage_theme` ซึ่งมีอยู่แล้วตั้งแต่ migration แรก
(ก๊อปมาจาก `draw_rooms` แต่ไม่เคยถูกอ่าน) `20260819_quiz_stage_skin.sql` เปลี่ยน
default เป็น `'day'` + ใส่ CHECK ให้เหลือสองค่า + ย้ายห้องเก่าที่ค้าง `'led'` ไป `'neon'`
เพราะใกล้ของเดิมที่สุด (จอควิซแบบเก่าเป็นพื้นดำตัวหนังสือขาว)

**สกินไม่แตะมือถือ** — มือถือแขกใช้ token ของแอปเสมอ ตามหลักเดิมของเกมแบบนี้:
จอบอกว่าอะไร มือถือมีแค่ปุ่มสี่สี ยิ่งมือถือนิ่งยิ่งดี

### 19.2 รูปทรงตัวเลือก — เลิกใช้กลิฟข้อความ

เดิมเรนเดอร์ `▲ ◆ ● ■` เป็นตัวอักษรตรงๆ ปัญหาคือฟอนต์วาดสี่ตัวนี้มาคนละสัดส่วน
ตั้ง `font-size` เท่ากันแต่ `●` กับ `■` ออกมาเล็กกว่า `▲` ราว 40% บนจอเวทีเห็นชัด
จนดูเหมือนตัวเลือกสองอันสำคัญกว่าอีกสองอัน

`components/quiz/OptionShape.jsx` วาดด้วย `clip-path` แล้วไล่ขนาดกล่องให้ทุกรูปมี
**พื้นที่หมึกเท่ากัน** (ไม่ใช่กล่องเท่ากัน — กล่องเท่ากันคือต้นเหตุเดิม):

| รูป | พื้นที่เทียบกล่อง | ขนาดกล่องที่ใช้ |
|---|---|---|
| สามเหลี่ยม / ข้าวหลามตัด | ครึ่งกล่อง | 1.000 |
| วงกลม | π/4 | 0.798 |
| สี่เหลี่ยม | เต็มกล่อง | 0.707 |
| กากบาท (tf) | แขนหนา 0.243 | 1.000 |

ผลพลอยได้: ไม่ต้องพึ่งฟอนต์ที่มีกลิฟพวกนี้ — ทีวี Android บนรถบางรุ่นไม่มี
แล้วขึ้นเป็นสี่เหลี่ยมว่าง ใช้ที่ `QuizStage`, `Quiz` (แขก), `QuizHost`,
`QuizBuilder` (พรีวิวต้องตรงกับของจริง) และ `QuizReport`

### 19.3 ฟอนต์ย้ายมา self-host ทั้งหมด

`src/index.css` เดิมขึ้นต้นด้วย `@import` จาก `fonts.googleapis.com` ซึ่งมีปัญหาสามข้อ:

1. แอปเป็น PWA แต่ workbox precache ได้เฉพาะไฟล์ใน `public/` — ฟอนต์จาก CDN
   ไม่ถูก cache หัวหน้าทัวร์ที่เปิดแอปตอนไม่มีเน็ตจึงได้ฟอนต์ระบบ
2. `@import` ใน CSS เป็น render-blocking ต่อกันสองชั้น
3. **ธีมจอสุ่มรางวัลใน `lib/drawStage.js` เรียก Kanit / Chakra Petch / Anuphan
   ซึ่งไม่เคยถูกโหลดที่ไหนเลย** ทั้ง 5 ธีมจึงเรนเดอร์ด้วยฟอนต์เดียวกันหมด
   คนกดสลับธีมเห็นแค่สีเปลี่ยน — บั๊กที่อยู่มานานโดยไม่มีใครสังเกต

ตอนนี้ประกาศ `@font-face` ครบ 26 รายการชี้ไป `public/fonts/` ทั้งหมด

**Bold ประกาศเป็นช่วง `700 900`** — ไฟล์ที่ self-host มีถึง 700 แต่โค้ดใช้
`font-black` (900) กับ `font-extrabold` (800) อยู่ 118 จุด ถ้าประกาศแค่ 700
เบราว์เซอร์จะทำหนาปลอมด้วยการถ่างเส้น ซึ่งกับตัวไทยมีหัวจะทำให้หัวตันและสระเบียดกัน
การประกาศเป็นช่วงคือบอกเบราว์เซอร์ว่า "ขอ 900 ก็ใช้ไฟล์นี้ อย่าปลอม"

**ยังค้าง:** `GoogleSans-{Regular,Medium}.woff2` ยังไม่ได้ subset (474 + 533 KB)
คิดเป็น ~78% ของฟอนต์ทั้งหมดที่ service worker ต้อง precache
วิธี subset อยู่ใน `public/fonts/README.md`
