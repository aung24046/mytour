## Imported Claude Cowork project instructions

---

# ⚠️ งานค้างที่ต้องทำ (Pending TODO)

> **ถึง AI ที่มาอ่านไฟล์นี้:** รายการข้างล่างคืองานที่ตัดสินใจแล้วแต่ยังไม่ได้ทำ
> ถ้าเจ้าของโปรเจกต์บอกว่าทำเสร็จแล้ว หรือคุณเป็นคนทำเสร็จเอง
> **ให้ลบหัวข้อนั้นออกจากไฟล์นี้ทันที** และถ้าไม่เหลือรายการไหนเลย
> ให้ลบทั้งบล็อก "งานค้างที่ต้องทำ" นี้ออกไป เหลือแค่บรรทัดบนสุดของไฟล์
> อย่าปล่อยให้ไฟล์นี้สะสมงานที่ทำไปแล้ว — ไม่งั้นครั้งหน้าจะไม่มีใครเชื่อมันอีก

## 1. ติดตั้ง dependency ใหม่ของเกม "เปิดแผ่นป้าย"

**สถานะ:** โค้ดเขียนเสร็จ · รัน migration แล้ว · **build ผ่านการตรวจในคอนเทนเนอร์แยกแล้ว**
เหลืออย่างเดียวคือ **ยังไม่ได้ `npm install` บนเครื่องนี้ จึงยังรัน dev/build ที่นี่ไม่ได้**

เกมนี้เพิ่ม dependency สองตัวใน `package.json`: `cropperjs` และ `react-cropper`
(ใช้ครอปภาพก่อนอัปโหลดใน `TilesBuilder`)

**ที่ต้องทำ:** รัน `npm install` บนเครื่อง Windows
(`node_modules` ในโฟลเดอร์เป็นของ Windows — ติดตั้งใน Linux VM แล้วเอามาใช้ไม่ได้
และถ้าเผลอรัน `npm install` ใน VM จะทับ binary ของ Windows จน `npm run dev` พัง)

**ไม่ต้องตรวจ chunk เองแล้ว — ตรวจไปแล้วเมื่อ 9 ก.ย. 2026 ในคอนเทนเนอร์แยก ผลคือ:**

- `npm run build` ผ่าน · 465 โมดูล · 10 วินาที
- `dist/assets/image-cropper-*.js` = 57 KB (gzip 19 KB) แยกออกมาเป็นก้อนเดียวจริง
- `dist/sw.js` ไม่มีคำว่า cropper เลย → ลูกทัวร์ไม่ต้องโหลดไลบรารีแต่งรูป
- `index-*.js` ไม่ได้ฝัง cropperjs มาด้วย → lazy ทำงานจริง

⚠️ **ถ้าวันหลังแก้ `manualChunks` หรือ `globIgnores` ใน `vite.config.js` ต้องตรวจสามข้อบนใหม่**
เพราะพลาดแล้วไม่มีอะไรฟ้อง แอปทำงานปกติทุกอย่าง แค่ทุกคนโหลดหนักขึ้นเงียบๆ

ลบหัวข้อนี้ทิ้งเมื่อ `npm install` เสร็จและ `npm run dev` ขึ้นได้

<!-- อ้างอิงเหตุผลเต็ม: MyTour_TileReveal_Design_v1.md ข้อ 9.3 -->

## 2. ตั้งค่า Supabase keep-alive ให้เสร็จ

**สถานะ:** เขียนโค้ดไว้แล้ว แต่ **ยังไม่ได้ตั้งค่า จึงยังไม่ทำงาน**

**ปัญหาที่จะเกิดถ้าไม่ทำ:** Supabase Free plan หยุดโปรเจกต์ที่ไม่มีการใช้งานติดกัน 7 วัน
บริษัททัวร์ไม่ได้ออกทริปทุกสัปดาห์ — ถ้าเว้นไป 10 วันแล้ววันออกทริปเปิดแอปมา
จะเจอ **แอปตายทั้งระบบ** (ไม่ใช่แค่ควิซ) ต้องเข้า Supabase Dashboard กด restore ก่อน
ซึ่งไม่ใช่สิ่งที่อยากทำตอนยืนอยู่หน้ารถบัสกับลูกทัวร์ 40 คน

**ของที่มีอยู่แล้ว:** `.github/workflows/supabase-keepalive.yml`
ยิง query จริงเข้า REST API ทุกวันจันทร์–พุธ–ศุกร์

**ที่ยังต้องทำ (ทำใน GitHub ไม่ใช่ในโค้ด):**

1. ไปที่ repo → Settings → Secrets and variables → Actions → New repository secret
2. เพิ่ม 2 ตัว:
   - `SUPABASE_URL` = `https://iirhnjoqpwwwdgoghnkc.supabase.co`
   - `SUPABASE_ANON_KEY` = anon key ตัวเดียวกับใน `.env`
     (คีย์นี้อยู่ใน bundle หน้าบ้านอยู่แล้ว ไม่ใช่ความลับใหม่ — **ห้ามใส่ `service_role`**)
3. แท็บ Actions → เลือก "Supabase keep-alive" → Run workflow → ต้องได้ HTTP 200

**ข้อจำกัดที่ต้องรู้:**

- GitHub ปิด scheduled workflow อัตโนมัติถ้า repo ไม่มี commit เลย 60 วัน
- ถ้า workflow พังเงียบๆ จะไปรู้ตัวเอาวันออกทริป — workflow ตั้งให้ `exit 1` เมื่อไม่ได้ 200
  แล้ว GitHub จะส่งอีเมลแจ้ง **อย่าปิดการแจ้งเตือนนั้น**
- นี่คือการเลี่ยงเงื่อนไข ไม่ใช่การแก้ที่ต้นเหตุ Supabase เปลี่ยนกติกาเมื่อไหร่ก็ได้

**ทางแก้ถาวร:** ขึ้น Supabase Pro ($25/เดือน) — ไม่มีการ pause เลย และได้ 8 GB DB /
100 GB storage / 250 GB egress มาด้วย
**ถ้าขึ้น Pro แล้ว ให้ลบ `.github/workflows/supabase-keepalive.yml` ทิ้ง แล้วลบหัวข้อนี้ออกจากไฟล์นี้ด้วย**

<!-- อ้างอิงเหตุผลเต็ม: MyTour_Quiz_Design_v1.md — คุยกันไว้ตอนประเมินโควตา Supabase -->
